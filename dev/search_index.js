var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/connectivity/#Connectivity","page":"Connectivity","text":"","title":"Connectivity"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.AbstractImageConvention","page":"Connectivity","text":"AbstractImageConvention\n\nHow a ball query treats a periodic direction: NearestImage or AllImages.\n\nSingleton types, like a stencil. The traversal branches on this per candidate; as a type the branch and the coordinate expression behind it resolve at compile time, and the walk allocates nothing.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.AbstractImageConvention"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.AbstractReach","page":"Connectivity","text":"AbstractReach\n\nWhich of the cells within ball a query returns: Unrestricted, every one of them, or Connected, those reachable from the seed without leaving the ball.\n\nA type, like AbstractImageConvention. The two are different sets computed by different algorithms, so the choice is visible in the call and fixed at compile time.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.AbstractReach"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.AllImages","page":"Connectivity","text":"AllImages()\n\nVisit every image of a cell that lands inside the ball, each carrying its own displacement — the set a periodic convolution sums over. See fold_within.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.AllImages"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.CSRConnectivity","page":"Connectivity","text":"CSRConnectivity{VN,VP}\n\nSparse neighbor list: node i owns nbrs[ptr[i]:ptr[i+1]-1]. The two buffers are typed independently, and their element type is any Integer, so a large mesh can hold Int32 indices (half the memory and bandwidth of Int64, and the width GPU kernels want).\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.CSRConnectivity"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.Connected","page":"Connectivity","text":"Connected()\nConnected(stencil)\n\nThe cells within ball reachable from the seed through cells that are themselves within ball and active: the connected component of the ball that contains the seed. A graph query — it expands along adjacency and prunes at the ball's edge — so it is a subset of Unrestricted. The two agree whenever the ball is connected under the adjacency, which a maskless Cartesian StructuredGrid always is: each index step moves monotonically in one coordinate, so a staircase path to a cell never leaves that cell's own distance. Connected is strictly smaller wherever a mask or a boundary separates two parts of the ball.\n\nAdjacency has to be named, since only a node set carries its own: with no argument, direction-1 adjacency (Stencils.Axial(1)) on the index-space architectures and the stored neighbour lists on an UnstructuredGrid. Connected(stencil) sets it explicitly for the former, and is an error for the latter, which has no index space for a stencil to mean anything in.\n\nThis is not a cheaper way to compute Unrestricted. Take cells P (the seed), Q and R, adjacent only as P–Q–R, with d(P,Q) = 1.2r and d(P,R) = 0.8r. A walk outward from P that drops any cell farther than r stops at Q and never reaches R, though R is inside the ball. Such a walk computes Connected; the ball is a different set, reached only by a spatial query.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.Connected"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.ConnectedScratch","page":"Connectivity","text":"ConnectedScratch{T}\n\nThe buffers a Connected query needs: the ball's cells and their distances, the visited flags, the breadth-first order, the queue, and the candidate buffer the ball pass itself takes.\n\nOne per task, exactly as ball_scratch is — every buffer is written per query, so two tasks cannot share a set. Build one with connected_scratch and pass it as scratch. A query given no scratch borrows one from a per-task pool for its duration and returns it, so repeated queries on one task allocate once; naming your own pins the buffers to a lifetime you control.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.ConnectedScratch"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.FormulaNeighborSeq","page":"Connectivity","text":"FormulaNeighborSeq{GR,K}\n\nLazy neighbour sequence of one cell of a layout whose adjacency is arithmetic.\n\nHolds the tuple Grids.formula_neighbors returned, so iterating it touches no heap: the counterpart of StencilNeighbors and MeshNeighbors for the third kind of adjacency.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.FormulaNeighborSeq"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.IndexTopology","page":"Connectivity","text":"IndexTopology(size, periodic, mask)\nIndexTopology(grid)\n\nExtent, wrapping and activity per dimension — the whole of what a neighbor computation reads. Coordinates, cell measure and geometry never enter one, so a sampling hands this over directly, with no grid (axes, dense measure, full mask) built to be read once and dropped. A curvilinear grid is the N = 2 case of the same algorithm.\n\nmask === nothing means every cell is active, and costs no storage and no load.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.IndexTopology"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.MeshNeighbors","page":"Connectivity","text":"MeshNeighbors{GR}\n\nLazy neighbour sequence of one node of a stored-incidence layout: iterating it walks the node's CSR block and yields each active neighbour.\n\nThe counterpart of StencilNeighbors: a traversal over every node allocates nothing.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.MeshNeighbors"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.MetricTopology","page":"Connectivity","text":"MetricTopology(grid; index = nothing)\n\nWhat a distance query reads that depends on the grid alone: the tightest per-direction step bound, which sizes the search window, the coordinate span, and a spatial index where the layout has no separable axes to bound with. IndexTopology is its counterpart for a stencil query.\n\nConstructing one is O(1) on a rectilinear grid, so passing topology to a query is optional. The index is worth hoisting: building a k-d tree for a single query costs more than the scan it replaces, so it is left out by default. foreach_within and mapreduce_within hoist one for a sweep, and indexed builds one explicitly.\n\nGrid types are immutable and the Adapt extension reconstructs them field by field for a device, so this is a separate value and never a cache field on the grid.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.MetricTopology"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.NearestImage","page":"Connectivity","text":"NearestImage()\n\nVisit each cell at most once, at its nearest image. The neighbour-set convention, and the default.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.NearestImage"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.StencilNeighbors","page":"Connectivity","text":"StencilNeighbors{G,N,S}\n\nLazy neighbor sequence of one cell of an index-topology grid: iterating it walks the stencil offsets and yields the linear index of each in-range, active neighbor.\n\nNothing is stored, so a traversal that visits every cell allocates nothing at all. Use neighbors! to write into a caller-supplied buffer, or collect this to materialize it.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.StencilNeighbors"},{"category":"type","location":"api/connectivity/#FlowGeometries.Connectivity.Unrestricted","page":"Connectivity","text":"Unrestricted()\n\nEvery cell whose centre lies within ball. The default, and a purely spatial query: with a spatial index it costs O(log n + m) and never consults adjacency.\n\nNote that the result is a ball, not a connected patch — with a mask, or a concave domain, it can contain cells that are near the seed in space but reachable from it only by leaving the ball.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.Unrestricted"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._buffered_candidates-Tuple{Nothing}","page":"Connectivity","text":"_buffered_candidates(index) -> Bool\n\nWhether an index has to materialize a candidate list to be queried.\n\nA cell list folds its bins directly and a bare scan has nothing to buffer, so both answer a query with no per-task storage, and a sweep over them runs per index, on a device included. A tree deduplicates the periodic images it searches over, so it needs a buffer, and its sweep needs chunks to give each task one.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._buffered_candidates"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._csr_from_candidates-Union{Tuple{F}, Tuple{F, Integer, Integer}} where F","page":"Connectivity","text":"_csr_from_candidates(emit!, n, maxdeg) -> CSRConnectivity\n\nBuild CSR for n nodes whose degree is bounded by maxdeg. emit!(buf, lo, i) writes node i's candidate neighbors into buf[lo+1 : lo+maxdeg] and returns how many it wrote; duplicates, self and out-of-range entries are removed here. emit! must touch only its own slice, and it is called twice per node — once to count the row, once to fill it — so it must be a pure function of i.\n\nCount, scan, fill. The scratch is one maxdeg buffer per task, and both node passes write only slots their own node owns, so both parallelize under a threaded backend.\n\nHost backends only: a work-item-per-node form needs maxdeg slots per item, an n · maxdeg scratch, and a device backend raises here. The grid layouts reach the same graph through build_connectivity on Grids.FormulaNeighbors, which is device-resident end to end.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._csr_from_candidates"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._csr_from_undirected_edges-Tuple{Integer, Any}","page":"Connectivity","text":"_csr_from_undirected_edges(nnodes, edges) -> CSRConnectivity\n\nBuild CSR from an undirected edge list, by counting degrees first and then filling each node's slot range directly — no intermediate per-node vectors, and no reallocation while filling.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._csr_from_undirected_edges"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._csr_total-Tuple{AbstractVector, Int64, Any}","page":"Connectivity","text":"_csr_total(deg, n, backend) -> Int\n\nThe CSR's edge count, sum(deg), reduced wherever the counting pass ran.\n\nA builder takes _index_type(max(n + 1, total)) from it and calls its filling pass through a branch that fixes the type at each call site. Vector{I} for an I the compiler cannot see is a dynamic call whose buffers are typed abstractly for the whole of that pass.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._csr_total"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._cubed_neighbor-NTuple{6, Int64}","page":"Connectivity","text":"Exact face-neighbor under offset (di,dj) for the gnomonic cubed sphere matching SphericalSampling._cubed_face_to_xyz / cubed_sphere_points!.\n\nPanel interiors stay on-face. Crossing an edge uses cube face adjacency with index maps derived by matching cube XYZ along shared edges. Diagonal (corner) exits return (0,0,0) — no unique adjacent face.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._cubed_neighbor"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._fold_healpix_neighbors-Union{Tuple{F}, Tuple{F, Any, Integer, Integer}} where F","page":"Connectivity","text":"_fold_healpix_neighbors(f, acc, nside, ipix0) -> acc\n\nThread acc = f(acc, ipix) over the RING-scheme topological neighbours of 0-based pixel ipix0, in the order SW, W, NW, N, NE, E, SE, S, skipping the ones that do not exist at the eight singular pixels.\n\nThe walk itself, so the buffer form and the tuple form share one copy of the arithmetic. The accumulator is threaded as a value, so the tuple form builds its result on the stack.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._fold_healpix_neighbors"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._ico_neighbor_ids-Tuple{Integer, Int64}","page":"Connectivity","text":"_ico_neighbor_ids(id, ν) -> (NTuple{6,Int}, n)\n\nA geodesic vertex's neighbours: the six barycentric lattice steps, taken on every face the vertex sits on and resolved back to global ids.\n\nA vertex on a macro-edge or at a corner sits on several faces, and the steps along a shared edge land on the same vertex from each of them, so the duplicates are dropped. What survives is five neighbours at a corner and six everywhere else.\n\nArithmetic in (id, ν) alone, so the grid layout and the sampling's own build_connectivity read the same adjacency from it.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._ico_neighbor_ids"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._icosahedral_dual_areas-Union{Tuple{TV}, Tuple{TG}, Tuple{FlowGeometries.Geometry.AbstractSphericalGeometry{TG}, AbstractArray{Tuple{TV, TV, TV}, 1}, AbstractVector{Tuple{Int64, Int64, Int64}}, Integer}} where {TG<:AbstractFloat, TV<:AbstractFloat}","page":"Connectivity","text":"_icosahedral_dual_areas(geometry, verts, triangles, nvert) -> Vector\n\nExact spherical-Voronoi dual-cell areas from the mesh's own triangulation — no convex hull, no optional dependency.\n\nEach triangle is divided among its three vertices by the three arcs from its circumcenter O to its edge midpoints. Those arcs are the perpendicular bisectors of the edges — O is equidistant from all three vertices and each midpoint is equidistant from its two — so vertex a's share is the spherical quadrilateral (a, M_ab, O, M_ca), exactly its Voronoi cell restricted to that triangle. The three shares tile the triangle, so accumulating over all 20ν² triangles tiles the sphere and the areas sum to 4πR² identically. Ordering the cells' corners is never needed, so there is no per-vertex sortperm and no incident-triangle list.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._icosahedral_dual_areas"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._index_type-Tuple{Integer}","page":"Connectivity","text":"_index_type(m) -> Type{<:Integer}\n\nThe narrowest integer that holds values up to m: Int32 for anything under two billion.\n\nA builder passes the larger of the node count and the edge count, so a single width serves both buffers — nbrs holds node ids, ptr holds offsets into nbrs. At one width the two arrays go straight to a SparseMatrixCSC, which declares one index type for both.\n\nHalves the CSR's memory and the bandwidth every traversal of it costs, and is the width a device kernel wants.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._index_type"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity._sort_ball!","page":"Connectivity","text":"_sort_ball!(idxs, ds, lo, hi) -> nothing\n\nSort the ball by linear index, carrying each cell's distance with it.\n\nMembership is then a binary search: the walk tests it once per (cell, neighbour) pair, and searchsortedfirst over a sorted Vector{Int} beats hashing at these sizes and allocates no dictionary per query.\n\nBoth arrays move together in place, so there is no permutation vector; sortperm plus two permute! is three allocations. Quicksort with a median-of-three pivot, recursing on the smaller side so the depth stays O(log m), and insertion sort for a short span.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._sort_ball!"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._sort_unique_filter!-Tuple{AbstractVector{<:Integer}, Vararg{Int64, 4}}","page":"Connectivity","text":"_sort_unique_filter!(buf, lo, m, self, n) -> Int\n\nSort buf[lo+1 : lo+m] in place, drop duplicates, self-references, and out-of-range indices, and return the surviving count (left packed at the front of the slice).\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._sort_unique_filter!"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity._stencil_closed-Union{Tuple{N}, Tuple{FlowGeometries.Connectivity.IndexTopology{N}, FlowGeometries.Stencils.AbstractStencil, NTuple{N, Int64}}} where N","page":"Connectivity","text":"_stencil_closed(t, stencil, I) -> Bool\n\nWhether cell I is active and every offset from it lands in range on an active cell — the one question both interior and boundary_cells ask, so they cannot answer it differently.\n\nThe offset walk is unrolled at compile time through Stencils.fold_offsets\n\n\n\n\n\n","title":"FlowGeometries.Connectivity._stencil_closed"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.adjacency_matrix!-Tuple{AbstractMatrix{Bool}, FlowGeometries.Connectivity.CSRConnectivity}","page":"Connectivity","text":"adjacency_matrix!(A, conn) -> A\nadjacency_matrix!(A, grid; stencil=Axial(1), active_only=true) -> A\n\nFill preallocated N×N A. Grid overload uses the stencil directly (no CSR alloc).\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.adjacency_matrix!"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.adjacency_matrix-Tuple{FlowGeometries.Connectivity.CSRConnectivity}","page":"Connectivity","text":"adjacency_matrix(grid_or_conn; kwargs...) -> Matrix{Bool}\n\nDense n × n adjacency over the n nodes.\n\nThis allocates n² bytes, which is quadratic in the node count and therefore quartic in the side of a 2-D grid: a 1000×1000 grid has 10⁶ nodes and so needs ~10¹² bytes. Dense adjacency is for small node counts and for testing. For anything else use sparse_adjacency_matrix, which stores nedges entries, or neighbors!, which answers neighbour queries from the index stencil with no graph storage at all.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.adjacency_matrix"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.ball_scratch-Tuple{}","page":"Connectivity","text":"ball_scratch() -> Vector{Int}\n\nA candidate buffer to hand to repeated ball queries through their scratch argument. One buffer per task; an indexed query then holds one allocation across any number of calls.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.ball_scratch"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.boundary_cells-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Connectivity","text":"boundary_cells(grid; stencil = Stencils.Axial(1), backend = nothing) -> Array{Bool}\n\nThe active cells interior excludes: those touching an edge or a masked-out neighbour.\n\nOne pass and one array: a cell is a boundary cell exactly when it is active and its stencil is not closed, the same predicate interior reads.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.boundary_cells"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.build_connectivity","page":"Connectivity","text":"build_connectivity(grid; stencil=Axial(1), active_only=true) -> CSRConnectivity\n\nMaterialize CSR adjacency.\n\nWhich builder runs is Grids.adjacency_source, the trait the per-cell neighbour queries resolve through, so a layout gets this by declaring how its adjacency is defined and nothing else: Grids.IndexStencilNeighbors ranges the stencil over an index space, Grids.FormulaNeighbors evaluates the arithmetic per cell, and Grids.StoredMeshNeighbors wraps the mesh's own buffers. stencil selects among the offsets of the first, which is the one whose adjacency is a stencil shape.\n\nFor adjacency by physical distance, see build_connectivity_within.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.build_connectivity"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.build_connectivity-Tuple{FlowGeometries.Grids.AbstractGrid, FlowGeometries.Grids.FormulaNeighbors}","page":"Connectivity","text":"build_connectivity(grid; active_only = true) -> CSRConnectivity\n\nThe adjacency of a layout whose neighbours are arithmetic, materialized.\n\nCount, scan, fill — the same three passes the index-stencil builder makes, and for the same reason: both cell passes write only slots their own cell owns, so they carry no running offset and need no coordination. The adjacency is a per-cell formula, so the whole shape it takes is that formula's.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.build_connectivity"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.build_connectivity-Tuple{FlowGeometries.SphericalSampling.AbstractTensorProductSphericalSampling, Integer}","page":"Connectivity","text":"build_connectivity(sampling, nlat; nlon, mask, periodic, stencil, active_only)\n\nSampling topology straight to CSR. A tensor-product sampling's neighbor graph is fixed by its axis lengths and longitude wrapping alone, so the axes themselves are never evaluated — for Gauss–Legendre that is an O(n²) root solve.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.build_connectivity"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.build_connectivity-Tuple{FlowGeometries.SphericalSampling.CubedSphereSampling, Integer}","page":"Connectivity","text":"build_connectivity(::CubedSphereSampling, n; stencil=Axial(1)) -> CSRConnectivity\n\nSix-panel gnomonic cubed sphere with cross-face seams. Indexing matches SphericalSampling.cubed_sphere_points!.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.build_connectivity"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.build_connectivity-Tuple{FlowGeometries.SphericalSampling.HEALPixSampling}","page":"Connectivity","text":"build_connectivity(s::HEALPixSampling) -> CSRConnectivity\n\nHEALPix RING topological adjacency (usually 8 neighbors; 7 or 6 at singular pixels). Julia node indices are 1-based (pixel 0 → node 1).\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.build_connectivity"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.build_connectivity-Tuple{FlowGeometries.SphericalSampling.IcosahedralSampling}","page":"Connectivity","text":"build_connectivity(s::IcosahedralSampling) -> CSRConnectivity\n\nUndirected edges of the frequency-ν geodesic triangulation (same vertex set as SphericalSampling.icosahedral_vertices).\n\nA vertex's neighbours are arithmetic in (ν, id) — see _ico_neighbor_ids — so this is the same count, scan, fill the layout's own builder makes, over 10ν² + 2 vertices. The triangulation's 20ν² triangles and 30ν² edges are never formed.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.build_connectivity"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.build_connectivity-Tuple{FlowGeometries.SphericalSampling.YinYangSampling, Integer, Integer}","page":"Connectivity","text":"build_connectivity(::YinYangSampling, nlon, nlat; stencil=Axial(1)) -> CSRConnectivity\n\nPanel-local face/vertex stencils on yin then yang. Global ordering matches SphericalSampling.spherical_points! for Yin–Yang: yin (nlon×nlat, lon fastest), then yang with the same panel indexing. The overlap is not cross-linked: this is the standard Yin–Yang discrete topology, where the panels couple through interpolation.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.build_connectivity"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.build_connectivity_within","page":"Connectivity","text":"build_connectivity_within(grid; ball, active_only=true) -> CSRConnectivity\n\nMaterialize the CSR adjacency of every pair of cells within ball of each other — the bulk form of neighbors_within, row k holding exactly what the per-cell query returns for cell k.\n\nSymmetric by construction, since the metric is.\n\nWhere a separable window can bound the candidates the default topology needs no index; elsewhere it carries a cell list, built once and amortized over the n rows, so a row costs O(log n + m). Pass topology = MetricTopology(grid) for the scanning build, or an indexed topology for the k-d tree.\n\nRows are balls, i.e. Unrestricted, and there is no Connected form: reachability within one cell's ball is asymmetric — a bridge cell can lie in one cell's ball while that cell lies outside its own — so such a graph is not an adjacency.\n\nFor a mesh-degree radius\n\nThe result is ∑ᵢ degree(i) integers, and a ball's degree grows with the cube (or square) of its radius. At a radius of a few cell widths that is mesh-degree adjacency, built once and read row by row many times. At a radius of tens of widths the degree reaches 10³–10⁴, and on a million cells the graph is 10⁹–10¹⁰ integers: tens of gigabytes.\n\nAsk neighbors_within!, fold_within or mapreduce_within instead. Those go through the same index and visit the same cells, one row at a time, and never hold more than one row.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.build_connectivity_within"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.connected_components-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Connectivity","text":"connected_components(grid; stencil = Stencils.Axial(1), active = true) -> (labels, ncomponents)\n\nLabel the connected components of the active region (or of the inactive region with active = false), by flood fill honouring the grid's own wrapping. labels is 0 off the region and 1:ncomponents on it.\n\nThere is no backend: a fill's next cell is decided by the cells already labelled, so the traversal is sequential. interior and boundary_cells are the per-cell questions, and both take one.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.connected_components"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.connected_scratch-Union{Tuple{}, Tuple{Type{T}}, Tuple{T}} where T","page":"Connectivity","text":"connected_scratch([T = Float64]) -> ConnectedScratch{T}\n\nBuffers for a Connected query, so a caller making many of them allocates none. T is the grid's coordinate type, which its distances are reported in.\n\nThe buffers grow to the largest ball seen and are reused, so the first query on a new size is the only one that allocates.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.connected_scratch"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.count_holes-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Connectivity","text":"count_holes(grid; stencil = Stencils.Axial(1)) -> Int\n\nHow many connected inactive regions are fully enclosed by active cells — the number of holes in the active region, and so an estimate of its first Betti number.\n\nA region that reaches a non-wrapping edge counts as outside. Along a wrapping direction there is no edge to reach, so enclosure there is decided by the fill alone.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.count_holes"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.csr_connectivity-Tuple{AbstractVector{<:Integer}, AbstractVector{<:Integer}}","page":"Connectivity","text":"csr_connectivity(nbrs, ptr; validate=true) -> CSRConnectivity\n\nWrap CSR buffers. validate=false skips O(nnz) checks (internal / trusted data).\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.csr_connectivity"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.empty_csr-Union{Tuple{Integer}, Tuple{I}, Tuple{Integer, Type{I}}} where I<:Integer","page":"Connectivity","text":"empty_csr(nnodes, [I=Int]) -> CSRConnectivity\n\nAdjacency in which every node has no neighbors, with I-typed indices.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.empty_csr"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.fold_at","page":"Connectivity","text":"fold_at(f, init, grid, p; ball, active_only=true, topology, scratch) -> acc\n\nFold acc = f(acc, J, d) over every cell J within ball of the point p, d being its distance. p is a coordinate in the grid's own coordinates, written any way a point is accepted elsewhere; fold_within is this at a cell centre.\n\nThere is no cell to exclude, so unlike the cell-seeded form every cell within ball is visited.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.fold_at"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.fold_within","page":"Connectivity","text":"fold_within(f, init, grid, I...; ball, images=NearestImage(), self=false, active_only=true)\n\nFold acc = f(acc, J, d) over every cell J within ball of cell I, d being its distance. The traversal every distance query here is built on; the accumulator is threaded through as a value, so nothing is captured and nothing is boxed.\n\nimages selects how a periodic direction is treated — NearestImage (the default, each cell once, the convention neighbors_within! exposes) or AllImages, which visits every image of a cell that lands inside the ball, each carrying its own displacement.\n\nAllImages is what a periodic convolution needs: on a torus of period L, f̄(x) = Σₖ ∫ K(x − y − kL) f(y) dy, so where the kernel support exceeds L/2 one cell contributes through several images at different displacements, and keeping only the nearest drops the rest. Below L/2 the two conventions coincide exactly. It also widens the search: the window becomes the uncapped ceil(r/s) per periodic direction, in place of metric_window's one-turn cap. It raises where a periodic direction is an angular identification.\n\nself = true also folds the centre cell, at distance zero. The default excludes it, matching a neighbour set; a convolution needs it, and it carries the kernel's largest weight.\n\nreach selects the ball (Unrestricted) or the part of it reachable from the seed without leaving it (Connected); topology and scratch are as in neighbors_within!.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.fold_within"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.foreach_within-Union{Tuple{F}, Tuple{F, FlowGeometries.Grids.AbstractGrid}} where F","page":"Connectivity","text":"foreach_within(f, grid; ball, …) -> nothing\n\nCall f(I, J, d) for every cell I of grid and every cell J within ball of it. The same hoisting as mapreduce_within, for an f that writes.\n\nUnder a threaded backend, f runs on disjoint spans of cells concurrently, so what it writes has to be determined by I — the same contract the connectivity builders keep.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.foreach_within"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.healpix_neighbor_ids-Tuple{Integer, Integer}","page":"Connectivity","text":"healpix_neighbor_ids(nside, ipix0) -> (NTuple{8,Int}, n)\n\nhealpix_neighbors! into a stack tuple: the n neighbours in the first n entries, still 0-based. Nothing is allocated, so a traversal over every pixel holds no buffer.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.healpix_neighbor_ids"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.healpix_neighbors!-Tuple{AbstractVector{<:Integer}, Integer, Integer}","page":"Connectivity","text":"healpix_neighbors!(out, nside, ipix0) -> n_written\n\nRING-scheme topological neighbors of 0-based pixel ipix0. Writes up to 8 0-based neighbor indices into out, skipping the neighbors that do not exist at the eight singular pixels. Order: SW, W, NW, N, NE, E, SE, S.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.healpix_neighbors!"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.indexed-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Connectivity","text":"indexed(grid) -> MetricTopology\n\nA MetricTopology carrying a spatial index, which brings a ball query to O(log n + m).\n\nRequires NearestNeighbors — Grids.spatial_index raises when it is not loaded. An unindexed topology answers the same queries in O(n).\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.indexed"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.interior-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Connectivity","text":"interior(grid; stencil = Stencils.Axial(1), backend = nothing) -> Array{Bool}\n\nWhich active cells have their whole stencil active and in range. false at a domain edge that does not wrap, and beside any masked-out cell.\n\nEach cell's answer depends on its own neighbourhood alone, so backend runs the cells concurrently.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.interior"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.is_symmetric_adjacency-Tuple{FlowGeometries.Connectivity.CSRConnectivity}","page":"Connectivity","text":"is_symmetric_adjacency(conn) -> Bool\n\nWhether j ∈ N(i) implies i ∈ N(j) throughout. O(nedges + nnodes), by comparing the graph with its transpose; it guards the shortcut that reads a CSR as a CSC.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.is_symmetric_adjacency"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.k_nearest","page":"Connectivity","text":"k_nearest(grid, I...; k, …) -> (idx, dist)\n\nAllocating form of k_nearest!: returns the indices and their distances, nearest first.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.k_nearest"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.k_nearest!","page":"Connectivity","text":"k_nearest!(idx, dist, grid, I...; k, active_only=true, …) -> n\n\nWrite the k cells nearest to cell I into idx, and their distances into dist, nearest first. Returns how many were written, which is fewer than k only when the grid holds fewer candidates. The cell itself is excluded, as in neighbors_within!.\n\nExact under the geometry's own metric, on every architecture. It searches a ball, widens it until k cells have been seen, and keeps the k smallest in a bounded heap — so the answer never depends on the starting radius, and no candidate list is materialized. topology, scratch and reach behave as they do for neighbors_within!; an indexed topology makes each round a range query.\n\nTies at equal distance are broken by linear index, so the result is reproducible.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.k_nearest!"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.k_nearest-Union{Tuple{D}, Tuple{T}, Tuple{G}, Tuple{FlowGeometries.Grids.AbstractGrid{G, T}, NTuple{D, Real}}} where {G, T, D}","page":"Connectivity","text":"k_nearest(grid, p; k, …) -> (idx, dist)\n\nThe k cells nearest the point p, nearest first, exact under the geometry's own metric. The cell-seeded k_nearest is this at a cell centre, minus the cell itself.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.k_nearest"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.mapreduce_within-Union{Tuple{O}, Tuple{F}, Tuple{F, O, Any, FlowGeometries.Grids.AbstractGrid}} where {F, O}","page":"Connectivity","text":"mapreduce_within(f, op, init, grid; ball, …) -> value\n\nReduce f(I, J, d) with op over every cell I of grid and every cell J within ball of it, d being the distance. The bulk counterpart of fold_within.\n\nEverything that depends on the grid alone is built once and reused across all n cells — above all the spatial index, which brings the sweep to O(n log n) on a curvilinear or node grid. A hand-written loop gets the topology for free, that being O(1), and rebuilds the index per cell.\n\nThat hoisting covers one call. The default index is binned at this call's ball, so a second sweep builds a second one; several sweeps at one radius should share a topology built once:\n\ntop = MetricTopology(grid; index = Grids.cell_list(grid; ball = r))\nmapreduce_within(f, op, init, grid; ball = r, topology = top)\n\nop must be associative and init its identity; partials are combined in cell order, so the answer is deterministic and independent of scheduling.\n\nWhich form the reduction takes is _buffered_candidates, as for foreach_within: where the candidates need no per-task buffer this is Execution.reduce_indices, so a grid integral runs wherever that does — a device included.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.mapreduce_within"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.metric_band","page":"Connectivity","text":"metric_band(grid, dim, coord_t, coord_n, ball) -> T\n\nThe exact half-width along direction dim of the part of the row at coord_n that lies within ball of a point at coord_t, in that direction's own coordinate units. coord_t and coord_n are coordinates on the other direction of a two-direction grid.\n\nmetric_window returns a bounding box, which suits a query that then filters on distance. A separable sweep — a prefix sum along a row, a row-by-row convolution — has no filtering step and needs the exact extent. This is the same geodesic solve, resolved per row.\n\nReturns a negative number where the row is out of reach entirely, so band < 0 is the empty test. A row the ball covers completely gives the half-width of the whole direction (π in longitude).\n\nOn a sphere, for dim = 1, this inverts the spherical law of cosines:\n\nΔλ  arccosleft(fraccos(rR) - sin φ_t sin φ_ncos φ_t cos φ_nright)\n\nThe empty band, the full circle and a pole at either end all fall out of that one expression. At a pole the denominator vanishes and the separation stops depending on λ; this returns the full half-width there, so no caller handles it.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.metric_band"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.metric_window","page":"Connectivity","text":"metric_window(grid, I, ball) -> NTuple{N,Int}\n\nPer-direction index half-width guaranteed to contain every cell within ball of cell I.\n\nEach direction is bounded through its smallest gap — Grids.minimum_spacing, and the seam gap where the direction wraps — which is one number on a uniform axis and an O(N) scan on a stretched one. MetricTopology holds those gaps, so the four-argument form does no scanning at all; the three-argument form builds a topology per call. On a spherical or ellipsoidal grid the longitude cut additionally walks the latitude window for its smallest cosφ, so it costs the window it returns.\n\nThe window is a bound: neighbors_within! scans it and then filters on the geometry's own distance. It must cover the ball on the geometry it is asked about, so it is derived per geometry. On a spherical grid one longitude step spans R·cosφ·Δλ, so the λ half-width is taken at the latitude in the window nearest a pole; at a polar cell every longitude is in range, and the window says so.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.metric_window"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.metric_window-Tuple{FlowGeometries.Grids.AbstractStructuredGrid, Union{Real, FlowGeometries.Stencils.MetricBall}}","page":"Connectivity","text":"metric_window(grid, ball) -> NTuple{N,Int}\nmetric_window(grid, ball, topology) -> NTuple{N,Int}\n\nThe per-cell window maximised over every cell of grid, for sizing a cache or a footprint table.\n\nO(1): Grids.minimum_spacing gives the smallest gap per axis, and the extreme |cos φ| over a latitude axis is at one of its two ends, since |cos| on [-π/2, π/2] peaks in the middle. No cos per row, and no scan.\n\nThe result is the per-cell window at the worst cell, so it covers the ball at every cell.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.metric_window"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.neighbors!","page":"Connectivity","text":"neighbors!(out, grid, I...; stencil=Axial(1), active_only=true) -> n_written\n\nCell I's neighbors, as linear indices into the grid's mask, written into out. This is the form to use on a hot path: nneighbors counts them without writing, and Grids.neighbors returns a fresh vector. stencil is any Stencils.AbstractStencil.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.neighbors!"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.neighbors_within","page":"Connectivity","text":"neighbors_within(grid, I...; ball, active_only=true) -> Vector{Int}\n\nThe allocating form of neighbors_within!: counts, sizes, and fills in one call.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.neighbors_within"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.neighbors_within!","page":"Connectivity","text":"neighbors_within!(out, grid, I...; ball, active_only=true, topology, scratch, reach) -> n_written\n\nWrite the linear indices of every cell whose centre lies within ball of cell I. ball is a Stencils.MetricBall or a bare radius in the geometry's length units.\n\nDistance is the geometry's own — great-circle on a sphere, Vincenty on a spheroid, the chord where a third direction is present — so the neighbourhood is a metric ball in that distance. The cell itself is excluded, matching stencil semantics where the zero offset is no neighbour. A periodic direction wraps, each cell appears at most once, and its coordinate is taken by minimum image, so the seam neither shortens nor lengthens a distance.\n\nCost is metric_window — O(1) per direction on any separable axis, uniform or stretched, given the per-direction minimum steps that topology carries — times one distance evaluation per candidate. Size the buffer with nneighbors_within: on a non-uniform or curved grid the number of cells within a fixed distance varies from cell to cell.\n\nThree arguments matter for anything beyond a single query:\n\ntopology — a MetricTopology, the grid invariants a ball query reads. The default is O(1) and allocation-free. On a curvilinear or node grid, pass indexed to bring each query to O(log n + m), or use foreach_within, which builds the index once for a whole sweep.\nscratch — a candidate buffer from ball_scratch, one per task. Accepted on every grid type and used where a query goes through a spatial index, i.e. on a curvilinear or node grid; a separable window has no candidate list to buffer. With one, an indexed query allocates nothing; without one it allocates its candidate list per call.\nreach — Unrestricted (the ball, and the default) or Connected (the part of it reachable from I without leaving it). A ball is not a connected patch: with a mask or a concave domain it can contain cells reachable from the seed only by going outside ball.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.neighbors_within!"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.neighbors_within-Union{Tuple{D}, Tuple{FlowGeometries.Grids.AbstractGrid, NTuple{D, Real}}} where D","page":"Connectivity","text":"neighbors_within(grid, p; ball, …) -> Vector{Int}\nnneighbors_within(grid, p; ball, …) -> Int\n\nCells within ball of the point p, and how many. p is a coordinate, where the cell-seeded methods take index arguments; it may be a Tuple, NamedTuple, AbstractVector or SVector, as everywhere else a point is accepted.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.neighbors_within"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.nneighbors","page":"Connectivity","text":"nneighbors(grid, I...; stencil=Axial(1), active_only=true) -> Int\n\nHow many neighbors neighbors! writes for cell I, counted without writing them.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.nneighbors"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.nneighbors_within","page":"Connectivity","text":"nneighbors_within(grid, I...; ball, active_only=true) -> Int\n\nThe count neighbors_within! writes — how large its buffer must be.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.nneighbors_within"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.sort_neighbors!-Tuple{FlowGeometries.Connectivity.CSRConnectivity}","page":"Connectivity","text":"sort_neighbors!(conn) -> conn\n\nSort each node's neighbor block ascending, in place. Rows are stencil-short, so an insertion sort per row is O(nedges) overall and needs no scratch.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.sort_neighbors!"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.sparse_adjacency_coo!-Tuple{AbstractVector{<:Integer}, AbstractVector{<:Integer}, FlowGeometries.Connectivity.CSRConnectivity}","page":"Connectivity","text":"sparse_adjacency_coo!(I, J, conn) -> ne\nsparse_adjacency_coo!(I, J, V, conn) -> ne\n\nFill preallocated COO buffers (length ≥ nedges(conn)). V, if given, is set to true.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.sparse_adjacency_coo!"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.sparse_adjacency_csc!-Tuple{AbstractVector{<:Integer}, AbstractVector{<:Integer}, FlowGeometries.Connectivity.CSRConnectivity}","page":"Connectivity","text":"sparse_adjacency_csc!(colptr, rowval, conn) -> nedges\n\nFill caller-owned CSC structure arrays (length(colptr) ≥ nnodes+1, length(rowval) ≥ nedges) for the adjacency of conn, so that entry (i, j) is set iff j is a neighbor of i.\n\nThis is the direct route to a sparse matrix: CSR and CSC are the same layout transposed, so the structure is obtained by one counting pass and one placement pass over the existing neighbor list — no coordinate triples are materialized, nothing is sorted, and no permutation vector is built. Row indices come out ascending within each column for free, because the placement pass walks nodes in order. The running cursors live in colptr itself and are shifted back at the end, so this needs no scratch beyond the two output arrays.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.sparse_adjacency_csc!"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.sparse_adjacency_matrix","page":"Connectivity","text":"sparse_adjacency_matrix(grid_or_conn; kwargs...) -> SparseMatrixCSC\n\nRequires using SparseArrays (extension). Prefer sparse_adjacency_coo! when reusing COO buffers.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.sparse_adjacency_matrix"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.sparse_adjacency_matrix!","page":"Connectivity","text":"sparse_adjacency_matrix!(colptr, rowval, nzval, conn) -> SparseMatrixCSC\n\nAssemble the adjacency matrix into caller-owned buffers and wrap them without copying, so a repeated build reuses one set of storage. Requires using SparseArrays.\n\nThe returned matrix aliases the three buffers, so reusing them for a later call invalidates any matrix built from them earlier. Buffers longer than needed are trimmed to fit (nnodes+1 and nedges), a matrix owning arrays of exactly the right length.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.sparse_adjacency_matrix!"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.structured_grid-Tuple{FlowGeometries.SphericalSampling.AbstractTensorProductSphericalSampling, Integer}","page":"Connectivity","text":"structured_grid([T], sampling, nlat; geometry, nlon, mask, periodic) -> StructuredGrid\n\nBuild a spherical StructuredGrid from a tensor-product sampling (Clenshaw–Curtis, Gauss–Legendre, Driscoll–Healy, McEwen–Wiaux, lat–lon, …). Longitude periodicity is auto-detected unless periodic is set.\n\nT is the element type to build in, and defaults to the geometry's own, a geometry fixing the width of every coordinate and metric factor computed against it. Naming T converts the geometry to that width, so the grid is T throughout.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.structured_grid"},{"category":"function","location":"api/connectivity/#FlowGeometries.Connectivity.unstructured_grid","page":"Connectivity","text":"unstructured_grid([T], sampling, args...; geometry, areas, mask) -> UnstructuredGrid\n\nPoints from spherical_points, exact sampling topology from build_connectivity, and exact cell areas from the sampling's own tessellation. Pass areas to supply them instead.\n\nT is the element type to build in, and defaults to the geometry's own, as for structured_grid.\n\nThe spherical pixelizations have their own layouts — Grids.HEALPixGrid, Grids.CubedSphereGrid, Grids.YinYangGrid, Grids.RingGrid — with coordinates, adjacency and measure arithmetic in their resolution parameters, and Grids.materialize turns one into a dense point cloud. This entry point covers the arbitrary mesh: the icosahedral geodesic, and a caller's own points.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.unstructured_grid"},{"category":"method","location":"api/connectivity/#FlowGeometries.Connectivity.unstructured_grid-Tuple{FlowGeometries.SphericalSampling.AbstractScatteredSphericalSampling, AbstractVector, AbstractVector}","page":"Connectivity","text":"unstructured_grid(::AbstractScatteredSphericalSampling, λ, φ; geometry, k, radius, areas, mask)\n\nBuild an UnstructuredGrid on an arbitrary (λ, φ) point set. The points are the caller's, so there is no resolution parameter. Adjacency comes from a k-d-tree query (k nearest, or everything within a physical radius) and cell areas default to the spherical Voronoi dual; see Grids.UnstructuredGrid for the extension each needs.\n\n\n\n\n\n","title":"FlowGeometries.Connectivity.unstructured_grid"},{"category":"method","location":"api/connectivity/#FlowGeometries.Grids.formula_neighbors-Tuple{FlowGeometries.Grids.CubedSphereGrid, Integer}","page":"Connectivity","text":"Grids.formula_neighbors(grid::CubedSphereGrid, k)\n\nA cubed-sphere cell's four edge neighbours: the panel-interior offsets where they stay on the panel, and _cubed_neighbor's exact seam fold where they cross to another. The fold is derived by matching cube coordinates along the shared edge, so a seam neighbour is symmetric.\n\n\n\n\n\n","title":"FlowGeometries.Grids.formula_neighbors"},{"category":"method","location":"api/connectivity/#FlowGeometries.Grids.formula_neighbors-Tuple{FlowGeometries.Grids.RingGrid, Integer}","page":"Connectivity","text":"Grids.formula_neighbors(grid::RingGrid, i)\n\nA ring grid's adjacency: the two points either side along the ring, wrapping in longitude, and on each adjacent ring the two points whose longitudes straddle this one's.\n\nThe straddling pair comes from proportional position, ⌊(j−1)·nlon[r′]/nlon[r]⌋ and its successor. Where adjacent rings differ in width this relation is directed — a wide ring's point can straddle a narrow ring's point that does not straddle it back — so the graph is not symmetric in general. It is symmetric whenever the two rings have equal counts, which is most of a Gaussian grid's interior.\n\nDuplicates are dropped, so a ring holding one or two points reports fewer than the six a wide ring does.\n\n\n\n\n\n","title":"FlowGeometries.Grids.formula_neighbors"},{"category":"method","location":"api/connectivity/#FlowGeometries.Grids.formula_neighbors-Tuple{FlowGeometries.Grids.YinYangGrid, Integer}","page":"Connectivity","text":"Grids.formula_neighbors(grid::YinYangGrid, k)\n\nA Yin–Yang cell's four edge neighbours within its own panel. Neither panel wraps and the two are not cross-linked, so a cell on a panel edge reports fewer; the panels couple through interpolation, the standard Yin–Yang discrete topology.\n\n\n\n\n\n","title":"FlowGeometries.Grids.formula_neighbors"},{"category":"section","location":"api/discretization/#Discretization","page":"Discretization","text":"","title":"Discretization"},{"category":"type","location":"api/discretization/#FlowGeometries.Discretization.AbstractLocation","page":"Discretization","text":"AbstractLocation\n\nWhere a value sits within a cell: Center or Face.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.AbstractLocation"},{"category":"type","location":"api/discretization/#FlowGeometries.Discretization.AbstractStencilPlan","page":"Discretization","text":"AbstractStencilPlan{T}\n\nA finite-difference weight set for one axis. UniformStencilPlan or TabulatedStencilPlan, chosen by the axis's Axes.spacing_trait.\n\nBuild one with stencil_plan and apply it with Operators.apply_stencil!.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.AbstractStencilPlan"},{"category":"type","location":"api/discretization/#FlowGeometries.Discretization.Center","page":"Discretization","text":"Center()\n\nCell centres — N of them along a direction of N cells.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.Center"},{"category":"type","location":"api/discretization/#FlowGeometries.Discretization.Face","page":"Discretization","text":"Face()\n\nCell boundaries — N+1 of them along a direction of N cells.\n\nCentres do not determine faces: N centres leave the N+1 faces underdetermined by one. The convention here is that faces sit midway between neighbouring centres, with the two outermost placed by linear extrapolation, which is the same rule the curvilinear corner reconstruction uses.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.Face"},{"category":"type","location":"api/discretization/#FlowGeometries.Discretization.StencilScratch","page":"Discretization","text":"StencilScratch{T}\n\nThe working buffers a degrading apply_stencil! needs to rebuild a window at a mask edge: the Fornberg table and the node list. Build one with stencil_scratch.\n\nOne per task, exactly as Connectivity.ball_scratch is — the buffers are written per cell, so chunks cannot share them. A threaded backend therefore allocates its own set per chunk and ignores one passed here.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.StencilScratch"},{"category":"type","location":"api/discretization/#FlowGeometries.Discretization.TabulatedStencilPlan","page":"Discretization","text":"TabulatedStencilPlan{T,K,MI,MW}\n\nThe weight set of a stretched axis: one row of node indices and weights per sample, as the n × K pair of matrices axis_stencils returns.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.TabulatedStencilPlan"},{"category":"type","location":"api/discretization/#FlowGeometries.Discretization.UniformStencilPlan","page":"Discretization","text":"UniformStencilPlan{T,K,MI,MW}\n\nThe weight set of a uniformly spaced axis: one centred row of K weights, plus the rows at the two ends where a bounded window shifts inward.\n\nEvery interior sample reads field[j - left + q - 1] weighted by weights[q], the same K numbers throughout, so they reach the sweep as a tuple in registers with no per-cell gather. Storage is O(K²), independent of the axis length. A wrapping axis has no shifted rows at all, every sample being centred.\n\nleft and right count the nodes before and after the sample and are equal only for odd K: a 4-node window reads one sample back and two forward, so two rows shift at the top end and one at the bottom.\n\nThe weights are built from the spacing. Mathematically they are the row axis_stencils holds, and the table carries per-row round-off from reconstructing each window's coordinates, at ~1e-14 relative on a 5-node first derivative; one exact row applied everywhere carries none.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.UniformStencilPlan"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization._uniform_row-Union{Tuple{K}, Tuple{T}, Tuple{Type{T}, T, Int64, Val{K}, Int64}} where {T, K}","page":"Discretization","text":"_uniform_row(T, h, s, Val(K), order) -> NTuple{K,T}\n\nThe weights of a row whose first node sits s steps of size h from the sample it is evaluated at.\n\ns = -left is the centred row. Only the offsets enter, so this is the exact weight set for every sample sharing that offset — on a uniform axis, all of them away from a shifted end.\n\n\n\n\n\n","title":"FlowGeometries.Discretization._uniform_row"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization._window_start-NTuple{4, Int64}","page":"Discretization","text":"_window_start(i, k, lo, hi) -> Int\n\nFirst index of a k-node window centred on i and shifted to fit inside [lo, hi]. The whole-axis case is lo = 1, hi = n; the masked case passes the bounds of the active run.\n\n\n\n\n\n","title":"FlowGeometries.Discretization._window_start"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.axis_length-Tuple{FlowGeometries.Discretization.UniformStencilPlan}","page":"Discretization","text":"axis_length(plan) -> Int\n\nThe number of samples on the axis plan describes. A field differenced with it has to have that many along the differenced direction.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.axis_length"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.axis_stencils!-Union{Tuple{T}, Tuple{AbstractMatrix{<:Integer}, AbstractMatrix{T}, AbstractVector{T}, Integer, Integer}} where T<:AbstractFloat","page":"Discretization","text":"axis_stencils!(indices, weights, x, order, nodes; period=nothing, scratch=nothing) -> (indices, weights)\n\naxis_stencils into two caller-owned n × nodes matrices.\n\nThe table is O(n·k), so a caller rebuilding it — at each step of a stretching mesh, say — owns the matrices and passes them in. Pass a stencil_scratch as well and the call allocates nothing: the remaining buffers are the Fornberg table and the node list, O(k) each, allocated per call without one.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.axis_stencils!"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.axis_stencils-Union{Tuple{T}, Tuple{AbstractVector{T}, Integer, Integer}} where T<:AbstractFloat","page":"Discretization","text":"axis_stencils(x, order, nodes; period=nothing) -> (indices, weights)\n\nThe order-th derivative's fd_weights at every sample of axis x, as two n × nodes matrices: the axis indices each sample reads, and the weight on each.\n\nOne row per sample, so a stretched axis costs nothing extra downstream — the varying weights are already here. Built once and reused by apply_stencil!.\n\nperiod === nothing shifts the stencil inward at the two ends, exactly as the single-sample fd_weights does. Given a period the stencil stays centred everywhere and wraps, with the wrapped samples' coordinates carried across the seam so the spacing there is the true one.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.axis_stencils"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.cell_width-Union{Tuple{T}, Tuple{AbstractVector{T}, Integer}, Tuple{AbstractVector{T}, Integer, Union{Nothing, Real}}} where T<:AbstractFloat","page":"Discretization","text":"cell_width(x, i, period=nothing) -> width\n\nThe coordinate width of cell i of an axis of cell centres x: the centred width (|h_m| + |h_p|)/2 at an interior cell — and, given a period, at the wrapped boundary too — the one-sided gap to the single neighbour at a genuinely non-periodic boundary, and 1 for a length-1 axis. On a uniform axis every width is the constant step.\n\nEquivalently abs(faces(x)[i+1] - faces(x)[i]): faces places a boundary midway between neighbouring centres, so the width between them is the average of the two adjacent gaps. Call this form per cell; faces materializes the whole axis.\n\nA width is a physical measure, so it is non-negative however the axis is stored — increasing or decreasing, as a dataset holding latitude, depth or pressure levels top-down is. This is where a spacing becomes a length, area or volume contribution, and where the abs is applied; local_spacing keeps the sign.\n\nA length-1 axis contributes the multiplicative identity to a measure that is a product of per-axis widths (Cartesian Δx·Δy), so a degenerate direction reduces an area to a length. The spherical R²cosφ·Δλ·Δφ measure is not a plain product and handles its own singleton case, in the Grids.StructuredGrid constructor.\n\nGrids.cell_width is this on a grid direction, and Grids.cell_widths the whole axis at once.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.cell_width"},{"category":"function","location":"api/discretization/#FlowGeometries.Discretization.cell_widths","page":"Discretization","text":"cell_widths(x, period=nothing) -> AbstractVector\n\ncell_width at every index of an axis at once: the coordinate widths a separable measure or a flux divergence is weighted by.\n\nA uniform axis gets an Axes.ConstantVector, one number and a length, every one of its cells having the same width. Anything else is built densely into the same kind of storage as x, by broadcasts over views, so a device-resident axis is widened on its device.\n\nGrids.cell_widths is this on a grid direction, taking the period from the grid itself.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.cell_widths"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.centers!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}}} where T<:AbstractFloat","page":"Discretization","text":"centers!(out, f) -> out\n\ncenters into a caller-owned vector of length N, for an axis of N+1 faces.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.centers!"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.centers-Union{Tuple{AbstractVector{T}}, Tuple{T}} where T<:AbstractFloat","page":"Discretization","text":"centers(f) -> AbstractVector\n\nThe N cell centres of an axis of N+1 cell boundaries: the midpoint of each pair.\n\nIt inverts faces exactly on a uniform axis. On a stretched one it does not: faces places a boundary midway between two centres, and re-midpointing those boundaries averages neighbouring cell widths. The two are inverse only where the widths are constant.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.centers"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.derivative_order-Tuple{FlowGeometries.Discretization.AbstractStencilPlan}","page":"Discretization","text":"derivative_order(plan) -> Int\n\nWhich derivative plan's weights compute.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.derivative_order"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.faces!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}}} where T<:AbstractFloat","page":"Discretization","text":"faces!(out, x) -> out\n\nfaces into a caller-owned vector of length N+1.\n\nThe form to use when staggering repeatedly — a moving mesh, a column solver stepping in time — where the N+1 faces are otherwise allocated per call.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.faces!"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.faces-Union{Tuple{AbstractVector{T}}, Tuple{T}} where T<:AbstractFloat","page":"Discretization","text":"faces(x) -> AbstractVector\n\nThe N+1 cell boundaries of an axis of N cell centres: midpoints of neighbouring centres, with the outermost two extrapolated a half-cell beyond the end centres.\n\nA uniform axis stays uniform — its faces are another Axes.UniformAxis, offset by half a cell — so nothing about the axis's spacing guarantee is lost.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.faces"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.fd_weights!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractMatrix{T}, AbstractVector{T}, Real, Integer}} where T<:AbstractFloat","page":"Discretization","text":"fd_weights!(w, c, nodes, x₀, order) -> w\n\nfd_weights into caller buffers: w holds the length(nodes) weights and c is the length(nodes) × (order+1) recursion table. Both are overwritten.\n\nThe allocating form allocates two arrays per call, and a stencil is built once per sample of an axis. The degrade path in apply_stencil! rebuilds one per cell near a mask edge, and holds these buffers across all of them.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.fd_weights!"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.fd_weights-Union{Tuple{T}, Tuple{AbstractVector{T}, Integer, Integer, Integer}} where T<:AbstractFloat","page":"Discretization","text":"fd_weights(x, i, order, nodes) -> (indices, weights)\n\nWeights for the order-th derivative at sample i of axis x, using nodes of its samples.\n\nThe stencil is centred on i where the axis allows and shifted inward at a boundary, holding the accuracy order at the two ends as well as the interior. Built on the arbitrary-node form above, so a stretched axis costs nothing extra.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.fd_weights"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.fd_weights-Union{Tuple{T}, Tuple{AbstractVector{T}, Real, Integer}} where T<:AbstractFloat","page":"Discretization","text":"fd_weights(nodes, x₀, order) -> Vector\n\nFinite-difference weights approximating the order-th derivative at x₀ from the values at nodes, by the recursion of Fornberg (1988), Math. Comp. 51, 699–706.\n\nOne recursion covers every case: any derivative order, any node count (hence any order of accuracy), any evaluation point — inside the node set or outside it — and arbitrarily spaced nodes. With m nodes the result is exact for polynomials of degree m-1, so accuracy order m - order.\n\nsum(w .* f.(nodes)) is then the derivative estimate. This returns the weights only; applying them to a field is the caller's.\n\nfd_weights([0.0, 1.0, 2.0], 1.0, 1)   # ≈ [-0.5, 0.0, 0.5], the centred first difference\n\n\n\n\n\n","title":"FlowGeometries.Discretization.fd_weights"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.interpolation_weights-Union{Tuple{T}, Tuple{AbstractVector{T}, Real}} where T<:AbstractFloat","page":"Discretization","text":"interpolation_weights(x, v) -> (i, w)\n\nLinear interpolation on axis x at coordinate v, as the left sample index i and the weight pair w = (w_i, w_{i+1}) with w_i + w_{i+1} == 1.\n\nWeights only: applying them to a field is the caller's loop. Outside the axis the nearest end is used with weight 1, so a coordinate beyond the ends clamps.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.interpolation_weights"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.jacobian-Tuple{FlowGeometries.Geometry.AbstractGeometry, Any}","page":"Discretization","text":"jacobian(geometry, point) -> Real\n\n∏ hᵈ from scale_factors: the volume element per unit coordinate volume at point.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.jacobian"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.lagrange_weights!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}, Real, Integer}} where T<:AbstractFloat","page":"Discretization","text":"lagrange_weights!(w, x, v, nodes) -> (indices, w)\n\nlagrange_weights into a caller-owned vector of length nodes.\n\nThe indices come back as a range, a stack value, so with w supplied the call allocates nothing. This is the form to use when interpolating many points along one axis.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.lagrange_weights!"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.lagrange_weights-Union{Tuple{T}, Tuple{AbstractVector{T}, Real, Integer}} where T<:AbstractFloat","page":"Discretization","text":"lagrange_weights(x, v, nodes) -> (indices, weights)\n\nLagrange interpolation weights of length(nodes) points on axis x at coordinate v, exact for polynomials up to degree nodes-1 and valid on an arbitrarily spaced axis.\n\nThe stencil is centred on v as far as the axis allows and shifted inward at the ends, so the node count holds at a boundary as well as in the interior.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.lagrange_weights"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.local_spacing-Union{Tuple{T}, Tuple{AbstractVector{T}, Integer}, Tuple{AbstractVector{T}, Integer, Union{Nothing, Real}}} where T<:AbstractFloat","page":"Discretization","text":"local_spacing(x, i, period=nothing) -> (h_m, h_p)\n\nThe one-sided coordinate gaps around index i of an axis x: h_m = x[i]-x[i-1] and h_p = x[i+1]-x[i].\n\nThis is the primitive a finite-difference operator assembled at the call site is built from — the h_m, h_p of Geometry.nonuniform_first_derivative, and what a staggered difference, a divergence or a curl needs from the grid. Always a scalar subtraction of two already-stored elements, never a heap allocation, so it is safe to call per grid point in a hot loop. On an axis whose spacing is known from its type there is not even a subtraction.\n\nThe gaps are signed, so a descending axis reports negative gaps and a derivative stencil keeps the index-versus-coordinate direction: composed with nonuniform_first_derivative this gives the same derivative with respect to the coordinate whichever way the axis is stored. cell_width drops the sign, a width being a length.\n\nperiod, if given (e.g. 2π for a periodic longitude axis), makes the boundary gaps wrap: at i == 1, h_m is the gap to the unwrapped previous point x[n]-period; at i == n, h_p is the gap to the unwrapped next point x[1]+period. Pass nothing (the default) for a non-periodic axis, where a boundary gap is zero and the caller falls back to a one-sided stencil.\n\nGrids.local_spacing is this on a grid direction, taking the period from the grid itself.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.local_spacing"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.locate-Union{Tuple{T}, Tuple{AbstractVector{T}, Real}} where T<:AbstractFloat","page":"Discretization","text":"locate(x, v) -> Int\n\nThe index of the cell of axis x that contains coordinate v, or 0 when v lies outside.\n\nCells are the intervals between the axis's faces, so cell i holds f[i] ≤ v < f[i+1] (the last cell includes its far face). O(1) on a uniform axis, from the closed form; O(log n) on a stretched one, by bisection. Both storage orders work.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.locate"},{"category":"function","location":"api/discretization/#FlowGeometries.Discretization.metric_floor","page":"Discretization","text":"metric_floor(geometry) -> T\n\nThe magnitude below which a scale factor is treated as degenerate: L·√eps(T) for a curved geometry of size L, and 0 for a Cartesian one, whose metric never degenerates.\n\nIt scales with both the geometry's size and the element type. At Float32 on Earth's radius, cos(Float32(π/2)) ≈ -4.4e-8 puts h_λ at the pole around 0.28 m, above any fixed small constant.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.metric_floor"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.nearest_index-Union{Tuple{T}, Tuple{AbstractVector{T}, Real}} where T<:AbstractFloat","page":"Discretization","text":"nearest_index(x, v) -> Int\n\nThe index of the axis sample closest to v, clamped into range. Unlike locate this always returns a valid index, since a nearest sample exists for any v.\n\nExact ties go to the lower index, and the uniform closed form and the general bisection agree on that, so the two paths never disagree. O(1) on a uniform axis, O(log n) on a stretched one.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.nearest_index"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.nnodes-Union{Tuple{FlowGeometries.Discretization.UniformStencilPlan{T, K, MI, MW} where {MI<:(AbstractMatrix{<:Integer}), MW<:AbstractMatrix{T}}}, Tuple{K}, Tuple{T}} where {T, K}","page":"Discretization","text":"nnodes(plan) -> Int\n\nHow many samples each row of plan reads.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.nnodes"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.nodes-Tuple{AbstractVector, FlowGeometries.Discretization.Center}","page":"Discretization","text":"nodes(x, loc) -> AbstractVector\n\nAn axis's sample positions at location loc: x itself at Center, and its faces at Face.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.nodes"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.plan_row-Union{Tuple{K}, Tuple{T}, Tuple{FlowGeometries.Discretization.UniformStencilPlan{T, K, MI, MW} where {MI<:(AbstractMatrix{<:Integer}), MW<:AbstractMatrix{T}}, Int64}} where {T, K}","page":"Discretization","text":"plan_row(plan, j) -> (nodes, weights)\n\nRow j of plan, as two K-tuples: the axis indices that sample reads and the weight on each.\n\nThe generic accessor, for a path that wants a row whatever form the plan took. The uniform sweep does not go through it — the whole point there is that the interior row is not fetched per cell.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.plan_row"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.scale_factors-Tuple{FlowGeometries.Geometry.AbstractGeometry, Any}","page":"Discretization","text":"scale_factors(geometry, point) -> NTuple\nscale_factors(geometry, point, Val(N)) -> NTuple{N}\n\nThe metric scale factors hᵈ at point: the physical length of a unit step in each coordinate direction. Cartesian gives 1 in every direction; spherical gives (R cosφ, R) on the surface and (r cosφ, r, 1) with a radius direction.\n\nThey turn a coordinate derivative into a physical one, ∂/∂sᵈ = (1/hᵈ)·∂/∂ξᵈ, so a divergence or a curl is assembled from these plus fd_weights at the call site, where the staggering and the boundary condition are chosen.\n\nVal(N) names how many components point has, giving a concrete NTuple{N} from a point whose length is a runtime value.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.scale_factors"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.stencil_plan-Union{Tuple{T}, Tuple{AbstractVector{T}, Integer, Integer}} where T<:AbstractFloat","page":"Discretization","text":"stencil_plan(x, order, nodes; period = nothing) -> AbstractStencilPlan\n\nThe order-th derivative's weights along axis x, held for reuse.\n\nA uniform axis gives a UniformStencilPlan and a stretched one a TabulatedStencilPlan; which it is comes from the axis's type through Axes.spacing_trait, never from inspecting its values.\n\nA wrapping uniform axis takes the uniform form only when the period given is the one the axis's own spacing implies, n·h. This checks the argument against the axis type, and reads no coordinate: any other period puts a seam gap other than h at the wrap, which the tabulated form describes.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.stencil_plan"},{"category":"method","location":"api/discretization/#FlowGeometries.Discretization.stencil_scratch-Tuple{Integer, Integer}","page":"Discretization","text":"stencil_scratch([T = Float64], order, nodes) -> StencilScratch\n\nBuffers for the degrade path, so a caller taking many derivatives on a masked grid does not allocate them per call. Without one a degrading call allocates a few hundred bytes each time — O(1) in the grid, but per call, so a flux computation taking nine derivatives pays it nine times.\n\nOnly the degrading policies need it. An unmasked grid, and any grid under BlankMasked, never rebuilds a window and allocates nothing regardless.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.stencil_scratch"},{"category":"section","location":"api/stencils/#Stencils","page":"Stencils","text":"","title":"Stencils"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.AbstractStencil","page":"Stencils","text":"AbstractStencil\n\nA neighbourhood shape in index space. Materialize it with offsets.\n\nConcrete shapes: Axial, VonNeumann, Moore (alias Vertex), Diagonal, Anisotropic, Custom.\n\nA shape supplies one method, offsets(s, ::Val{N}), returning its offsets as a tuple; foreach_offset, fold_offsets, nstencil and reach all follow from it, and their loops still unroll at compile time. Put whatever the offsets depend on in the type, so the tuple is inferable:\n\nstruct Upwind{R} <: AbstractStencil end\nStencils.offsets(::Upwind{R}, ::Val{N}) where {R,N} =\n    ntuple(i -> ntuple(d -> d == cld(i, R) ? mod1(i, R) : 0, Val(N)), Val(N * R))\n\n\n\n\n\n","title":"FlowGeometries.Stencils.AbstractStencil"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.Anisotropic","page":"Stencils","text":"Anisotropic(radii)\n\nA box with its own radius per direction: |δ_d| ≤ radii[d], origin excluded. A direction with radius 0 contributes no offset of its own, which is how a stencil is confined to a subset of the directions.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.Anisotropic"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.Axial","page":"Stencils","text":"Axial(r = 1)\n\nAxis-aligned neighbours out to r cells: ±k·ê_d for every direction d and k = 1…r. 2·N·r offsets. Axial(1) is the classic 4-in-2-D / 6-in-3-D face stencil; beyond radius 1 the offsets run along the axis lines, past the touching faces.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.Axial"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.CellRadius","page":"Stencils","text":"CellRadius(r)\n\nA stencil extent of r cells, as distinct from a physical MetricBall. Stencil constructors accept a bare Integer too; this names the distinction at a call site where it is worth naming.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.CellRadius"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.Custom","page":"Stencils","text":"Custom(offsets)\n\nAn explicit offset set, e.g. Custom(((1, 0), (0, 1))) for a forward-only two-point stencil. The origin is rejected: a cell is not its own neighbour.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.Custom"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.Diagonal","page":"Stencils","text":"Diagonal(r = 1)\n\nPure diagonals only: every offset whose components are all ±k for a single k = 1…r. 2^N·r offsets, and no axis-aligned neighbour among them.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.Diagonal"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.MetricBall","page":"Stencils","text":"MetricBall(r)\n\nEvery cell within physical distance r, measured through the geometry. Distinct from a CellRadius: on a stretched or spherical grid the number of cells within r varies from cell to cell, so this reduces to no fixed offset set.\n\nQuery it with Connectivity.neighbors_within! / nneighbors_within / neighbors_within, which take it as their ball keyword.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.MetricBall"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.Moore","page":"Stencils","text":"Moore(r = 1)\nVertex(r = 1)\n\nEvery cell in the surrounding box: 0 < |δ|_∞ ≤ r, i.e. (2r+1)^N - 1 offsets. Moore(1) is the 8-in-2-D / 26-in-3-D vertex stencil.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.Moore"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.Vertex","page":"Stencils","text":"Alias for Moore, under the name the face/vertex pairing uses.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.Vertex"},{"category":"type","location":"api/stencils/#FlowGeometries.Stencils.VonNeumann","page":"Stencils","text":"VonNeumann(r = 1)\n\nEvery cell within r steps measured in the L^1 (taxicab) metric: 0 < |δ|₁ ≤ r. Equal to Axial at r = 1 and strictly larger beyond it, since it admits diagonal combinations whose step count still fits.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.VonNeumann"},{"category":"method","location":"api/stencils/#FlowGeometries.Stencils.fold_offsets-Union{Tuple{N}, Tuple{Any, Any, FlowGeometries.Stencils.AbstractStencil, Val{N}}} where N","page":"Stencils","text":"fold_offsets(f, init, stencil, Val(N))\n\nFold f(acc, δ) over the stencil's offsets, unrolled at compile time.\n\nThe accumulator is threaded through as a value, so nothing is captured and nothing is boxed. This is the form a counting or filling kernel takes.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.fold_offsets"},{"category":"method","location":"api/stencils/#FlowGeometries.Stencils.foreach_offset-Union{Tuple{N}, Tuple{Any, FlowGeometries.Stencils.AbstractStencil, Val{N}}} where N","page":"Stencils","text":"foreach_offset(f, stencil, Val(N))\n\nApply f to each of the stencil's offsets, with the loop unrolled at compile time.\n\nEach offset reaches f as a register-sized NTuple{N,Int} and the offset set is never materialized on the heap — unlike offsets, whose returned tuple is allocated once it outgrows a register. Every bulk neighbour kernel goes through this.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.foreach_offset"},{"category":"function","location":"api/stencils/#FlowGeometries.Stencils.is_symmetric","page":"Stencils","text":"is_symmetric(stencil, Val(N)) -> Bool\n\nWhether the offset set is closed under negation: δ is an offset exactly when −δ is.\n\nA graph built from a symmetric stencil is a symmetric graph, and stays one under masking and under clipping at a boundary — an edge and its reverse are dropped together, both being the same pair of cells. A symmetric adjacency is then read as its own transpose.\n\nAxial, VonNeumann, Moore, Diagonal and Anisotropic are symmetric by construction: each is defined by a condition on |δ| and so contains −δ with δ. Custom is whatever it was given, and is checked once at compile time. A caller's own shape answers false until it says otherwise, at the cost of a transpose — see AbstractStencil.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.is_symmetric"},{"category":"method","location":"api/stencils/#FlowGeometries.Stencils.nstencil-Union{Tuple{N}, Tuple{FlowGeometries.Stencils.AbstractStencil, Val{N}}} where N","page":"Stencils","text":"nstencil(stencil, Val(N)) -> Int\n\nHow many offsets the stencil has in N dimensions — the buffer length a neighbors! call needs.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.nstencil"},{"category":"function","location":"api/stencils/#FlowGeometries.Stencils.offsets","page":"Stencils","text":"offsets(stencil, Val(N)) -> NTuple{K,NTuple{N,Int}}\n\nThe stencil's offsets in N dimensions, as a tuple built at compile time so a loop over it unrolls.\n\nOffsets come out in column-major order of the enclosing box (direction 1 varying fastest), which puts Axial(1) and Moore(1) in the conventional order.\n\nThis is the one method a new shape defines; see AbstractStencil.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.offsets"},{"category":"method","location":"api/stencils/#FlowGeometries.Stencils.reach-Union{Tuple{N}, Tuple{FlowGeometries.Stencils.AbstractStencil, Val{N}}} where N","page":"Stencils","text":"reach(stencil, Val(N)) -> NTuple{N,Int}\n\nThe stencil's extent in cells along each direction: maximum(|δ_d|) over its offsets. This is the halo width a traversal must leave, and the window a distance query has to scan.\n\n\n\n\n\n","title":"FlowGeometries.Stencils.reach"},{"category":"section","location":"connectivity/#connectivity-page","page":"Stencils & Connectivity","text":"Connectivity answers: which cells are neighbours?","title":"Stencils & Connectivity"},{"category":"section","location":"connectivity/#Stencil-shapes","page":"Stencils & Connectivity","text":"A stencil is a neighbourhood shape in index space. It carries no dimensionality — the same shape applies to a 2-D and a 5-D grid — and its shape and radius live in its type, so the offset set is built at compile time and a loop over it unrolls.\n\nusing FlowGeometries: FlowGeometries as FG\nS = FG.Stencils\n\nS.offsets(S.Axial(1), Val(2))\n\nS.offsets(S.Moore(1), Val(2))\n\nshape offsets meaning\nAxial(r) 2·N·r axis-aligned only, ±k·êᵈ for k = 1…r\nVonNeumann(r) — the L¹ ball, 0 < ‖δ‖₁ ≤ r\nMoore(r) (alias Vertex) (2r+1)^N − 1 the L^∞ ball, the surrounding box\nDiagonal(r) 2^N·r pure diagonals only\nAnisotropic(radii) — a box with its own radius per direction\nCustom(offsets) as given an explicit offset set\n\nAxial(1) and VonNeumann(1) are the same four-in-2-D set; beyond radius 1 they differ, because VonNeumann admits diagonal combinations whose step count still fits.\n\nS.nstencil(S.Axial(2), Val(2)), S.nstencil(S.VonNeumann(2), Val(2)), S.nstencil(S.Moore(2), Val(2))\n\nAny dimension:\n\nS.nstencil(S.Axial(1), Val(5)), S.nstencil(S.Moore(1), Val(5))\n\nS.reach(S.Anisotropic((3, 1)), Val(2))     # halo width per direction\n\nnote: A stencil is named by its type, never by a symbol\nThe type resolves at compile time, so the neighbour iterator built from it is concretely typed and a traversal allocates nothing. stencil = S.Moore(2) is free; there is no symbol form to reach for.","title":"Stencil shapes"},{"category":"section","location":"connectivity/#Your-own-shape","page":"Stencils & Connectivity","text":"A shape supplies Stencils.offsets and nothing else. Put whatever the offsets depend on in the type, so the tuple is inferable — then nstencil, reach, foreach_offset, fold_offsets and every neighbour query work on it, with the loop still unrolled and nothing allocated.\n\nstruct Upwind{R} <: S.AbstractStencil end\nUpwind(r::Integer) = Upwind{Int(r)}()\nS.offsets(::Upwind{R}, ::Val{N}) where {R,N} =\n    ntuple(i -> ntuple(d -> d == cld(i, R) ? mod1(i, R) : 0, Val(N)), Val(N * R))\n\nS.offsets(Upwind(2), Val(2)), S.nstencil(Upwind(2), Val(3)), S.reach(Upwind(2), Val(2))\n\ngu = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry(),\n                             range(0.0; step = 1.0, length = 6),\n                             range(0.0; step = 1.0, length = 5))\nFG.Connectivity.nneighbors(gu, 2, 2; stencil = Upwind(1)),\nFG.Connectivity.nneighbors(gu, 6, 5; stencil = Upwind(1))\n\nTwo different things get called a radius, and they are separate types. Stencils.CellRadius counts cells in index space; Stencils.MetricBall is a physical distance measured through the geometry. On a stretched or spherical grid the number of cells within a given distance varies across the grid, so the two cannot be collapsed into one.","title":"Your own shape"},{"category":"section","location":"connectivity/#Neighbourhoods-by-distance","page":"Stencils & Connectivity","text":"A Stencils.MetricBall is queried with Connectivity.neighbors_within and its buffer/counting forms — every cell whose centre lies within the given physical distance, under the geometry's own metric:\n\ngeo = FG.Geometry.SphericalGeometry()            # Earth radius, metres\nλ = range(0, 2π; length = 25)[1:24]\nφ = range(-π/2, π/2; length = 13)\ng = FG.Grids.StructuredGrid(geo, λ, φ)\nball = S.MetricBall(2.0e6)                       # everything within 2000 km\nFG.Connectivity.nneighbors_within(g, 5, 7; ball = ball)\n\nThe same call near a pole finds many more cells, because 2000 km spans every longitude there — which is exactly why this cannot be a stencil:\n\nFG.Connectivity.nneighbors_within(g, 5, 13; ball = ball)\n\nA bare number works as the ball, and the buffer form follows the neighbors! pattern — size with the count, fill in place:\n\nn = FG.Connectivity.nneighbors_within(g, 5, 7; ball = 2.0e6)\nbuf = Vector{Int}(undef, n)\nFG.Connectivity.neighbors_within!(buf, g, 5, 7; ball = 2.0e6)\nbuf\n\nThe candidate window is Connectivity.metric_window — O(1) per direction on any separable axis, uniform or stretched, given the minimum steps Connectivity.MetricTopology carries — and each candidate is then kept or dropped by the geometry's distance, so the result is a genuine metric ball: great-circle on a sphere, Vincenty on a spheroid, the chord where a radial or height direction is present. Periodic directions wrap by minimum image, so a ball sitting on the seam is the same ball as anywhere else:\n\nFG.Connectivity.neighbors_within(g, 1, 7; ball = 1.5e6) ==\n    FG.Connectivity.neighbors_within(g, 1, 7; ball = S.MetricBall(1.5e6))","title":"Neighbourhoods by distance"},{"category":"section","location":"connectivity/#Distance-between-two-cells","page":"Stencils & Connectivity","text":"Geometry.distance also takes two cell indices, resolving them through the grid's own topology. Across a periodic seam it takes the short way round:\n\ngper = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry(),\n                               range(0.0; step = 1.0, length = 10),\n                               range(0.0; step = 1.0, length = 6);\n                               periodic = true, period = 10.0)\nFG.Geometry.distance(gper, (1, 1), (10, 1)), FG.Grids.displacement(gper, (1, 1), (10, 1))\n\nGrids.displacement is the signed per-direction offset that distance was taken from. It is a coordinate quantity, so it sits in Grids, while the distance extends Geometry.distance.","title":"Distance between two cells"},{"category":"section","location":"connectivity/#Convolutions-over-every-image","page":"Stencils & Connectivity","text":"A neighbour set visits each cell once, at its nearest image. A convolution on a torus cannot: with period L,\n\nbar f(x) = int K(x-y) f(y)dy = sum_k int_textcell K(x-y-kL) f(y)dy\n\nso once the kernel support exceeds L/2 one cell contributes through several images at different displacements. Connectivity.fold_within takes the convention as an argument — NearestImage or AllImages — and AllImages also widens the window past metric_window's one-turn cap, which excludes those images.\n\nNx, Δx = 32, 62.5; Lx = Nx * Δx\naxx = range(0.0; step = Δx, length = Nx)\ngt = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry(), axx, axx;\n                             periodic = (true, true), period = (Lx, Lx))\ncount_within(conv, rad) = FG.Connectivity.fold_within(\n    (a, J, d) -> a + 1, 0, gt, 1, 1; ball = rad, images = conv)\ncount_within(FG.Connectivity.NearestImage(), 3500.0),\ncount_within(FG.Connectivity.AllImages(), 3500.0)\n\nBelow L/2 the two agree exactly. Above it, filtering one Fourier mode with a Gaussian of width ℓ = L reproduces the analytic transfer exp(-k²ℓ²/4α) to roundoff under AllImages; under nearest-image the error stays at 40% however far the support is widened, since a dropped image is not recovered by searching further.\n\nSumming images asserts that a periodic direction is a translation of the domain. On a sphere it is an identification — λ and λ+2π are the same point — so AllImages raises there, where summing images counts one cell repeatedly.\n\nself = true folds the centre cell too, at distance zero. A neighbour set excludes it; a convolution needs it, and it carries the kernel's largest weight.\n\nTo query every cell, materialize the whole graph once instead — Connectivity.build_connectivity_within is the ball analogue of build_connectivity, row k holding exactly what the per-cell query returns for cell k, and symmetric because the metric is:\n\ncsr = FG.Connectivity.build_connectivity_within(g; ball = 2.0e6)\nFG.Connectivity.nedges(csr), FG.Connectivity.is_symmetric_adjacency(csr)","title":"Convolutions over every image"},{"category":"section","location":"connectivity/#What-a-ball-query-reads-about-the-grid","page":"Stencils & Connectivity","text":"The observation the stencil side of the module rests on is that a neighbour computation never looks at a coordinate. It reads three things — extent per dimension, wrapping per dimension, and which cells are active — and nothing else. That triple is IndexTopology, under which a curvilinear grid needs no separate implementation from a structured one: it is the N = 2 case of the same algorithm.\n\nA ball query is the one thing that cannot work that way. It needs coordinates, and the smallest step per direction, which bounds the candidate window on a stretched axis. That is Connectivity.MetricTopology, the same idea for the metric path. It is O(1) to build: it reads the per-axis reductions through Grids.minimum_spacing and Grids.bounds, which every layout answers without a scan:\n\nmt = FG.Connectivity.MetricTopology(g)\nFG.Connectivity.nneighbors_within(g, 5, 7; ball = ball, topology = mt)\n\nSo passing it changes nothing and omitting it costs nothing — the default is already free. There is no hoisting to remember here.\n\nThe spatial index is different: it is worth hoisting, and building one costs O(n), so it pays back over many queries. Curvilinear and node grids have no separable axes for a window to bound, so an unindexed query tests every cell.\n\nGrids.cell_list is the one to reach for, and it needs no package at all. It bins the cell centres at the radius you mean to query at, and a query visits the bins its ball can reach:\n\ngc = FG.Grids.HEALPixGrid(8)\ntop = FG.Connectivity.MetricTopology(gc; index = FG.Grids.cell_list(gc; ball = 2.0e6))\nFG.Connectivity.nneighbors_within(gc, 1; ball = 2.0e6, topology = top)\n\nEvery cell lands in exactly one bin — periodicity wraps the bin coordinate, leaving the point where it is — so a query emits each cell once and needs no buffer to deduplicate into. It therefore runs inside a kernel, and the sweeps build one by default.\n\nConnectivity.indexed is the alternative, a k-d tree, and needs NearestNeighbors:\n\nusing NearestNeighbors                                    # loads the extension\ngu = FG.Grids.HEALPixGrid(8)\nixu = FG.Connectivity.indexed(gu)\nscratch = FG.Connectivity.ball_scratch()\nFG.Connectivity.nneighbors_within(gu, 1; ball = 2.0e6, topology = ixu, scratch = scratch)\n\nThe index only ever returns a superset of the ball — the exact distance ≤ r gate still decides membership — so an indexed query and a scan return the same cells, and loading the extension changes speed and nothing else.\n\nA neighbour list is a set: the cells come back in whatever order enumerated them, and no query sorts, keeping an O(m) query O(m). Connectivity.sort_neighbors! sorts on request. Connectivity.ball_scratch is the candidate buffer, one per task; a query given none allocates its own.","title":"What a ball query reads about the grid"},{"category":"section","location":"connectivity/#Sweeping-every-cell","page":"Stencils & Connectivity","text":"Better still, do not write the loop. Connectivity.foreach_within and Connectivity.mapreduce_within walk every cell's ball and build the topology and the index once for the whole sweep, making them O(n log n) where a per-cell loop that rebuilds the index is O(n²):\n\nFG.Connectivity.mapreduce_within((I, J, d) -> 1, +, 0, g; ball = 1.0e6)   # total pairs within 1000 km\n\nFG.Connectivity.mapreduce_within((I, J, d) -> d, +, 0.0, g; ball = 1.0e6) # and the total distance\n\nforeach_within is the same traversal for an f that writes; under a threaded backend it runs on disjoint spans of cells, so what f writes must be determined by I. build_connectivity_within does the same hoisting internally.","title":"Sweeping every cell"},{"category":"section","location":"connectivity/#The-k-nearest","page":"Stencils & Connectivity","text":"A ball asks \"everything within r\". The other question is \"the nearest k\", and Connectivity.k_nearest answers it exactly under the same metric, on every architecture:\n\nidx, dist = FG.Connectivity.k_nearest(g, 5, 7; k = 4)\ndist\n\nIt widens a ball until k cells have been seen and keeps the k smallest in a bounded heap, so the result never depends on the starting radius and nothing is materialized along the way. Equal distances resolve by linear index, so an indexed query and a scan agree exactly. Connectivity.k_nearest! writes into your own buffers and allocates nothing.","title":"The k nearest"},{"category":"section","location":"connectivity/#Seeding-from-a-coordinate","page":"Stencils & Connectivity","text":"Every query above starts at a cell. Observational data does not arrive that way — a station, a ship track or a float has a coordinate, and the cell it belongs to is part of the question. Passing a coordinate in place of the cell indices asks the same questions about a point — written as a Tuple, NamedTuple, AbstractVector or SVector, as anywhere else a point is taken:\n\nFG.Grids.locate(g, (0.4, 0.1))                    # the cell the point falls in\n\nlength(FG.Connectivity.neighbors_within(g, (0.4, 0.1); ball = 1.0e6)),\nFG.Connectivity.k_nearest(g, (0.4, 0.1); k = 3)[2]\n\nConnectivity.fold_at is the fold behind them, as Connectivity.fold_within is for a cell. There is no seed cell to skip, so unlike the cell-seeded form every cell within the ball is visited — including the point's own.\n\nlocate is the containing cell on a rectilinear grid, per direction and wrapping a periodic one, and the nearest cell centre elsewhere. For a node set those coincide, its cells being the Voronoi regions of its nodes; on a strongly sheared curvilinear grid they can differ, so the contract is nearest-centre.\n\nThe traversal is the cell-seeded one with the window widened by how far the point sits from its cell's centre, keeping it O(1) per direction. Off the rectilinear grids the cost depends on the index: Grids.cell_list answers \"which bins does this point's ball reach\" directly, so a point query visits only those bins.","title":"Seeding from a coordinate"},{"category":"section","location":"connectivity/#The-ball,-and-the-part-of-it-you-can-get-to","page":"Stencils & Connectivity","text":"A ball is not a connected patch. With a mask, or a concave domain, it can contain cells that are close to the seed in space but reachable from it only by leaving the ball. Both sets are useful and they are different algorithms, so reach names which one you get:\n\nwall = trues(9, 9); wall[:, 5] .= false                  # a barrier through the middle\ngw = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry(),\n                             collect(0.0:8.0), collect(0.0:8.0), wall)\nlength(FG.Connectivity.neighbors_within(gw, 5, 3; ball = 3.5)),\nlength(FG.Connectivity.neighbors_within(gw, 5, 3; ball = 3.5,\n                                        reach = FG.Connectivity.Connected(S.Moore(1))))\n\nConnectivity.Unrestricted is every cell within the radius, and the default. Connectivity.Connected is the connected component of that set containing the seed: it walks adjacency and prunes at the ball's edge, so it is a subset. The two agree whenever the ball is connected under the adjacency — always so on a maskless Cartesian StructuredGrid, where each index step moves monotonically in one coordinate — and Connected is strictly smaller wherever something separates two parts of the ball.\n\nNeither is a cheaper version of the other. Take cells P (the seed), Q and R, adjacent only as P–Q–R, with d(P,Q) = 1.2r and d(P,R) = 0.8r. Walking outward from P and dropping anything farther than r stops at Q, so it never reaches R, which is inside the ball. Such a walk computes Connected; the ball is reached only by a spatial query.\n\nAdjacency has to be named, because only a node set carries its own: Connected() means direction-1 adjacency on the index-space architectures and the stored neighbour lists on an UnstructuredGrid, and Connected(stencil) sets it explicitly for the former. On the latter a stencil has no index space to mean anything in, so it raises.","title":"The ball, and the part of it you can get to"},{"category":"section","location":"connectivity/#Querying-neighbours","page":"Stencils & Connectivity","text":"(Image: HEALPix and icosahedral connectivity)\n\nThree forms, in increasing order of how much they allocate:\n\nout = Vector{Int}(undef, 8)\nn = FG.Connectivity.neighbors!(out, grid, i, j; stencil = FG.Stencils.Moore(1))   # writes n indices\nk = FG.Connectivity.nneighbors(grid, i, j)                            # just the count\nit = FG.Grids.neighbors(grid, i, j)                            # lazy iterator, no array\n\nstencil is any Stencils shape — Axial(r), VonNeumann(r), Moore(r) (alias Vertex), Diagonal(r), Anisotropic(radii) or Custom(offsets) — at any radius and in any number of dimensions. A stencil is named by its type, so the neighbour iterator built from it resolves at compile time and allocates nothing per cell. active_only = true excludes masked-out cells from both ends of an edge.\n\nPrefer neighbors! in hot loops: neighbors returns a lazy iterator, and neighbors! writes into a buffer you own.","title":"Querying neighbours"},{"category":"section","location":"connectivity/#CSR","page":"Stencils & Connectivity","text":"For a whole graph at once:\n\nconn = FG.Connectivity.build_connectivity(grid)                   # or (sampling, args...)\nFG.Connectivity.nnodes(conn), FG.Connectivity.nedges(conn)\nFG.Grids.neighbors(conn, i)                                # a view into the flat array\n\nCSRConnectivity stores one flat neighbour array plus offsets, so a traversal reads contiguous memory and the whole graph is two allocations. Both buffers accept any Integer, and a builder picks the narrowest that holds both a node id and an offset: Int32 below two billion of either, halving the graph's memory and the bandwidth every traversal of it costs. Ti names a width explicitly.\n\nBuilding from a sampling never constructs a grid:\n\nFG.Connectivity.build_connectivity(FG.SphericalSampling.GaussLegendreSampling(), 1000)\n\nThe neighbour graph of a tensor-product sampling is fixed by its axis lengths and longitude wrapping alone, so the axes are never evaluated. For Gauss–Legendre, evaluating them is an O(n²) root solve whose result the graph does not depend on.","title":"CSR"},{"category":"section","location":"connectivity/#Index-topology-directly","page":"Stencils & Connectivity","text":"topo = FG.Connectivity.IndexTopology((nlon, nlat), (true, false), nothing)   # mask = nothing → all active\nconn = FG.Connectivity.build_connectivity(topo; stencil = FG.Stencils.Axial(1))\n\nUseful when you have a shape and a wrapping rule but no grid, and enough to drive every stencil query in the package.","title":"Index topology directly"},{"category":"section","location":"connectivity/#Dense-and-sparse-adjacency","page":"Stencils & Connectivity","text":"FG.Connectivity.adjacency_matrix(conn)                       # Matrix{Bool}\nFG.Connectivity.adjacency_matrix!(A, conn)                   # into your buffer\n# needs SparseArrays; see the Extensions page\n# FG.Connectivity.sparse_adjacency_matrix(grid)\n\nwarning: The dense form is N × N\nadjacency_matrix on a 1000² grid asks for a 10⁶ × 10⁶ Matrix{Bool} — 10¹² bytes. It exists for small grids and for testing. Use sparse_adjacency_matrix for anything real.\n\nThe sparse path builds CSC straight from the neighbour list by one counting pass and one placement pass — no coordinate triples, no sort, no permutation vector, and row indices come out ascending for free because the placement walks nodes in order.\n\nFor a symmetric adjacency with sorted rows the CSR and CSC arrays are the same arrays, so sparse_adjacency_matrix(grid) hands the connectivity's own buffers to the matrix, with no transpose and no second copy.\n\nWhether that holds is read from the layout before the graph exists. Under an index stencil it is the stencil's own symmetry; a formula layout answers with Grids.has_symmetric_adjacency, which HEALPix, the cubed sphere, the icosahedral geodesic and Yin–Yang declare. A ring grid does not: adjacent rings of unequal width make its straddling relation directed, so it takes the transpose.\n\nFG.Grids.has_symmetric_adjacency(FG.Grids.HEALPixGrid(4))   # licenses the shortcut, before the build\nFG.Connectivity.is_symmetric_adjacency(conn)                # the same question of a built graph\nFG.Connectivity.sort_neighbors!(conn)                       # order each node's block ascending","title":"Dense and sparse adjacency"},{"category":"section","location":"connectivity/#Threading","page":"Stencils & Connectivity","text":"Every builder takes an opt-in backend; see Performance.\n\nusing ComputationalBackends: ThreadedBackend\nFG.Connectivity.build_connectivity(grid; backend = ThreadedBackend())\n\nResults are bit-identical to serial; the test suite asserts it with ==.","title":"Threading"},{"category":"section","location":"connectivity/#Sampling-specific-topology","page":"Stencils & Connectivity","text":"FG.Connectivity.build_connectivity(FG.SphericalSampling.HEALPixSampling(64))              # RING face-table adjacency\nFG.Connectivity.build_connectivity(FG.SphericalSampling.CubedSphereSampling(), n)         # with the gnomonic seam fold\nFG.Connectivity.build_connectivity(FG.SphericalSampling.YinYangSampling(), nlon, nlat)\nFG.Connectivity.healpix_neighbors!(out, nside, 0)             # one pixel; 0-based pixel ids\n\nHEALPix RING neighbours follow the standard face-table algorithm (Górski et al. 2005; Reinecke 2003). The offsets are walked in ring order, so the emitted ids arrive already ascending for ~99% of pixels and the dedup pass has almost nothing to move.","title":"Sampling-specific topology"},{"category":"section","location":"connectivity/#Mask-topology","page":"Stencils & Connectivity","text":"The mask's own shape is a grid property, so it is answered here.\n\nC = FG.Connectivity\ngeo = FG.Geometry.CartesianGeometry()\nm = trues(9, 9)\nm[4:6, 4:6] .= false          # an enclosed block\ng = FG.Grids.StructuredGrid(geo, 0.0:1.0:8.0, 0.0:1.0:8.0, m)\n\nC.count_holes(g), C.connected_components(g)[2]\n\ncount(C.interior(g)), count(C.boundary_cells(g)), count(m)\n\ninterior is the active cells whose whole stencil is active and in range; boundary_cells is the rest of the active set, and the two partition it. count_holes counts the connected inactive regions fully enclosed by active cells — an estimate of the active region's first Betti number. A region that reaches a non-wrapping edge counts as outside, so wrapping a direction can turn an outside region into an enclosed one:\n\ngp = FG.Grids.StructuredGrid(geo, 0.0:1.0:8.0, 0.0:1.0:8.0, m;\n                             periodic = (true, true), period = (9.0, 9.0))\nC.count_holes(gp)\n\nAll of it is dimension-generic:\n\nm3 = trues(7, 7, 7); m3[3:5, 3:5, 3:5] .= false\nC.count_holes(FG.Grids.StructuredGrid(geo, 0.0:1.0:6.0, 0.0:1.0:6.0, 0.0:1.0:6.0, m3))","title":"Mask topology"},{"category":"section","location":"connectivity/#Index-helpers","page":"Stencils & Connectivity","text":"FG.Connectivity.linear_index(grid, i, j)      # column-major, i fastest\nFG.Connectivity.cartesian_index(grid, lin)    # the inverse","title":"Index helpers"},{"category":"section","location":"performance/#performance-page","page":"Performance","text":"The design rules, and the measurements behind them.","title":"Performance"},{"category":"section","location":"performance/#Rules","page":"Performance","text":"No superlinear algorithm where a linear one exists.\nIntrinsic data is O(∑ Nᵈ); anything O(∏ Nᵈ) is a materialization — make it opt-in.\nCompute nothing twice, across calls as well as within one.\nOne algorithm, one implementation.\nNo transcendental in an inner loop that a recurrence, sincos, or a hoist can remove.\nNever compute at runtime what is a mathematical constant.\nAn API must not be able to request a terabyte by accident.","title":"Rules"},{"category":"section","location":"performance/#Scaling","page":"Performance","text":"Every public entry point is linear or better in its problem size. Measured as cost ~ nᵖ across a size step:\n\nentry point MiB allocs t ~ nᵖ\nlatitude_weights Gauss–Legendre 0.01 3 0.99\nspherical_axes Gauss–Legendre 0.02 6 0.96\nspherical_quadrature Gauss–Legendre 0.03 9 0.97\nlatitude_weights Driscoll–Healy 0.02 11 0.34\nlatitude_weights Clenshaw–Curtis 0.02 11 1.14\nUnstructuredGrid k-d tree, 40k 6.23 52 1.06\nbuild_connectivity sampling 91.48 9 0.93\nunstructured_grid icosahedral 4.00 80 1.04\n\nAllocation counts are flat in n everywhere. build_connectivity's 91 MiB is the CSR output for ~2M nodes.","title":"Scaling"},{"category":"section","location":"performance/#Ball-queries:-nothing-per-query-that-belongs-to-the-grid","page":"Performance","text":"A ball query's costs belong to the grid: the smallest step per direction, which bounds the candidate window, the span of each axis, and — where there are no separable axes to bound with — a spatial index. The first two are read through Grids.minimum_spacing and Grids.bounds, which every layout answers in O(1) — a rectilinear grid from reductions taken once when it was built, a layout whose coordinates are a formula from its own parameters. Connectivity.MetricTopology carries them for a whole sweep.\n\nThe effect is that the per-query default costs nothing, so there is no hoisting to get right. On a stretched axis, with the radius and the window's cell count held fixed:\n\naxis length default topology explicitly hoisted\n256 0.029 µs 0.029 µs\n1 024 0.030 µs 0.030 µs\n4 096 0.029 µs 0.029 µs\n16 384 0.030 µs 0.029 µs\n\nBoth columns allocate nothing, and both are flat in the axis length: the per-direction reductions are taken once, so no query rescans a stretched axis. Every Gaussian-latitude grid has a stretched axis.","title":"Ball queries: nothing per query that belongs to the grid"},{"category":"section","location":"performance/#A-heterogeneous-coordinate-tuple","page":"Performance","text":"Each direction keeps whatever AbstractVector type it was built from, so a grid with a range for longitude and a vector for latitude has a heterogeneous coordinate tuple. Indexing that tuple with a loop variable is a dynamic lookup, so every read of it goes through a recursive tail-split that keeps each branch concretely typed.\n\nEvery per-cell entry point is therefore allocation-free on every grid shape — coords, distance(grid, I, J), nneighbors_within and fold_within all at 0 B — and the suite checks it across a matrix of shapes, mixed-axis ones included.","title":"A heterogeneous coordinate tuple"},{"category":"section","location":"performance/#Which-index","page":"Performance","text":"Two are available. Grids.cell_list needs no package, and on a 65 536-cell curvilinear grid it builds faster, queries faster and allocates less than the k-d tree, so it is what the sweeps build by default:\n\n build per query bytes per query\ncell list 2.75 ms 1.08 µs 256\nk-d tree (NearestNeighbors) 8.95 ms 1.25 µs 672\nno index (scan) — 646 µs 0\n\nThe cell list also enumerates through a fold, so a query holds no buffer and runs inside a kernel. A tree searches replicated points and has to deduplicate, so it needs one.\n\nIts build is O(n), with the point dimension lifted into the type behind a function barrier; left as a runtime value it leaves the whole construction loop dynamically dispatched.","title":"Which index"},{"category":"section","location":"performance/#Indexing-the-architectures-with-no-axes","page":"Performance","text":"Curvilinear and node grids have no window to bound, so a query without an index tests every cell. With Connectivity.indexed (needs NearestNeighbors), one query on a 2-D curvilinear grid, radius fixed at 2.5 cells:\n\ncells indexed scanning speedup\n1 024 0.741 µs 4.892 µs 6.6×\n4 096 0.952 µs 20.045 µs 21.0×\n16 384 0.946 µs 77.308 µs 81.7×\n65 536 0.927 µs 306.059 µs 330×\n\nand the whole-grid build, which is n of those queries:\n\ncells indexed scanning speedup\n1 024 0.002 s 0.010 s 4.2×\n4 096 0.011 s 0.165 s 14.9×\n16 384 0.048 s 2.574 s 53.6×\n65 536 0.208 s 41.519 s 200×\n\nO(n log n) against O(n²). The index returns a superset and the exact distance gate still decides membership, so both columns give the same graph — the index buys speed and changes nothing else. Rows come out in whatever order enumerated them, as everywhere else here; sort_neighbors! if you need them ordered.\n\nBecause the index cannot be built per query, Connectivity.foreach_within and Connectivity.mapreduce_within exist to build it once for a whole sweep: 800.7 ms → 88.8 ms (9.0×) against the same sweep written as a hand loop over the per-cell entry points, on 9 216 cells.\n\nConnectivity.ball_scratch supplies the candidate buffer, which takes an indexed query to zero allocation whatever the grid size, against 480 bytes on a small ball and 6.1 KB on a 310-candidate one when the query allocates its own.","title":"Indexing the architectures with no axes"},{"category":"section","location":"performance/#Quadrature","page":"Performance","text":"(Image: Quadrature exactness and cost)\n\nGauss–Legendre holds machine precision past degree 2N−1; Driscoll–Healy and Clenshaw–Curtis lose exactness just after N−1, which is the distinction admits_exact_bandlimited_quadrature encodes. The fitted exponents on the right are measured per run.\n\nGauss–Legendre uses asymptotic expansions (Bogaert 2014; Hale & Townsend 2013) above n = 60: each node sits near j_k/(n+½) for j_k a zero of J₀, with corrections in powers of (n+½)⁻². Nothing iterates and no root depends on its neighbours, so it is O(1) per node and allocation-free.\n\nn = 2048 Golub–Welsch asymptotic expansion\ntime 279.4 ms 0.05 ms\nmemory 33.4 MiB 32 KiB\n\nAccuracy against a 256-bit reference is ~6e-16 relative weight error at every n from 64 to 4096.\n\nBelow n = 60, and for element types wider than Float64, the solve falls back to Newton on the Bonnet recurrence: the expansion's coefficients are a fixed Float64 set and cannot exceed that precision, while Newton converges to eps(T) — BigFloat at 256 bits gives |Σw − 2| = 1.7e-77.\n\nEquiangular (DH/CC) weights are O(n log n) with an FFT loaded and O(n²) without, with the two agreeing to 1.4e-14.","title":"Quadrature"},{"category":"section","location":"performance/#Memory","page":"Performance","text":"Neither of the two large per-cell fields is materialized:\n\nCell measure — held as per-axis factors: 0.046 MiB at 2000², against 61.0 MiB dense.\nCurvilinear corner directions — two rows live at a time: 23.1 MiB at 1000², against 61.3 MiB for the whole field, with bit-identical areas.\n\nThe factored measure is also faster to read. At 2000² the dense array is 61 MiB and DRAM-bound while the factors stay in cache:\n\naccess factored vs dense\nfull sweep 0.91×\nstrided, radius 8 0.43×\nstrided, radius 32 0.40×\nsum ~6000×\n\nwarning: Measure at realistic sizes\nA memory-bound comparison taken at a size where the dense array still fits in cache measures the cache. These are taken at 2000² and above.","title":"Memory"},{"category":"section","location":"performance/#Threading","page":"Performance","text":"Opt-in through ComputationalBackends tags, serial by default, bit-identical results:\n\nkernel speedup (8 threads)\ncubed_sphere_points 6.2×\ncurvilinear corner areas 4.9×\nindex-topology connectivity 3.7–4.3×\ncandidate CSR builder (HEALPix / cubed sphere / Yin–Yang) 1.8–3.7×\n\nusing ComputationalBackends: ThreadedBackend\nFG.Connectivity.build_connectivity(grid; backend = ThreadedBackend())\n\nChunks are contiguous, so each thread touches one span of every array. Passing nothing (the default) hands the loop body the whole range in one call, so the serial path adds no partitioning at all.\n\nTwo passes are not threaded, for structural reasons. In the CSR compacting move, row j's destination can fall inside row i's source block for i < j, so concurrent rows overwrite unread candidates. And the prefix scan between the count and fill passes is inherently sequential; it is O(n) against the O(n·stencil) passes it separates.","title":"Threading"},{"category":"section","location":"performance/#Measured-non-issues","page":"Performance","text":"getproperty coordinate-name lookup is free — 0.065 vs 0.077 ms per 10⁶ reads; it const-folds.\nThe dense-mask branch is free in a predictable loop.\nCross-point SIMD is unavailable — @simd over 10⁶ haversines is 1.01×; LLVM cannot vectorize sin/cos/atan. Eliminating trig is the only lever, so the geometry kernels use sincos and hoist atan out of loops.\nVector-axis construction is not superlinear — the gap against a uniform axis is a constant factor.\nThe ball-query candidate buffer is mostly an allocation win — 1.43× in time on a 20-candidate ball and 1.06× on a 310-candidate one, at zero bytes per query against 480 B and 6.1 KB. Worth passing in a sweep; the time it saves shows up only where the ball is small enough that the allocation is most of the query.","title":"Measured non-issues"},{"category":"section","location":"axes/#axes-page","page":"Axes","text":"An axis answers: where along this direction are the samples, and is the spacing constant?\n\nThe second half is answered by the axis's type, so the fast paths that depend on constant spacing are selected at compile time.","title":"Axes"},{"category":"section","location":"axes/#Uniform-or-not","page":"Axes","text":"using FlowGeometries: FlowGeometries as FG\nA = FG.Axes\n\nu = A.UniformAxis(0.0, 0.25, 5)     # origin, Δ, n\nv = collect(u)                       # the same numbers, as a plain Vector\n\nA.isuniform(u), A.isuniform(v)\n\nisuniform reads the type, and that is the only question this package asks. A Vector holding an arithmetic sequence answers false, and no code path inspects coordinate values to decide it — no constructor sniffs your data, and there is no tolerance anywhere.\n\nWhere the data question genuinely matters, the spacing accessors answer it exactly: an axis is equally spaced precisely when its smallest gap equals its largest.\n\ngeo = FG.Geometry.CartesianGeometry()\ngv = FG.Grids.StructuredGrid(geo, v, v)\nFG.Grids.minimum_spacing(gv, 1) == FG.Grids.maximum_spacing(gv, 1)\n\nIf that holds and you want the fast path, build the axis yourself. The conversion replaces your coordinates with the exact arithmetic sequence, so it is yours to ask for:\n\nΔ = FG.Grids.minimum_spacing(gv, 1)            # equals maximum_spacing, so it IS the spacing\nA.spacing(A.UniformAxis(first(v), Δ, length(v)))","title":"Uniform or not"},{"category":"section","location":"axes/#Your-axis-type-is-kept","page":"Axes","text":"A grid stores the axis you hand it. Any AbstractRange already of the geometry's element type passes through untouched — your own range subtype, a StepRangeLen whose TwicePrecision internals you want, a BigFloat-backed range:\n\nr = range(0.0; step = 0.5, length = 9)\ng = FG.Grids.StructuredGrid(geo, r, r)\nFG.Grids.coordinates(g, 1) === r, typeof(FG.Grids.coordinates(g, 1))\n\nNothing needs converting to earn the fast paths — they dispatch on spacing_trait, which is UniformSpacing() for every range, so whatever you brought gets them:\n\nFG.Grids.isuniform(g), FG.Grids.spacing(g, 1), typeof.(FG.Grids.measure_factors(g))\n\nConversion happens only where the element type must change, since an arbitrary range subtype cannot be rebuilt at a new eltype generically. That case becomes a UniformAxis{T}:\n\ngi = FG.Grids.StructuredGrid(geo, 0:8, 0:8)      # Int range, Float64 grid\ntypeof(FG.Grids.coordinates(gi, 1))\n\nA UniformAxis stores three numbers and computes origin + (i-1)·Δ in its own element type; uniform_axis builds one. range(0f0; step = 0.25f0, length = 5) is a StepRangeLen{Float32, Float64, Float64, Int} — Float32 elements over a Float64 offset and step — so a Float32 grid built from one does Float64 index arithmetic. The grid keeps what you passed, and converting is opt-in:\n\ntypeof(range(0.0f0; step = 0.25f0, length = 5)), typeof(A.uniform_axis(Float32, range(0.0f0; step = 0.25f0, length = 5)))\n\nΔ may be negative, for an axis stored descending — which is routine, and which nothing in the package assumes away:\n\nd = A.UniformAxis(1.0, -0.25, 5)\nA.spacing(d), extrema(d), collect(d)\n\nA slice, a reversal, or any affine map of a uniform axis is still uniform, so the guarantee is not lost by manipulating it:\n\nA.isuniform(u[2:4]), A.isuniform(reverse(u)), A.isuniform(2.0 .* u .+ 1.0)\n\nAnything that is not affine becomes a plain array, as it must:\n\nA.isuniform(cos.(u))","title":"Your axis type is kept"},{"category":"section","location":"axes/#Your-own-uniform-axis-type","page":"Axes","text":"If you want a uniform axis carrying something extra — a name, a unit tag, a provenance field — subtype AbstractUniformAxis and implement the three methods AbstractRange requires anyway:\n\nstruct NamedAxis{T} <: A.AbstractUniformAxis{T}\n    start::T\n    h::T\n    count::Int\n    name::Symbol\nend\nBase.first(a::NamedAxis) = a.start\nBase.step(a::NamedAxis) = a.h\nBase.length(a::NamedAxis) = a.count\n\nz = NamedAxis(0.0, 0.25, 5, :depth)\ncollect(z), sum(z), extrema(z), z[3], collect(z[2:4]), collect(reverse(z))\n\nThat is the whole contract. Indexing, the O(1) reductions, slicing, reversal and the affine arithmetic all follow, and no generic method reads a field — the names above are nothing like UniformAxis's and it makes no difference:\n\ncollect(2.0 .* z .+ 1.0), A.isuniform(z), A.spacing(z)\n\nA derived axis is a plain UniformAxis, since the extra field has no generic meaning under a slice. Define similar_axis to say what it should be instead:\n\nA.similar_axis(a::NamedAxis{T}, origin, Δ, n::Integer) where {T} =\n    NamedAxis{T}(convert(T, origin), convert(T, Δ), Int(n), a.name)\n\nzz = reverse(z)[2:3] .+ 1.0\ntypeof(zz), zz.name, collect(zz)\n\nSuch an axis is stored as itself on a grid and takes every uniform fast path:\n\ngz = FG.Grids.StructuredGrid(geo, z, z)\nFG.Grids.coordinates(gz, 1) === z, FG.Grids.isuniform(gz),\ntypeof.(FG.Grids.measure_factors(gz)), Base.summarysize(gz)","title":"Your own uniform axis type"},{"category":"section","location":"axes/#It-is-an-AbstractRange","page":"Axes","text":"u isa AbstractRange, step(u)\n\nTwo things follow. Base solves searchsorted on a range in closed form, so lookup cost is flat in the axis length where the equivalent Vector bisects. And any package that already tests isa AbstractRange to pick a uniform-grid fast path accepts this type without knowing it exists.\n\nbig = A.UniformAxis(0.0, 1e-7, 10^7)\nsearchsortedfirst(big, 0.5), searchsortedfirst(big, 0.5) == searchsortedfirst(collect(0.0:1e-7:0.9999999), 0.5)","title":"It is an AbstractRange"},{"category":"section","location":"axes/#What-it-buys","page":"Axes","text":"minimum, maximum, extrema and sum are closed forms, and the axis is isbits, so moving it to another storage backend costs nothing.\n\nsum(u), extrema(u), isbits(u), sizeof(u)\n\nOn a grid the payoff is the cell measure. A uniform axis's per-cell width is one number repeated, held as a ConstantVector:\n\ngeo = FG.Geometry.CartesianGeometry()\nN = 2000\ng = FG.Grids.StructuredGrid(geo, range(0.0; step = 0.5, length = N),\n                            range(0.0; step = 0.25, length = N))\ntypeof.(FG.Grids.measure_factors(g)), Base.summarysize(g)\n\nThat whole 2000×2000 grid is a few hundred bytes. A stretched axis is fully supported and simply stores what it must:\n\nxs = cumsum([0.0, 1.0, 0.3, 2.5, 0.7])\ngs = FG.Grids.StructuredGrid(geo, xs, xs)\nFG.Grids.isuniform(gs), typeof(FG.Grids.measure_factors(gs)[1])","title":"What it buys"},{"category":"section","location":"axes/#Grid-accessors","page":"Axes","text":"FG.Grids.isuniform(g, 1), FG.Grids.spacing(g, 1), FG.Grids.origin(g, 1),\nFG.Grids.bounds(g, 1), FG.Grids.extent(g, 1),\nFG.Grids.minimum_spacing(gs, 1), FG.Grids.maximum_spacing(gs, 1)\n\nminimum_spacing and maximum_spacing are O(1) on a uniform direction and O(N) otherwise. They bound how far an index window must reach to cover a given physical distance, as a neighbourhood-by-distance query on a stretched axis needs.","title":"Grid accessors"},{"category":"section","location":"grids/#grids-page","page":"Grids","text":"A grid answers: how is the data stored? It pairs a geometry with coordinates, a per-cell measure and an activity mask.\n\ntype coordinates use\nStructuredGrid one 1-D axis per direction rectilinear: lat–lon, any tensor-product sampling\nCurvilinearGrid one N-D array per direction logically rectangular, geometrically warped\nHEALPixGrid none — (nside, pixel) arithmetic the HEALPix pixelization\nCubedSphereGrid none — (n, cell) arithmetic the gnomonic cubed sphere\nYinYangGrid none — (nlon, nlat, cell) arithmetic Kageyama–Sato overset panels\nIcosahedralGrid none — (ν, id) arithmetic geodesic, 12 pentagons\nRingGrid one value per ring reduced Gaussian, octahedral\nUnstructuredGrid one value per node, plus CSR neighbours scattered points, an arbitrary mesh\n\nThe first two store coordinates because a warped mesh's are arbitrary; the last stores them because a node set's are the caller's. The five in between store none, or O(√n): a cell's position, its neighbours and its area are closed-form in the layout's parameters, so they hold those parameters instead. HEALPixGrid is the same size at nside = 1024 — 12.6 million pixels — as at nside = 1. Ask for the dense cloud with Grids.materialize when something outside needs it.","title":"Grids"},{"category":"section","location":"grids/#Building-one","page":"Grids","text":"using FlowGeometries: FlowGeometries as FG\n\n# From a sampling — the usual route\ngrid = FG.Connectivity.structured_grid(FG.SphericalSampling.ClenshawCurtisSampling(), 64)\ng    = FG.Grids.HEALPixGrid(16)\nrg   = FG.Grids.RingGrid(FG.SphericalSampling.OctahedralGaussianSampling(64))\ncs   = FG.Grids.CubedSphereGrid(24)\nyy   = FG.Grids.YinYangGrid(32, 22)\nico  = FG.Grids.IcosahedralGrid(16)\n\n# Or directly, in any number of dimensions; the mask is optional\nsg  = FG.Grids.StructuredGrid(FG.Geometry.SphericalGeometry(), λaxis, φaxis, mask)\ncg  = FG.Grids.CurvilinearGrid(FG.Geometry.SphericalGeometry(), λ2d, φ2d, mask)\ng4  = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry(),\n                              0:1.0:3, 0:1.0:3, 0:1.0:3, 0:1.0:3)  # 4-D, all active\nsize(g4)\n\nA formula layout's storage does not depend on its resolution, and the arithmetic is what answers every per-cell question:\n\ng1024 = FG.Grids.HEALPixGrid(1024)                 # 12 582 912 pixels\nBase.summarysize(g) == Base.summarysize(g1024), length(g1024)\n\nFG.Grids.coords(g, 100), FG.Grids.measure(g, 100), collect(FG.Grids.neighbors(g, 100))\n\nλ, φ = FG.Grids.materialize(g)                     # the dense cloud, asked for explicitly\nlength(λ)\n\n# The panel layouts answer the same way, and `sum(measure)` is the region they tile\nsum(FG.Grids.measure(cs)) / (4π * FG.Geometry.radius(FG.Grids.grid_geometry(cs))^2),\nsum(FG.Grids.measure(yy)) / (4π * FG.Geometry.radius(FG.Grids.grid_geometry(yy))^2)","title":"Building one"},{"category":"section","location":"grids/#The-common-interface","page":"Grids","text":"Everything below works on every architecture:\n\nout = zeros(2)\nFG.Grids.coords(grid, i, j)                        # (λ = …, φ = …) or (x = …, y = …)\nFG.Grids.coords!(out, grid, i, j)                  # into your buffer\nFG.Grids.coords(NTuple{2,Float64}, grid, i, j)     # a specific storage type\nFG.Grids.measure(grid, i, j)                       # cell area / volume / control-volume size\nFG.Grids.isactive(grid, i, j)                      # participates in the domain?\ncollect(FG.Grids.neighbors(grid, i, j))            # lazy iterator over neighbour indices\nsize(grid), length(grid), FG.Grids.size_tuple(grid)\nFG.Grids.isperiodic(grid, 1), FG.Grids.period(grid, 1)\n\nCoordinates are also reachable by their geometry-correct names — grid.λ, grid.φ on a sphere; grid.x, grid.y on a plane.","title":"The common interface"},{"category":"section","location":"grids/#Cell-measure-is-stored-factored","page":"Grids","text":"On a rectilinear grid every measure this package supports is a product of one factor per axis: Cartesian Δx·Δy·Δz, spherical R²cosφ·Δλ·Δφ = (Δλ)·(R²cosφ·Δφ). A StructuredGrid stores those ∑ Nᵈ factors, and the ∏ Nᵈ products come out of them.\n\nm = FG.Grids.measure(grid)           # a SeparableMeasure — a real AbstractArray\nm[3, 5]                        # indexes exactly like the dense outer product\nsum(m)                         # ∏ᵈ ∑ᵢ wᵈᵢ — O(∑ Nᵈ) against the dense O(∏ Nᵈ)\nFG.Grids.measure_factors(grid)       # the per-axis factors, or `nothing`\nFG.Grids.measure_array(grid)         # materialize densely, if you really need it\n\nIndexing, broadcasting and collect behave exactly as for the dense array, and the values are bit-identical. At 2000² the measure is 0.046 MiB against 61.0 MiB dense, and faster to read on every access pattern measured: the factors stay in cache while a 61 MiB array is DRAM-bound. See Performance.\n\nOperations that keep the measure a product stay factored, so a unit conversion does not undo the saving: scaling, a multiplicative map (abs, abs2, sqrt, inv), and a factor-wise product or quotient of two measures.\n\nkm² = m ./ 1e6\ntypeof(km²).name.name, Base.summarysize(km²), sum(km²) ≈ sum(m) / 1e6\n\nAnything else materializes, correct and dense. exp is the clean example: it is not multiplicative, so exp(∏wᵈ) ≠ ∏exp(wᵈ) and there is no factored form to keep. A negative scale materializes too, non-negative factors being an invariant the findmax/findmin shortcut relies on.\n\ntypeof(exp.(m)).name.name, typeof((-1.0) .* m).name.name\n\nsum being O(∑Nᵈ) is also what stops show from adding eight million numbers to print one line.","title":"Cell measure is stored factored"},{"category":"section","location":"grids/#Masks","page":"Grids","text":"FG.Grids.mask(grid)                  # the mask array\nFG.Grids.isactive(grid, i, j)        # false = excluded (land, say)\n\nWhen every cell participates the mask is AllActive, which stores its size and nothing else — getindex folds to a constant and count is length without a scan. Pass a real BitArray or Array{Bool} when you need to exclude cells.","title":"Masks"},{"category":"section","location":"grids/#Curvilinear-corners","page":"Grids","text":"CurvilinearGrid's exact quadrilateral cell area comes from its cell vertices — the spherical excess of the two triangles through the four corner directions on a sphere, the shoelace area on a plane.\n\ncg2 = FG.Grids.CurvilinearGrid(geo, λ2d, φ2d, mask; x_corner = λc, y_corner = φc)\nsize(FG.Grids.corners(cg2, 1)), FG.Grids.corner_coords(cg2, i, j)\n\nSupply x_corner/y_corner when your source model ships its own cell-vertex grid; otherwise they are reconstructed from the centres, which needs at least a 2×2 grid.\n\nVertices you supply are kept on the grid. Reconstructed ones are input to the area kernel, and are kept only if you ask — one array per direction, one larger in every direction:\n\nlean = FG.Grids.CurvilinearGrid(geo, λ2d, φ2d, mask)\nfull = FG.Grids.CurvilinearGrid(geo, λ2d, φ2d, mask; keep_corners = true)\nFG.Grids.has_corners(lean), FG.Grids.has_corners(full),\n    FG.Grids.measure(lean) == FG.Grids.measure(full),\n    round(Base.summarysize(lean) / Base.summarysize(full); digits = 2)\n\nA curvilinear grid takes any number of directions — one N-D array each, mask last. Beyond 2-D the cell measure is yours to pass: the corner-area kernel is an exact-quadrilateral algorithm with no N-D generalization here, so asking it for a 3-D measure raises.\n\ncart = FG.Geometry.CartesianGeometry()\nX = [x for x in 0.0:1.0:3.0, _ in 1:3, _ in 1:2]\nY = [y for _ in 1:4, y in 0.0:2.0:4.0, _ in 1:2]\nZ = [z for _ in 1:4, _ in 1:3, z in 0.0:0.5:0.5]\ncg3 = FG.Grids.CurvilinearGrid(cart, X, Y, Z, fill(1.0, 4, 3, 2), trues(4, 3, 2))\nsize(cg3), FG.Grids.coordinate_names(cg3), FG.Grids.coords(cg3, 2, 3, 2)\n\nThe measure goes either before the mask, as above, or as the measure keyword — the same array either way:\n\nFG.Grids.measure(FG.Grids.CurvilinearGrid(cart, X, Y, Z, trues(4, 3, 2);\n                                          measure = fill(1.0, 4, 3, 2))) ==\n    FG.Grids.measure(cg3)\n\nNode grids generalize the same way, with the coordinates as a tuple — a run of vectors cannot say how many of them are coordinates, where a curvilinear grid counts them from ndims(mask):\n\nnodes = FG.Grids.UnstructuredGrid(cart, (rand(6), rand(6), rand(6)), ones(6), trues(6))\nFG.Grids.coordinate_names(nodes), FG.Grids.coords(nodes, 4)","title":"Curvilinear corners"},{"category":"section","location":"grids/#Cell-areas-on-the-spherical-node-sets","page":"Grids","text":"Cell areas are an exact closed form, dispatched on the sampling — never a uniform 4πR²/N fallback, which is right only for equal-area samplings and silently wrong elsewhere (icosahedral dual cells span a min/max ratio of 0.52).\n\nlayout / sampling cell areas\nHEALPixGrid uniform 4πR²/N — exact by construction, and stored as one number\nCubedSphereGrid exact solid angle of the cell's own gnomonic rectangle\nYinYangGrid lat–lon patch area, R²Δλ·2sin(Δφ/2)·cosφ\nRingGrid R²·(2π/nlonᵣ)·wᵣ from the ring's Gaussian weight\nIcosahedralGrid the spherical Voronoi dual, from the vertex's own incident triangles\narbitrary points Voronoi areas, via the Quickhull or DelaunayTriangulation extension\n\n(Image: Cell area relative to the mean)\n\nOnly HEALPix is one flat colour. The dark spots on the icosahedral panel are its twelve pentagons — the smallest cells on the mesh, and the reason a uniform default is off by nearly a factor of two between the largest and smallest cell there.\n\nEvery closed form here sums to 4πR² to machine precision — with the documented Yin–Yang excess — and needs no optional dependency. On the node sets, pass areas = … to supply your own.\n\n(Image: Yin–Yang overlap)\n\nnote: Yin–Yang panels overlap\nThe two panels overlap by construction, so their cell areas sum to 3√2πR² — 6.07% more than the sphere, at every resolution. The excess is the overlap itself, and it does not shrink with the mesh. Integrating over both panels needs a partition-of-unity weight for the shared region, which is a modelling choice made on top of these areas.","title":"Cell areas on the spherical node sets"},{"category":"section","location":"api/axes/#Axes","page":"Axes","text":"","title":"Axes"},{"category":"type","location":"api/axes/#FlowGeometries.Axes.AbstractAnalyticAxis","page":"Axes","text":"AbstractAnalyticAxis{T} <: AbstractVector{T}\n\nAn axis whose coordinate is a formula of its index, and whose formula inverts in closed form.\n\nThe third axis kind. An AbstractUniformAxis carries a constant spacing in its type; a plain Vector has nothing but its samples. A stretched vertical coordinate is neither: the spacing genuinely varies, so it is not uniform, but the samples are n evaluations of two or three parameters, and storing them stores a formula's output.\n\nA subtype implements three methods:\n\nBase.length(a)\ncoordinate(a, ξ) — the coordinate at continuous index ξ, agreeing with a[i] at every integer i\nindex_at(a, x) — its inverse, the continuous index whose coordinate is x\n\nand gets indexing, the O(1) endpoint reductions, and — through the inverse — locate, nearest_index and the interpolation weights without a search. Locating a coordinate on a stretched Vector bisects in O(log n); here the inverse names the cell directly and one comparison against the neighbouring face settles the rounding.\n\nThe coordinate must be strictly monotone in ξ, which makes the inverse single-valued. A constructor checks it once. Where index_at cannot answer — a coordinate outside the formula's domain, which a far-extrapolated face can be — it returns a non-finite value and the caller falls back to the search, reaching the same answer.\n\n\n\n\n\n","title":"FlowGeometries.Axes.AbstractAnalyticAxis"},{"category":"type","location":"api/axes/#FlowGeometries.Axes.AbstractUniformAxis","page":"Axes","text":"AbstractUniformAxis{T} <: AbstractRange{T}\n\nSupertype for axes whose spacing is constant and known from their type. Default: UniformAxis.\n\nTo add one, implement the three methods AbstractRange already requires — Base.first, Base.step and Base.length — and everything else here follows: indexing, the O(1) reductions, slicing, reversal, and the affine arithmetic. None of the generic methods touch a field, so a subtype may store whatever it likes under whatever names.\n\nImplement similar_axis as well to have derived axes keep the subtype; the fallback returns a plain UniformAxis.\n\n\n\n\n\n","title":"FlowGeometries.Axes.AbstractUniformAxis"},{"category":"type","location":"api/axes/#FlowGeometries.Axes.ConstantVector","page":"Axes","text":"ConstantVector(value, n)\n\nn copies of value, stored as that value and a length. A uniform axis's per-cell width is one.\n\nA genuine AbstractVector: indexing, iteration, broadcasting and collect behave as for fill(value, n). getindex folds to a constant and every reduction below is closed-form.\n\n\n\n\n\n","title":"FlowGeometries.Axes.ConstantVector"},{"category":"type","location":"api/axes/#FlowGeometries.Axes.GeometricAxis","page":"Axes","text":"GeometricAxis(origin, Δ, ratio, n)\nGeometricAxis{T}(origin, Δ, ratio, n)\n\nn samples whose successive gaps are Δ, Δ·r, Δ·r², … — the stretched grid a boundary layer or a model's vertical levels are built on — held as four numbers.\n\nx(ξ) = origin + Δ·(r^(ξ-1) − 1)/(r − 1)\n\nso x(1) = origin and x(i+1) − x(i) = Δ·r^(i-1). The inverse is a logarithm, so locating a coordinate is O(1).\n\nr > 0 and r ≠ 1. At r == 1 the gaps are constant, which is a UniformAxis; that type carries the spacing where this one hides it in a parameter.\n\n\n\n\n\n","title":"FlowGeometries.Axes.GeometricAxis"},{"category":"type","location":"api/axes/#FlowGeometries.Axes.NonuniformSpacing","page":"Axes","text":"See UniformSpacing.\n\n\n\n\n\n","title":"FlowGeometries.Axes.NonuniformSpacing"},{"category":"type","location":"api/axes/#FlowGeometries.Axes.PowerAxis","page":"Axes","text":"PowerAxis(origin, extent, exponent, n)\nPowerAxis{T}(origin, extent, exponent, n)\n\nn samples spanning origin to origin + extent with the index mapped through a power:\n\nx(ξ) = origin + extent·((ξ-1)/(n-1))^p\n\np == 1 is uniform, p > 1 clusters samples near origin, and p < 1 clusters them near the far end — the shape ocean depth levels and a stretched radial coordinate are usually given. The inverse is a root, so locating a coordinate on it is O(1).\n\nn ≥ 2, p > 0, and a nonzero extent.\n\n\n\n\n\n","title":"FlowGeometries.Axes.PowerAxis"},{"category":"type","location":"api/axes/#FlowGeometries.Axes.UniformAxis","page":"Axes","text":"UniformAxis(origin, Δ, n)\nUniformAxis{T}(origin, Δ, n)\n\nn samples at origin + (i-1)·Δ, stored as those three numbers.\n\nPreferred over range/StepRangeLen for a grid axis:\n\nrange(0f0; step = 0.1f0, length = n) is a StepRangeLen{Float32, Float64, Float64, Int} — Float32 elements over a Float64 offset and step. UniformAxis{T} computes in T throughout.\nStepRangeLen indexes through TwicePrecision arithmetic, buying an exactness a grid axis does not need; UniformAxis indexes with one multiply and one add.\nisbits, so moving an axis to another storage backend is free.\n\nΔ may be negative, for a descending axis. n must be non-negative.\n\nUnlike LinRange this does not pin last to a prescribed endpoint: on a grid the spacing is the primary datum.\n\nAn AbstractRange: that gets Base's searchsorted in closed form, so a lookup is O(1) in the axis length, and isa AbstractRange dispatch from other packages.\n\n\n\n\n\n","title":"FlowGeometries.Axes.UniformAxis"},{"category":"type","location":"api/axes/#FlowGeometries.Axes.UniformSpacing","page":"Axes","text":"UniformSpacing()\nNonuniformSpacing()\n\nWhether an axis's spacing is known from its type. spacing_trait returns one of these, so a method can dispatch on it.\n\n\n\n\n\n","title":"FlowGeometries.Axes.UniformSpacing"},{"category":"function","location":"api/axes/#FlowGeometries.Axes.coordinate","page":"Axes","text":"coordinate(a, ξ) -> T\n\nThe coordinate of AbstractAnalyticAxis a at continuous index ξ, equal to a[i] at every integer index. A half-integer ξ is the midpoint in index space; this package's faces are midpoints in coordinate space, so the two coincide only on a uniform axis. What is extended here is the formula.\n\n\n\n\n\n","title":"FlowGeometries.Axes.coordinate"},{"category":"function","location":"api/axes/#FlowGeometries.Axes.index_at","page":"Axes","text":"index_at(a, x) -> T\n\nThe continuous index at which AbstractAnalyticAxis a has coordinate x: the inverse of coordinate, and the reason a query on such an axis needs no search.\n\nReturns a non-finite value where x lies outside the formula's domain, which callers take as \"no closed-form answer\" and fall back to bisection.\n\n\n\n\n\n","title":"FlowGeometries.Axes.index_at"},{"category":"method","location":"api/axes/#FlowGeometries.Axes.isuniform-Tuple{Any}","page":"Axes","text":"isuniform(x) -> Bool\n\nWhether x has UniformSpacing. Const-folds, so it is free to branch on in a hot loop.\n\n\n\n\n\n","title":"FlowGeometries.Axes.isuniform"},{"category":"method","location":"api/axes/#FlowGeometries.Axes.similar_axis-Tuple{FlowGeometries.Axes.AbstractUniformAxis, Any, Any, Integer}","page":"Axes","text":"similar_axis(a, origin, Δ, n) -> AbstractUniformAxis\n\nThe axis of a's own kind with the given origin, spacing and length: the hook every derived axis goes through — a slice, a reversal, 2a, a .+ c.\n\nDefaults to a UniformAxis, so a subtype that does not define it still gets correct results, just not its own type back.\n\n\n\n\n\n","title":"FlowGeometries.Axes.similar_axis"},{"category":"method","location":"api/axes/#FlowGeometries.Axes.spacing-Tuple{AbstractRange}","page":"Axes","text":"spacing(x) -> Number\n\nThe constant spacing of a uniform axis, read from its type. Signed, so a descending axis reports a negative spacing. Raises for an axis that is not isuniform.\n\n\n\n\n\n","title":"FlowGeometries.Axes.spacing"},{"category":"method","location":"api/axes/#FlowGeometries.Axes.spacing_trait-Tuple{AbstractArray}","page":"Axes","text":"spacing_trait(x) -> UniformSpacing() | NonuniformSpacing()\n\nThe axis's spacing trait. UniformAxis and any AbstractRange carry a constant step in their type; every other array is nonuniform.\n\nThe question is answered by the type: a Vector holding an arithmetic sequence is NonuniformSpacing(). No code path here inspects values to decide it.\n\n\n\n\n\n","title":"FlowGeometries.Axes.spacing_trait"},{"category":"method","location":"api/axes/#FlowGeometries.Axes.uniform_axis-Tuple{Any}","page":"Axes","text":"uniform_axis(x) -> UniformAxis\nuniform_axis(T, x) -> UniformAxis{T}\n\nThe UniformAxis equal to x, for any axis whose spacing is known from its type. A nonuniform vector raises, because it has no uniform form: replacing its coordinates with a fitted sequence is a decision only its owner can make, and they can build the axis directly.\n\n\n\n\n\n","title":"FlowGeometries.Axes.uniform_axis"},{"category":"method","location":"api/axes/#FlowGeometries.Axes.wrap_sign-Union{Tuple{AbstractVector{T}}, Tuple{T}} where T<:AbstractFloat","page":"Axes","text":"wrap_sign(x) -> ±1\n\n+1 for an ascending axis and -1 for a descending one: the sign that turns a period magnitude into the wrapped neighbour's offset in index order. A descending axis is routine in stored data, and its wrapped neighbour lies at x[1] - period, not x[1] + period.\n\n\n\n\n\n","title":"FlowGeometries.Axes.wrap_sign"},{"category":"section","location":"api/grids/#Grids","page":"Grids","text":"","title":"Grids"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AbstractAdjacency","page":"Grids","text":"AbstractAdjacency\n\nWhere a cell's neighbours come from: IndexStencilNeighbors, FormulaNeighbors or StoredMeshNeighbors. Read it with adjacency_source.\n\nDistinct from AbstractCandidateSource: adjacency is the mesh's own neighbour relation, and a candidate source answers a question about distance, which no adjacency determines.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AbstractAdjacency"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AbstractCandidateSource","page":"Grids","text":"AbstractCandidateSource\n\nHow a distance query enumerates the cells it must test: SeparableWindow or IndexedCandidates. Read it with candidate_source.\n\nMembership is decided by the caller's exact distance ≤ r gate, so each need only return a superset of the ball, and the two enumerations agree on the result.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AbstractCandidateSource"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AbstractCellAddress","page":"Grids","text":"AbstractCellAddress\n\nHow a cell of a grid is named: CartesianCells or FlatCells. Read it with cell_address.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AbstractCellAddress"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AbstractCurvilinearGrid","page":"Grids","text":"AbstractCurvilinearGrid{G,T} <: AbstractGrid{G,T}\n\nCurvilinear grids.  Default: CurvilinearGrid.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AbstractCurvilinearGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AbstractEmbedding","page":"Grids","text":"AbstractEmbedding\n\nHow a grid's cell centres sit in the Euclidean space an index searches, and therefore what a physical radius means there. A type, as stencils are: the conversion is applied once per query, and being resolved at compile time it leaves no branch and no boxed radius in the hot path.\n\nCartesianEmbedding, ChordEmbedding, ArcEmbedding.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AbstractEmbedding"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AbstractGrid","page":"Grids","text":"AbstractGrid{G<:AbstractGeometry, T<:AbstractFloat}\n\nSupertype for all grid architectures.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AbstractGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AbstractStructuredGrid","page":"Grids","text":"AbstractStructuredGrid{G, T} <: AbstractGrid{G,T}\n\nRectilinear grids.  Default: StructuredGrid.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AbstractStructuredGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AbstractTopology","page":"Grids","text":"AbstractTopology\n\nHow a coordinate direction closes: Periodic or Bounded.\n\nCarried in the grid's type, so isperiodic is a compile-time answer and callers can dispatch on it. The cell measure depends on it: a wrapped boundary cell has a width that a bounded one lacks.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AbstractTopology"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AbstractUnstructuredGrid","page":"Grids","text":"AbstractUnstructuredGrid{G,T} <: AbstractGrid{G,T}\n\nUnstructured / node grids.  Default: UnstructuredGrid.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AbstractUnstructuredGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AllActive","page":"Grids","text":"AllActive(size)\n\nThe mask of a grid where every cell participates, stored as its size alone. getindex is a constant the compiler can fold, and count is length without a scan.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AllActive"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.ArcEmbedding","page":"Grids","text":"ArcEmbedding(R)\n\nPoints on the reference sphere of radius R, where the metric is the great-circle arc. A radius has to be converted to the chord it subtends.\n\n\n\n\n\n","title":"FlowGeometries.Grids.ArcEmbedding"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.AxisSummary","page":"Grids","text":"AxisSummary{T}\n\nOne direction's reductions: the values origin, bounds, extent, minimum_spacing and maximum_spacing report.\n\nA stretched axis answers each of these by scanning, and a distance query reads them through Connectivity.MetricTopology, which is the default topology on every per-cell entry point. Holding them keeps that construction O(1) and keeps a query off the coordinates.\n\nisbits, five numbers per direction, filled once by _axis_summary: one pass per axis, the O(∑ Nᵈ) the measure factors already cost. first is kept apart from lo because a descending axis starts at its largest value.\n\n\n\n\n\n","title":"FlowGeometries.Grids.AxisSummary"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.Bounded","page":"Grids","text":"Bounded()\n\nA direction that ends: its first and last cells have one neighbour each.\n\n\n\n\n\n","title":"FlowGeometries.Grids.Bounded"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.CartesianCells","page":"Grids","text":"CartesianCells()\n\nA cell is an NTuple{N,Int} into an N-dimensional array of cells, so a traversal walks CartesianIndices and converts to a linear index to report. Rectilinear, curvilinear and every panel layout.\n\n\n\n\n\n","title":"FlowGeometries.Grids.CartesianCells"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.CartesianEmbedding","page":"Grids","text":"CartesianEmbedding()\n\nThe coordinates themselves, replicated at the periodic images. A radius passes through unchanged.\n\n\n\n\n\n","title":"FlowGeometries.Grids.CartesianEmbedding"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.CellListIndex","page":"Grids","text":"CellListIndex\n\nA uniform-bin spatial index over the embedded cell centres: points are bucketed by which bin of side h they fall in, and a query visits the bins its ball can reach.\n\nThree properties let it run on a device, where a tree cannot:\n\nit is arrays only — bin offsets, point ids and the bin each sits in — so Adapt moves it like any other field, and it holds no copy of the coordinates, which stay on the grid;\nevery point lands in exactly one bin, periodicity wrapping the bin coordinate and leaving the point where it is, so a query emits each cell once and needs no candidate buffer to deduplicate into. fold_candidates is therefore a fold, with no list to materialize;\nbins are hashed into O(n) buckets, so the memory is independent of h. A dense lattice over a sphere binned at 100 km is (2R/h)³ ≈ 2×10⁶ mostly empty cells, and grows as h shrinks.\n\nBuild it for the radius you intend to query at: h is that radius, so a query touches 3ᴰ bins. A much larger radius still works and costs (2⌈r/h⌉+1)ᴰ bins.\n\n\n\n\n\n","title":"FlowGeometries.Grids.CellListIndex"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.CellMesh","page":"Grids","text":"CellMesh(cell_ptr, cell_nodes, node_ptr, node_cells)\nCellMesh(cell_ptr, cell_nodes, nnodes)\n\nA node set's cells, and the nodes each one joins — both directions, each as CSR.\n\nNode→node adjacency says which nodes are linked; it does not say which nodes bound a face. The cells do, and an area, a flux through an edge, or an interpolation inside a triangle is defined on them. A triangulation produces them, and the tessellation that computes a node set's Voronoi areas builds one, which is the mesh held here.\n\ncell_nodes[cell_ptr[c] : cell_ptr[c+1]-1] are the nodes of cell c, in order around it\nnode_cells[node_ptr[i] : node_ptr[i+1]-1] are the cells incident on node i\n\nBoth are O(n): a triangulation of n nodes has about 2n triangles and 6n entries either way. The second is the transpose of the first and the two-argument form builds it.\n\nThe cells are a triangulation or the caller's own. A k-nearest graph is neither planar nor symmetric once truncated, and its cells tile nothing, so it cannot serve here.\n\n\n\n\n\n","title":"FlowGeometries.Grids.CellMesh"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.ChordEmbedding","page":"Grids","text":"ChordEmbedding()\n\nThe embedded distance already is the metric, or a lower bound on it: (λ, φ, r) on a sphere, where the metric is the 3-D chord, and geodetic coordinates on a spheroid, where the ECEF chord is at most the geodesic. A radius passes through unchanged; under-approximating means the query over-returns, and an index is allowed to over-return.\n\n\n\n\n\n","title":"FlowGeometries.Grids.ChordEmbedding"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.CubedSphereGrid","page":"Grids","text":"CubedSphereGrid(geometry, n; mask = nothing)\n\nThe gnomonic cubed sphere as a grid: six n × n panels of equiangular cells, 6n² in all.\n\nIt stores n, the geometry and the mask. A cell's coordinates are its panel-local angles through the gnomonic map, its neighbours are the panel-interior offsets and the exact seam fold, and its area is the closed-form solid angle of its gnomonic rectangle — so all three are arithmetic in (n, cell), and Base.summarysize is the same at every n.\n\nCells are numbered panel by panel with i fastest, matching SphericalSampling.cubed_sphere_points. panel_cell and cell_id convert between a cell id and its (panel, i, j).\n\nCoordinates are (λ, φ): coords(grid, k) reads one cell, and materialize gives the whole cloud as dense vectors.\n\n\n\n\n\n","title":"FlowGeometries.Grids.CubedSphereGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.CurvilinearGrid","page":"Grids","text":"CurvilinearGrid{T, G, N, TP, C, KC, MA, B}\n\nCurvilinear grid whose cell-center coordinates are N-dimensional arrays (e.g. an orthogonal curvilinear mesh). coordinates holds one N-D cell-center array per direction.\n\nAt N = 2 the cell measure is the exact quadrilateral area through the cell's four vertices. Those vertex arrays are what corners holds, one per direction and one larger in every direction. They are retained when the caller supplies them or asks for them with keep_corners = true, and are otherwise construction input to the area kernel alone — so corners is nothing on a grid built from centres, and has_corners reports which. In any dimension other than 2 the measure is the caller's to supply: the corner-area kernel is a 2-D algorithm with no N-D generalization here.\n\nType parameters\n\nT: coordinate float type. G<:AbstractGeometry{T} is tied to it, so a mismatched-eltype geometry raises a type error. T therefore precedes G: Julia binds type parameters left to right, and G<:AbstractGeometry{T} requires T already bound.\nN: number of coordinate directions.\nC: tuple type of the center coordinate arrays.\nKC: tuple type of the cell-vertex arrays, or Nothing where they were not retained.\nMA: array type of the derived measure field — independent of C, since it is a computed field with no reason to match the coordinate arrays' storage type.\nB: array type of the active mask.\n\n\n\n\n\n","title":"FlowGeometries.Grids.CurvilinearGrid"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.CurvilinearGrid-Tuple{FlowGeometries.Geometry.AbstractGeometry, Vararg{Any}}","page":"Grids","text":"CurvilinearGrid(geometry, coords..., mask; measure=nothing, corners=nothing, …)\nCurvilinearGrid(geometry, coords..., measure, mask; corners=nothing, …)\n\nBuild a curvilinear grid in any number of directions from one N-D cell-center coordinate array per direction. mask is the trailing array; a measure array before it is used verbatim (common when a dataset ships its own cell areas), and may equally be given as the measure keyword.\n\nWith no measure supplied, one is computed from the cell-vertex arrays — at N = 2 only, as the exact quadrilateral cell area. Spherical cells use the exact spherical-quadrilateral area, the spherical excess of the two triangles through the cell's four corner directions (see Geometry.spherical_excess); Cartesian cells the exact planar shoelace area. That kernel is a 2-D algorithm with no N-D generalization here, so in any other dimension the measure must be given.\n\nPass corners (a tuple of arrays, each one larger than the centers in every direction) for exact cell vertices, e.g. from the source mesh's own vertex grid; otherwise they are reconstructed from the centers per direction (see _centers_to_corners), which requires at least 2 cells across.\n\nCorners supplied this way are kept on the grid, and reconstructed ones are input to the area kernel — pass keep_corners = true to keep those too, at one array per direction. Reconstruction happens only where something needs it, so a grid given its own measure derives none.\n\nperiodic is a Bool (applied to direction 1) or an NTuple{N,Bool}. When omitted, direction-1 periodicity is auto-detected the same way as StructuredGrid (full-circle spherical longitude), and every other direction is bounded.\n\n\n\n\n\n","title":"FlowGeometries.Grids.CurvilinearGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.FlatCells","page":"Grids","text":"FlatCells()\n\nA cell is one integer into a flat list, so the index a traversal walks and the index it reports are the same number. Node sets, and the pixelizations whose cells are enumerated by a single id.\n\n\n\n\n\n","title":"FlowGeometries.Grids.FlatCells"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.FormulaNeighbors","page":"Grids","text":"FormulaNeighbors()\n\nNeighbours are closed-form arithmetic on the cell id, with no graph and no coordinates stored: a pixelization's face tables, a ring grid's in-ring and adjacent-ring maps, a panel seam.\n\nA layout with this trait supplies formula_neighbors.\n\n\n\n\n\n","title":"FlowGeometries.Grids.FormulaNeighbors"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.GridMeasure","page":"Grids","text":"GridMeasure(grid)\n\nThe cell measure of a layout whose measure is a formula, computed per entry and stored nowhere.\n\nA genuine AbstractVector: indexing, broadcasting and collect behave as for the dense equivalent, and measure(grid, k) is what an entry is. sum goes through _total_measure, which a layout covering a known region answers in O(1).\n\n\n\n\n\n","title":"FlowGeometries.Grids.GridMeasure"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.HEALPixGrid","page":"Grids","text":"HEALPixGrid(geometry, nside; scheme = SphericalSampling.Ring(), mask = nothing)\n\nThe HEALPix pixelization as a grid: 12·nside² equal-area pixels on 4·nside − 1 iso-latitude rings.\n\nIt stores nside, the scheme, the geometry and the mask. A pixel's coordinates, its neighbours and its area are each closed-form in (nside, pixel), so those four fields are the whole grid: Base.summarysize is the same at nside = 1024 — 12.6 million pixels — as at nside = 1.\n\nnlon varies by ring, so this is a layout of its own, and it answers the three traits a neighbourhood algorithm asks of one: cells are FlatCells (a pixel is one id), adjacency is FormulaNeighbors (the face tables of Górski et al.), and a distance query enumerates through IndexedCandidates.\n\nCoordinates are (λ, φ): coords(grid, i) reads one pixel, and materialize gives the whole cloud as dense vectors.\n\n\n\n\n\n","title":"FlowGeometries.Grids.HEALPixGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.IcosahedralGrid","page":"Grids","text":"IcosahedralGrid(geometry, frequency; mask = nothing)\n\nThe frequency-ν icosahedral geodesic as a grid: 10ν² + 2 vertices, twelve of them pentagonal and the rest hexagonal.\n\nIt stores ν, the geometry and the mask. Vertex ids are assigned by topology — the twelve corners, then each macro-edge's interior, then each face's — so a vertex's position, its neighbours and its dual-cell area are each arithmetic in (ν, id): reading the numbering backwards gives the lattice positions the vertex occupies, and everything follows from those.\n\nA vertex sits on one face if a face owns it, two if a macro-edge does, and five if it is a corner. That count is its valence, so the twelve corners are the grid's twelve pentagons.\n\nThe measure is the spherical Voronoi dual, computed from the vertex's own incident triangles — each triangle split among its three vertices by the arcs from its circumcenter to its edge midpoints. It costs a fan of spherical excesses per cell, so use measure_array when sweeping every cell.\n\nCoordinates are (λ, φ): coords(grid, id) reads one vertex, and materialize gives the whole cloud as dense vectors.\n\n\n\n\n\n","title":"FlowGeometries.Grids.IcosahedralGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.IndexStencilNeighbors","page":"Grids","text":"IndexStencilNeighbors()\n\nNeighbours are offsets in the cell index space, wrapped or clipped per direction — the whole of what Connectivity.IndexTopology carries. Coordinates never enter.\n\n\n\n\n\n","title":"FlowGeometries.Grids.IndexStencilNeighbors"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.IndexedCandidates","page":"Grids","text":"IndexedCandidates()\n\nThe grid has no separable axes to bound with, so candidates come from a spatial index over its cell centres.\n\n\n\n\n\n","title":"FlowGeometries.Grids.IndexedCandidates"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.Periodic","page":"Grids","text":"Periodic()\n\nA direction that wraps: the last cell's neighbour is the first, one period away.\n\n\n\n\n\n","title":"FlowGeometries.Grids.Periodic"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.RingGrid","page":"Grids","text":"RingGrid(geometry, sampling::AbstractReducedGaussianSampling; mask = nothing)\nRingGrid(geometry, latitudes, nlon_per_ring, ring_area; mask = nothing)\n\nIso-latitude rings whose longitude count varies by ring: the reduced Gaussian family, of which the octahedral grid is the operational case.\n\nStorage is O(nrings) = O(√npoints) — one latitude, one longitude count, one offset and one cell area per ring. A point's longitude is (j−1)·2π/nlon[r], and its latitude and area are its ring's, so all three come from the ring's four numbers. At N = 1280 that is four vectors of 2560 entries, covering 6.6 million points.\n\nCells are numbered ring by ring from the north, matching SphericalSampling.ring_range and the order spherical_points writes.\n\nCoordinates are (λ, φ): coords(grid, i) reads one point, and materialize gives the whole cloud as dense vectors.\n\n\n\n\n\n","title":"FlowGeometries.Grids.RingGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.RingwiseVector","page":"Grids","text":"RingwiseVector(value, offset)\n\nThe cell measure of a ring grid, held as its per-ring values.\n\nEvery cell of an iso-latitude ring has the same area, so npoints entries carry only nrings numbers — O(√npoints). A genuine AbstractVector: indexing, broadcasting and collect behave as for the dense equivalent.\n\nsum is specialized to Σᵣ nlonᵣ·valueᵣ, O(nrings) against the dense O(npoints).\n\n\n\n\n\n","title":"FlowGeometries.Grids.RingwiseVector"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.RotatedGrid","page":"Grids","text":"RotatedGrid(base, rotation, map)\n\nA rectilinear grid whose cells are reported in a rotated frame: the mesh is base's, and a cell's position is map(rotation, λ, φ) evaluated where it is asked for.\n\nrotate and unrotate build one. map is which direction the frame goes — Geometry.rotate for a mesh given in geographic coordinates and reported in the rotated frame, Geometry.unrotate for the reverse — and it is a singleton function type, so the choice resolves at compile time and the grid stays isbits apart from what base already holds.\n\nNothing is materialized. A rotation is an isometry of the sphere, so the cell measure carries over exactly and this shares base's array. The mesh, the mask, the index topology and the wrap lengths are base's too, all being properties of the cell lattice. What differs is where each cell sits, which costs two sincos pairs per coordinate access.\n\n\n\n\n\n","title":"FlowGeometries.Grids.RotatedGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.SeparableMeasure","page":"Grids","text":"SeparableMeasure(factors)\n\nThe cell measure of a rectilinear grid, held as its per-axis factors.\n\nEvery measure this package supports on such a grid is a product of one factor per axis (see _measure_factors), so the ∏ Nᵈ entries carry only ∑ Nᵈ numbers. It is a genuine AbstractArray: indexing, broadcasting and collect behave as for the dense equivalent.\n\nsum is specialized to ∏ᵈ ∑ᵢ wᵈᵢ, O(∑ Nᵈ) against the dense O(∏ Nᵈ).\n\n\n\n\n\n","title":"FlowGeometries.Grids.SeparableMeasure"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.SeparableWindow","page":"Grids","text":"SeparableWindow()\n\nThe grid has separable axes, so a bounding index window per direction contains every cell within a given distance and is O(1) to compute — see Connectivity.metric_window. No index is needed and nothing is buffered.\n\n\n\n\n\n","title":"FlowGeometries.Grids.SeparableWindow"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.SlabMeasure","page":"Grids","text":"SlabMeasure(lead, slab, rest)\n\nThe cell measure of a rectilinear grid with a single coupled pair of directions: measure[i, j, k, l…] == lead[i] · slab[j, k] · rest[1][l] · ….\n\nA geodetic (λ, φ, h) volume element is (N(φ)+h)·cosφ·(M(φ)+h)·Δλ·Δφ·Δh. The height offsets both curvature radii, so no product of per-axis factors reproduces it and SeparableMeasure cannot hold it. Longitude enters none of that coupling, so only (φ, h) are stored together, giving Nφ·Nh + Nλ + … numbers against the dense ∏ Nᵈ.\n\nsum and extrema are specialized on the same argument as the separable case: the whole is a product of independent groups, so its total is the product of their totals and its extremes are attained with every group at one of its own.\n\n\n\n\n\n","title":"FlowGeometries.Grids.SlabMeasure"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.StaggeredGrid","page":"Grids","text":"StaggeredGrid(geometry, axes...; mask = nothing, topology = nothing, period = nothing)\nStaggeredGrid(center::StructuredGrid)\n\nOne rectilinear mesh, readable at any staggered location: a family of grids.\n\nA location is one FlowGeometries.Discretization.AbstractLocation per direction — Center() where the samples are the cell centres, Face() where they are the cell boundaries. On an Arakawa C arrangement the tracer sits at all-centres, the velocity component along direction d at Face() in d and Center() elsewhere, and the vorticity at Face() in the two directions it circulates about. grid_at returns the ordinary StructuredGrid at any of them, so every operator that already works on a rectilinear grid works at every location without knowing this type exists.\n\nThe face axes are built once, at construction. A face axis derived from a uniform primal axis is itself an Axes.UniformAxis — see FlowGeometries.Discretization.faces — so a uniform mesh stays uniform to every method that dispatches on spacing.\n\nA periodic direction of N cells has N faces, its last face being its first; a bounded direction has N+1.\n\nA mask is given over the centre cells, and a staggered point is active where every centre it is built from is. This is the finite-volume rule: a velocity face between an active and an inactive cell is a boundary, on the same footing as the least-squares gradient's unresolved direction and a stencil's masked cell.\n\n\n\n\n\n","title":"FlowGeometries.Grids.StaggeredGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.StoredMeshNeighbors","page":"Grids","text":"StoredMeshNeighbors()\n\nNeighbours are read from stored incidence, because the mesh is genuinely arbitrary and no formula describes it.\n\n\n\n\n\n","title":"FlowGeometries.Grids.StoredMeshNeighbors"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.StructuredGrid","page":"Grids","text":"StructuredGrid{T, G, N, S, TP, C, AT, BT}\n\nRectilinear N-dimensional grid, for any N: one coordinate vector per direction (coordinates), an N-D cell measure (length in 1-D, area in 2-D, volume in 3-D, the N-D measure in general), an N-D active mask, per-direction topology, and the wrap period of each periodic direction.\n\nType parameters\n\nT: coordinate float type. G<:AbstractGeometry{T} is tied to it, so a mismatched-eltype geometry raises a type error. T therefore precedes G: Julia binds type parameters left to right, and G<:AbstractGeometry{T} requires T already bound. CurvilinearGrid and UnstructuredGrid carry the same convention.\nN: number of coordinate directions.\nS: the SphericalSampling recipe the axes came from, or Nothing for axes given directly. A zero-size singleton, so it costs nothing to carry and lets quadrature exactness, the matching latitude weights and a rebuild at another resolution dispatch on which node set this is — none of which the coordinates alone determine. See sampling.\nTP: the per-direction AbstractTopology — singletons, so no storage, readable from the type.\nC: a heterogeneous NTuple{N,AbstractVector{T}}. Each axis independently keeps whatever concrete AbstractVector{T} type it was constructed with — an Axes.UniformAxis, a plain Vector, a device array — so one axis can be uniform while another is stretched. A UniformAxis's type is a compile-time proof of constant spacing that spacing_trait and spacing read without touching a coordinate, and a common vector type across the axes erases it.\nAT, BT: array types of the derived measure and of the active mask.\n\n\n\n\n\n","title":"FlowGeometries.Grids.StructuredGrid"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.StructuredGrid-Union{Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractGeometry{T}, Vararg{Any}}} where T<:AbstractFloat","page":"Grids","text":"StructuredGrid(geometry, axes...; mask = nothing, topology = nothing, period = nothing)\nStructuredGrid(geometry, axes..., mask; topology = nothing, period = nothing)\n\nBuild a rectilinear grid in any number of dimensions from one coordinate vector per direction, pre-computing the separable cell measure from the geometry.\n\nEach axis may independently be uniform or stretched, and keeps whichever it is in its own type — see isuniform. Axes are adapted to the geometry's float type T by _to_axis, which preserves uniformity and keeps a device-resident axis on its device.\n\nFor a SphericalGeometry the directions are (λ, φ, r, …): longitude, geographic latitude, and — in 3-D and above — the absolute radius from the origin. (A Geometry.SpheroidGeometry's third direction is a height above the surface instead.) Measures are the metric elements R·Δλ, R²cosφ·Δλ·Δφ and r²cosφ·Δλ·Δφ·Δr, with further directions entering as plain widths. A CartesianGeometry measure is the product of the per-direction widths.\n\nKeywords\n\nmask: an N-D Bool array of active cells. Omit it (or pass nothing) for an all-active grid, which stores only its size — see AllActive. It may also be given positionally, after the axes.\ntopology: per-direction closure. Accepts Periodic/Bounded instances, a tuple of them, a Bool, or a tuple of Bools; a single value or a short tuple applies to the leading directions and the rest are Bounded. When omitted, direction 1 is auto-detected — on a spherical grid a longitude axis spanning the full circle is Periodic and a regional span is not, in either storage order — and every other direction is Bounded.\nperiod: the wrap length of each periodic direction. Omit it and the axis's own closure is used: 2π for spherical longitude, and extent + one spacing for a Cartesian direction, which is exact for a uniform axis (n·|Δ|). A nonuniform periodic Cartesian direction has no closure to infer, its seam gap being undetermined by its samples, so period is required there.\n\n\n\n\n\n","title":"FlowGeometries.Grids.StructuredGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.UnstructuredGrid","page":"Grids","text":"UnstructuredGrid(geometry, x, y, mask; k=6, radius=nothing, areas=nothing)\n\nBuild an UnstructuredGrid whose adjacency comes from a k-d-tree nearest-neighbour search over the nodes: either the k nearest per node (default k = 6), or every neighbour within a physical radius. The two are mutually exclusive. Requires NearestNeighbors.\n\nFor SphericalGeometry the tree is built on the 3-D Cartesian embedding of the nodes, where nearest by chord distance and nearest by great-circle distance give the same ordering.\n\nareas supplies per-node cell areas, as a dataset that ships its own does; nothing derives exact Voronoi-cell areas from a Delaunay or convex-hull tessellation (DelaunayTriangulation.jl for Cartesian, Quickhull.jl for spherical — see _voronoi_areas).\n\nperiodic/period declare a wrapping domain, and the neighbour search honours it: a node near one face finds the nodes across the opposite face. Spherical longitude wraps by default; a Cartesian box is opt-in and needs its period.\n\n\n\n\n\n","title":"FlowGeometries.Grids.UnstructuredGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.UnstructuredGrid-2","page":"Grids","text":"UnstructuredGrid{T, G, V, VA, B, VI}\n\nUnstructured mesh (e.g. radial data, finite volume, or triangular mesh) where coords are 1D vectors.\n\nType parameters\n\nT: coordinate float type. G<:AbstractGeometry{T} is tied to it, so a mismatched-eltype geometry raises a type error. T therefore precedes G: Julia binds type parameters left to right, and G<:AbstractGeometry{T} requires T already bound. CurvilinearGrid follows the same convention.\nC: tuple type of the per-direction node-coordinate vectors.\nVA: vector type of the derived measure field, independent of C, a Voronoi tessellation having no reason to match the coordinate vectors' storage type.\nB: mask storage type.\nVN/VP: CSR neighbour-list and offset storage types, independent of each other. Their element type is a free Integer, so a large mesh carries Int32 indices — half the memory and bandwidth of Int64, and the width a GPU kernel wants — through this same type.\n\nNeighbour adjacency is CSR: a flat neighbor_nbrs with neighbor_ptr offsets, node t owning neighbor_ptr[t]:neighbor_ptr[t+1]-1. The adjacency is immutable after construction, so the whole graph is two contiguous allocations and a traversal reads them in order.\n\n\n\n\n\n","title":"FlowGeometries.Grids.UnstructuredGrid"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.UnstructuredGrid-Union{Tuple{G}, Tuple{T}, Tuple{N}, Tuple{G, NTuple{N, AbstractVector}, AbstractVector}, Tuple{G, NTuple{N, AbstractVector}, AbstractVector, AbstractVector{Bool}}, Tuple{G, NTuple{N, AbstractVector}, AbstractVector, AbstractVector{Bool}, Any}, Tuple{G, NTuple{N, AbstractVector}, AbstractVector, AbstractVector{Bool}, Any, Any}} where {N, T<:AbstractFloat, G<:FlowGeometries.Geometry.AbstractGeometry{T}}","page":"Grids","text":"UnstructuredGrid(geometry, coords::Tuple, measure, mask[, neighbor_nbrs, neighbor_ptr]; periodic, period)\nUnstructuredGrid(geometry, x, y, measure, mask[, neighbor_nbrs, neighbor_ptr]; periodic, period)\n\nBuild a node grid in any number of directions from one coordinate vector per direction and CSR adjacency. Coordinates come as a tuple; the two-direction case may pass x, y positionally.\n\nOmitting the CSR pair gives a grid with no adjacency — every node reports zero neighbours, which serves a scattered-point spectral method that never queries it. A real-space neighbourhood operation needs adjacency: build it (e.g. through the k-d-tree constructor below) and pass it in, or query by distance with Connectivity.neighbors_within, which reads coordinates.\n\nperiodic declares that the enclosing domain wraps in a direction, and period gives the wrap length there. A scattered point set carries no axis to infer this from, so both are explicit — except on a sphere, where longitude wraps at 2π by construction and is the default. See isperiodic and period.\n\n\n\n\n\n","title":"FlowGeometries.Grids.UnstructuredGrid"},{"category":"type","location":"api/grids/#FlowGeometries.Grids.YinYangGrid","page":"Grids","text":"YinYangGrid(geometry, nlon, nlat; mask = nothing)\n\nThe Kageyama–Sato Yin–Yang grid: two nlon × nlat lat–lon panels covering [-3π/4, 3π/4] × [-π/4, π/4] in their own frames, 2·nlon·nlat cells in all.\n\nIt stores nlon, nlat, the geometry and the mask. In its own frame each panel is a separable lat–lon patch, and yang is yin rigidly rotated, so a cell's coordinates are the panel formula composed with SphericalSampling._yin_yang_rotate. A cell's area depends on its latitude row alone.\n\nCells are numbered yin first with i fastest, then yang the same way, matching SphericalSampling.spherical_points(YinYangSampling(), nlon, nlat). panel_cell and cell_id convert between a cell id and its (panel, i, j).\n\nnote: The panels overlap\nThe two panels overlap by construction, so the cell areas sum to 3√2πR² — 6.07% more than the sphere, at every resolution. That excess is the grid's real geometry, and integrating over both panels needs a partition-of-unity weight for the shared region, which is a modelling choice made on top of these areas. The panels are also not cross-linked: a cell's neighbours are its own panel's, which is the standard Yin–Yang discrete topology — the panels couple through interpolation.\n\n\n\n\n\n","title":"FlowGeometries.Grids.YinYangGrid"},{"category":"method","location":"api/grids/#FlowGeometries.Geometry.distance-Tuple{FlowGeometries.Grids.AbstractGrid, Any, Any}","page":"Grids","text":"Geometry.distance(grid, I, J) -> T\nGeometry.distance(grid::UnstructuredGrid, i, j) -> T\n\nDistance between the centres of two cells under the grid's own geometry and topology: the coordinates are resolved from the indices, reduced to the nearest image in every periodic direction, and handed to the point form of Geometry.distance.\n\nAcross a periodic seam this is the short way round: one spacing between the first and last cell of a periodic direction, where the point form on the raw coordinates gives the full extent. A bounded direction contributes its plain coordinate difference. See displacement for the offset it was taken from.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.distance"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._ambient_bound-Union{Tuple{FlowGeometries.Grids.AbstractGrid{G, T}}, Tuple{T}, Tuple{G}} where {G<:FlowGeometries.Geometry.AbstractSphericalGeometry, T}","page":"Grids","text":"_ambient_bound(grid) -> T\n\nThe largest |x|, |y| or |z| any of this grid's cells can embed to.\n\n\n\n\n\n","title":"FlowGeometries.Grids._ambient_bound"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._axis_summary-Union{Tuple{AbstractVector{T}}, Tuple{T}} where T<:AbstractFloat","page":"Grids","text":"_axis_summary(x) -> AxisSummary\n\nReduce axis x in a single pass. A uniform axis answers from first, step and length, reading no element.\n\n\n\n\n\n","title":"FlowGeometries.Grids._axis_summary"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._build_kdtree_neighbors-Union{Tuple{D}, Tuple{FlowGeometries.Geometry.AbstractGeometry, NTuple{D, AbstractVector}}} where D","page":"Grids","text":"_build_kdtree_neighbors(geometry, coords::Tuple; k=6, radius=nothing) -> (nbrs, ptr)\n\nExtension hook: build CSR neighbor adjacency via a k-d tree. Overridden by a consumer NearestNeighbors extension (load using NearestNeighbors). radius, if given, switches to an all-neighbors-within-radius query (mutually exclusive with k); radius is in the grid's physical distance units — Geometry.distance, so meters for SphericalGeometry and the geometry's own units for CartesianGeometry.\n\n\n\n\n\n","title":"FlowGeometries.Grids._build_kdtree_neighbors"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._cell_from_linear-Tuple{FlowGeometries.Grids.AbstractGrid, Integer}","page":"Grids","text":"_cell_from_linear(grid, lin) -> cell\n\nThe cell a linear index names: the inverse of the linear index a traversal reports, and what turns a spatial index's answer back into a cell.\n\n\n\n\n\n","title":"FlowGeometries.Grids._cell_from_linear"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._cell_indices-Tuple{FlowGeometries.Grids.AbstractGrid, Any}","page":"Grids","text":"_cell_indices(grid, cell) -> Tuple{Vararg{Int}}\n\nA cell as the index tuple every mask and coordinate accessor here takes: the tuple itself where cells are CartesianCells, and (i,) where they are FlatCells.\n\nBoth the trait and the cell's own type are dispatched on, because they answer different questions: the trait says how this layout names a cell, the cell's type how the caller named it. A pair that does not agree — an integer for a tuple-addressed grid — reaches the message below, at the entry point the caller wrote.\n\n\n\n\n\n","title":"FlowGeometries.Grids._cell_indices"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._cell_measure-Tuple{Any, Any, Any}","page":"Grids","text":"_cell_measure(geometry, axes, periods)\n\nThe grid's stored measure: a SeparableMeasure wherever the metric factors per axis, and a SlabMeasure where one pair of directions is coupled and the rest are not.\n\n\n\n\n\n","title":"FlowGeometries.Grids._cell_measure"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._cell_named_by-Tuple{FlowGeometries.Grids.AbstractGrid, Tuple{Vararg{Integer}}}","page":"Grids","text":"_cell_named_by(grid, I::Tuple) -> cell\n\nThe cell a caller named with the indices I: the tuple itself where cells are CartesianCells, and its single element where they are FlatCells. An entry point taking I::Vararg{Integer} hands the traversals this.\n\n\n\n\n\n","title":"FlowGeometries.Grids._cell_named_by"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._centers_to_corners-Union{Tuple{AbstractArray{T, N}}, Tuple{N}, Tuple{T}} where {T<:AbstractFloat, N}","page":"Grids","text":"_centers_to_corners(C) -> K\n\nReconstruct a cell-vertex array one larger in every direction from the N-D cell-center array C, by averaging the (up to 2^N) surrounding centers with a linearly-extrapolated one-cell ghost ring, so the true domain-boundary vertices land a half-cell outside the outermost centers. Used only when the caller does not supply explicit corner arrays; requires at least 2 centers across every direction.\n\nUnlike the corner-area kernel this is dimension-generic: it is a multilinear midpoint, and the ghost ring is a per-direction linear extension of it.\n\n\n\n\n\n","title":"FlowGeometries.Grids._centers_to_corners"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._curvilinear_periods-Union{Tuple{T}, Tuple{N}, Tuple{FlowGeometries.Geometry.AbstractGeometry{T}, NTuple{N, AbstractArray{T, N}}, NTuple{N, FlowGeometries.Grids.AbstractTopology}, Any}} where {N, T<:AbstractFloat}","page":"Grids","text":"_curvilinear_periods(geometry, centers, topology, period) -> NTuple{N,T}\n\nWrap length per periodic direction, zero where bounded. Spherical longitude closes at 2π; a Cartesian direction's comes from that direction's own line of centres.\n\n\n\n\n\n","title":"FlowGeometries.Grids._curvilinear_periods"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._curvilinear_topology-Union{Tuple{N}, Tuple{FlowGeometries.Geometry.AbstractGeometry, AbstractArray{<:Any, N}, Any}} where N","page":"Grids","text":"_curvilinear_topology(geometry, x, topo) -> NTuple{2,AbstractTopology}\n\nPer-direction closure. Absent a caller's choice, direction 1 is auto-detected from the first row of centres read as a longitude-like axis, as StructuredGrid does.\n\n\n\n\n\n","title":"FlowGeometries.Grids._curvilinear_topology"},{"category":"function","location":"api/grids/#FlowGeometries.Grids._embedded_floor","page":"Grids","text":"_embedded_floor(grid, embedding, h, ::Val{D}) -> NTuple{D,T}\n\nA lower bound on the embedded coordinate in each direction, less one bin, which is where the lattice starts. O(1): no cell is read.\n\nAny bound serves: the lattice is unbounded along a direction that does not wrap, and bins are hashed, so a loose origin shifts every bin index equally. Taking it from the geometry indexes a layout without a pass over its cells.\n\n\n\n\n\n","title":"FlowGeometries.Grids._embedded_floor"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._embedding_for-Tuple{FlowGeometries.Geometry.AbstractCartesianGeometry, Integer}","page":"Grids","text":"_embedding_for(geo, ncoords) -> AbstractEmbedding\n\nThe space a geometry's ncoords-coordinate points sit in. embedding_of is this for a grid, which knows its own coordinate count; a caller holding loose coordinate vectors states it.\n\nOne definition, so an index built from a grid and one built from bare coordinates convert a radius through the same code.\n\n\n\n\n\n","title":"FlowGeometries.Grids._embedding_for"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._ghost_points-Union{Tuple{T}, Tuple{D}, Tuple{AbstractMatrix{T}, NTuple{D, Bool}, NTuple{D, T}}} where {D, T}","page":"Grids","text":"_ghost_points(pts, periodic, period) -> (all_pts, nghost)\n\nReplicate the D × N point matrix once per combination of periodic image offsets, originals in the first N columns. A wrapping domain is then searched by an ordinary Euclidean query over the images.\n\n\n\n\n\n","title":"FlowGeometries.Grids._ghost_points"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._gnomonic_solid_angle-Union{Tuple{T}, NTuple{4, T}} where T","page":"Grids","text":"_gnomonic_solid_angle(X1, X2, Y1, Y2) -> T\n\nThe solid angle subtended by the gnomonic rectangle [X1, X2] × [Y1, Y2] on the unit sphere, exactly.\n\natan(XY / √(1 + X² + Y²)) at a corner is the solid angle of the rectangle from the panel centre to that corner, so the four corners combine by inclusion–exclusion. Over a whole panel it gives 4π/6.\n\n\n\n\n\n","title":"FlowGeometries.Grids._gnomonic_solid_angle"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._grid_points-Union{Tuple{FlowGeometries.Grids.AbstractGrid{G, T}}, Tuple{T}, Tuple{G}} where {G, T}","page":"Grids","text":"_grid_points(grid) -> (raw, D)\n\nCell centres as a D × n matrix, in the grid's own coordinates.\n\n\n\n\n\n","title":"FlowGeometries.Grids._grid_points"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._ico_class-Tuple{Int64, Int64}","page":"Grids","text":"_ico_class(k, ν) -> (b, c)\n\nVertex k's symmetry class: the two smallest of its barycentric parts (i, j, ν−i−j), the largest being ν minus their sum.\n\nAll twenty faces are the same spherical triangle and the geodesic is built the same way on each, so a vertex's dual area depends only on its barycentric position within a face; the face being equilateral, only on that triple up to order. A vertex on a macro-edge or at a corner sits on several faces, and an isometry of the icosahedron carrying it to another vertex of its class carries those faces with it, so the areas agree there too.\n\nThe classes are the partitions of ν into three parts, O(ν²/12) of them against 10ν² + 2 vertices.\n\n\n\n\n\n","title":"FlowGeometries.Grids._ico_class"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._ico_dual_share-Union{Tuple{T}, Tuple{Tuple{T, T, T}, Tuple{T, T, T}, Tuple{T, T, T}}} where T","page":"Grids","text":"_ico_dual_share(V, A, B) -> T\n\nVertex V's share of the spherical triangle (V, A, B): the quadrilateral (V, M_VA, O, M_VB) with O the circumcenter and M the edge midpoints, as two spherical excesses.\n\nThose arcs are the triangle's perpendicular bisectors — O is equidistant from all three vertices and each midpoint from its two — so the share is exactly V's Voronoi cell restricted to that triangle, and the three shares tile the triangle.\n\n\n\n\n\n","title":"FlowGeometries.Grids._ico_dual_share"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._ico_vertex_measure-Union{Tuple{T}, Tuple{FlowGeometries.Grids.IcosahedralGrid{T, G} where G<:FlowGeometries.Geometry.AbstractSphericalGeometry{T}, Int64, Int64}} where T","page":"Grids","text":"_ico_vertex_measure(grid, k, ν) -> T\n\nThe dual area of vertex k, from its own incident triangles: the fan of _ico_dual_shares over every face the vertex sits on.\n\n\n\n\n\n","title":"FlowGeometries.Grids._ico_vertex_measure"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._linear_of-Tuple{FlowGeometries.Grids.AbstractGrid, Any}","page":"Grids","text":"_linear_of(grid, cell) -> Int\n\nThe linear index a cell reports as, and the inverse of _cell_from_linear.\n\n\n\n\n\n","title":"FlowGeometries.Grids._linear_of"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._max_gap-Union{Tuple{AbstractVector{T}}, Tuple{T}} where T<:AbstractFloat","page":"Grids","text":"_max_gap(x) -> maximum consecutive |gap|, or 0 if length(x) < 2\n\nLargest spacing anywhere on axis x, the counterpart of _min_gap. Together they bound how far an index window must reach to cover a given physical distance.\n\n\n\n\n\n","title":"FlowGeometries.Grids._max_gap"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._measure_factors-Union{Tuple{G}, Tuple{T}, Tuple{N}, Tuple{G, NTuple{N, AbstractVector{T}}, NTuple{N, Union{Nothing, Real}}}} where {N, T<:AbstractFloat, G<:FlowGeometries.Geometry.AbstractCartesianGeometry{T}}","page":"Grids","text":"_measure_factors(geometry, axes, periods) -> NTuple{N,AbstractVector}\n\nPer-axis factors whose outer product is the cell measure: measure[I...] == prod(w[d][I[d]]).\n\nEvery rectilinear cell measure this package supports is separable in exactly this way — Cartesian Δx·Δy·Δz, and spherical R²cosφ·Δλ·Δφ = (Δλ) · (R²cosφ·Δφ) or r²cosφ·Δλ·Δφ·Δr = (Δλ) · (cosφ·Δφ) · (r²·Δr). Building the measure as an outer product of these factors keeps the result in whatever array type the axes use, and exposes the separability to a caller that can exploit it.\n\nA degenerate (length-1) angular axis drops its differential, so a zonal transect measures arc length R·cosφ·Δλ along its circle of latitude and a meridional one measures R·Δφ.\n\n\n\n\n\n","title":"FlowGeometries.Grids._measure_factors"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._min_gap-Union{Tuple{AbstractVector{T}}, Tuple{T}} where T<:AbstractFloat","page":"Grids","text":"_min_gap(x) -> minimum consecutive |gap|, or Inf if length(x) < 2\n\nSmallest spacing found anywhere on axis x. It bounds the search radius for a genuinely nonuniform axis: a distance check still gates what is included, so the smallest gap anywhere can only widen the search window, and no in-range cell is missed.\n\n\n\n\n\n","title":"FlowGeometries.Grids._min_gap"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._min_image-Union{Tuple{N}, Tuple{NTuple{N, Any}, NTuple{N, Any}, NTuple{N, Any}}} where N","page":"Grids","text":"_min_image(p0, pt, prd) -> NTuple\n\npt brought to the image nearest p0, per component, for each direction with a nonzero wrap length.\n\nFor an angular coordinate the geometry's own distance is already 2π-periodic and this changes nothing (it also keeps Vincenty inside its |Δλ| ≤ π regime); for a periodic Cartesian coordinate it is what makes the seam invisible. Per-component minimum image is the global minimum for a separable metric, which the Euclidean one is.\n\n\n\n\n\n","title":"FlowGeometries.Grids._min_image"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._raw_coords-Union{Tuple{N}, Tuple{T}, Tuple{G}, Tuple{FlowGeometries.Grids.AbstractStructuredGrid{G, T}, Vararg{Integer, N}}} where {G, T, N}","page":"Grids","text":"_raw_coords(grid, I...) -> NTuple\n\nPositional coordinate values at indices I. Internal; prefer coords.\n\n\n\n\n\n","title":"FlowGeometries.Grids._raw_coords"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._sep_extrema-Union{Tuple{N}, Tuple{T}, Tuple{Any, FlowGeometries.Grids.SeparableMeasure{T, N, F} where F<:NTuple{N, AbstractVector{T}}}} where {T, N}","page":"Grids","text":"_sep_extrema(f, m) -> (lo, hi)\n\nSmallest and largest f(cell) over a SeparableMeasure, from the per-axis extremes.\n\nA product's extremes are attained with every factor at one of its own endpoints, so all 2^N endpoint combinations are formed and the best taken. This holds for factors of either sign, where ∏ maximum alone holds only for non-negative ones, and it costs O(∑ Nᵈ + 2^N) against the dense O(∏ Nᵈ).\n\n\n\n\n\n","title":"FlowGeometries.Grids._sep_extrema"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._shift_set-Union{Tuple{T}, Tuple{Bool, T}} where T","page":"Grids","text":"_shift_set(periodic, period)\n\nOffsets to replicate a point set by: (0,) in a non-wrapping direction, (0, -L, +L) in a wrapping one. Zero first, so the originals occupy the first block of the replicated set.\n\n\n\n\n\n","title":"FlowGeometries.Grids._shift_set"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._to_axis-Union{Tuple{T}, Tuple{Type{T}, AbstractRange{T}}} where T<:AbstractFloat","page":"Grids","text":"_to_axis(T, x) -> AbstractVector{T}\n\nAdapt axis input x to element type T, keeping whatever is known about its spacing and never collapsing a provably uniform axis into a plain Vector.\n\nAn axis already of element type T is kept exactly as it is, whatever type it is. A caller's own range subtype, a StepRangeLen whose TwicePrecision internals they want, a BigFloat-backed range — all pass through untouched. Nothing needs converting to get the uniform fast paths: those dispatch on Axes.spacing_trait, which is UniformSpacing() for every AbstractRange, so the caller's own range takes them.\n\nConversion happens only where the element type must change, and there an arbitrary range subtype cannot generically be rebuilt at a new eltype. That case becomes an Axes.UniformAxis{T}, which is also how a Float32 axis stops carrying Float64 internals. Call Axes.uniform_axis to convert at the call site.\n\nFour methods, ordered so nothing is ambiguous (a StepRangeLen{T} is both an AbstractRange and an AbstractVector{T}, so the parameterized range form is needed to break that tie):\n\nAbstractRange{T} / AbstractVector{T}: passthrough, zero cost, type preserved.\nAbstractRange (wrong eltype): rebuilt as UniformAxis{T}, still uniform and now isbits.\nAbstractVector (wrong eltype): copied with similar, so a device-resident array stays in its own storage.\n\n\n\n\n\n","title":"FlowGeometries.Grids._to_axis"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._total_measure-Union{Tuple{FlowGeometries.Grids.AbstractGrid{G, T}}, Tuple{T}, Tuple{G}} where {G, T}","page":"Grids","text":"_total_measure(grid) -> T\n\nsum(measure(grid)), from whatever the layout knows about the region its cells tile. The fallback adds the cells up; a layout tiling the whole sphere returns 4πR² without visiting one.\n\n\n\n\n\n","title":"FlowGeometries.Grids._total_measure"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._voronoi_areas-Tuple{Any, AbstractVector, AbstractVector}","page":"Grids","text":"_voronoi_areas(geometry, x, y) -> Vector{T}\n\nThe areas alone, for a caller with no use for the cells — see _voronoi_tessellation.\n\n\n\n\n\n","title":"FlowGeometries.Grids._voronoi_areas"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._voronoi_tessellation-Tuple{FlowGeometries.Geometry.AbstractCartesianGeometry, AbstractVector, AbstractVector}","page":"Grids","text":"_voronoi_tessellation(geometry, x, y) -> (areas, mesh::CellMesh)\n\nExtension hook: exact per-node Voronoi-cell area from a Delaunay/convex-hull tessellation of the node coordinates, together with the triangulation it came from. Dispatched on the geometry type (each needs a different tessellation library): overridden for CartesianGeometry by a consumer DelaunayTriangulation extension (load using DelaunayTriangulation, planar Voronoi clipped to the point set's convex hull) and for SphericalGeometry by a consumer Quickhull extension (load using Quickhull, spherical Voronoi from the dual of the 3D convex hull of the unit-sphere embedding).\n\nAn Geometry.AbstractEllipsoidalGeometry has no such construction and says so: its cell areas are the caller's to supply.\n\n\n\n\n\n","title":"FlowGeometries.Grids._voronoi_tessellation"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._wrap_lengths-Union{Tuple{N}, Tuple{FlowGeometries.Grids.AbstractGrid, Val{N}}} where N","page":"Grids","text":"_wrap_lengths(grid, Val(N)) -> NTuple{N,T}\n\nWrap length per direction, zero where the direction is bounded, so _min_image leaves those components alone.\n\n\n\n\n\n","title":"FlowGeometries.Grids._wrap_lengths"},{"category":"method","location":"api/grids/#FlowGeometries.Grids._wrap_sign-Tuple{AbstractVector}","page":"Grids","text":"_wrap_sign(x) -> ±1\n\n+1 for an ascending axis and -1 for a descending one: the sign that turns a period magnitude into the wrapped neighbour's offset in index order. Defined in Axes — it is a property of the axis alone, and the discretization layer needs the same answer.\n\n\n\n\n\n","title":"FlowGeometries.Grids._wrap_sign"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.adjacency_source","page":"Grids","text":"adjacency_source(grid) -> AbstractAdjacency\n\nWhere grid's neighbour relation comes from.\n\n\n\n\n\n","title":"FlowGeometries.Grids.adjacency_source"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.area-Tuple{FlowGeometries.Grids.AbstractGrid, Vararg{Integer}}","page":"Grids","text":"area(grid, I...) -> T\n\nmeasure under its 2-D name.\n\n\n\n\n\n","title":"FlowGeometries.Grids.area"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.axis-Tuple{FlowGeometries.Grids.AbstractStructuredGrid, Integer}","page":"Grids","text":"axis(grid::AbstractStructuredGrid, d::Integer) -> AbstractVector\n\nDirection d's coordinate axis. Only rectilinear grids have axes; this is coordinates under the name that is exact for them.\n\n\n\n\n\n","title":"FlowGeometries.Grids.axis"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.axis_at-Tuple{FlowGeometries.Grids.StaggeredGrid, Integer, FlowGeometries.Discretization.Center}","page":"Grids","text":"axis_at(sg, d, loc) -> AbstractVector\n\nDirection d's samples at location loc: the primal axis at Discretization.Center, its faces at Discretization.Face.\n\n\n\n\n\n","title":"FlowGeometries.Grids.axis_at"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.base_grid-Tuple{FlowGeometries.Grids.RotatedGrid}","page":"Grids","text":"base_grid(grid::RotatedGrid) -> AbstractStructuredGrid\n\nThe rectilinear mesh underneath. Its axes are the coordinates a derivative differences along; the rotation changes where cells are reported, leaving the lattice as it is.\n\n\n\n\n\n","title":"FlowGeometries.Grids.base_grid"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.bounds-Tuple{FlowGeometries.Grids.AbstractGrid, Integer}","page":"Grids","text":"bounds(grid, d) -> (lo, hi)\n\nSmallest and largest coordinate along direction d, ordered lo ≤ hi regardless of whether the direction is stored ascending or descending. These are the extreme sample positions, i.e. cell centres.\n\nO(1) on a rectilinear grid: an axis is monotone, as every search here bisects it, so its extremes are its endpoints. O(N) where the coordinates are per-cell fields, which carry no such order; a query that reads this repeatedly takes it from a MetricTopology, built once.\n\n\n\n\n\n","title":"FlowGeometries.Grids.bounds"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.candidate_source","page":"Grids","text":"candidate_source(grid) -> AbstractCandidateSource\n\nHow a distance query on grid enumerates candidates.\n\n\n\n\n\n","title":"FlowGeometries.Grids.candidate_source"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.cell_address","page":"Grids","text":"cell_address(grid) -> AbstractCellAddress\n\nWhether grid's cells are named by an index tuple or by a single integer.\n\n\n\n\n\n","title":"FlowGeometries.Grids.cell_address"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.cell_at-Tuple{FlowGeometries.Grids.AbstractGrid, Any}","page":"Grids","text":"cell_at(grid, c)\n\nOne element of cells as the cell spelling the grid's own entry points take: an index tuple where cells are addressed Cartesian-wise, a linear id where they are flat.\n\n\n\n\n\n","title":"FlowGeometries.Grids.cell_at"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.cell_id-Tuple{FlowGeometries.Grids.CubedSphereGrid, Integer, Integer, Integer}","page":"Grids","text":"cell_id(grid, panel, i, j) -> Int\n\nThe cell id of (panel, i, j), the inverse of panel_cell.\n\n\n\n\n\n","title":"FlowGeometries.Grids.cell_id"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.cell_list-Union{Tuple{FlowGeometries.Grids.AbstractGrid{G, T}}, Tuple{T}, Tuple{G}} where {G, T}","page":"Grids","text":"cell_list(grid; ball, active_only = false) -> CellListIndex\n\nBuild a CellListIndex over grid's cell centres, binned at side ball — the radius you mean to query at. Needs no external package.\n\nThe default indexes every cell, which serves a query at either mask policy: an active_only = true query filters what it is handed.\n\nactive_only = true indexes the active region alone, sizing a mostly-masked grid — a basin, a catchment — by that region. Such an index answers queries at that same policy, and raises for one asking to see a masked cell. Ask for it where the policy is fixed, as a sweep does.\n\n\n\n\n\n","title":"FlowGeometries.Grids.cell_list"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.cell_mesh-Tuple{FlowGeometries.Grids.UnstructuredGrid}","page":"Grids","text":"cell_mesh(grid) -> CellMesh | Nothing\n\nThe grid's CellMesh, or nothing where it has none.\n\nA node set built by tessellation keeps the cells that tessellation produced; one given its adjacency directly has only that adjacency, and no cells to speak of unless it is handed some.\n\n\n\n\n\n","title":"FlowGeometries.Grids.cell_mesh"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.cell_nodes-Tuple{FlowGeometries.Grids.CellMesh, Integer}","page":"Grids","text":"cell_nodes(mesh, c) -> AbstractVector{<:Integer}\n\nThe nodes of cell c, in order around it, as a view into the mesh's own storage.\n\n\n\n\n\n","title":"FlowGeometries.Grids.cell_nodes"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.cell_width-Tuple{FlowGeometries.Grids.AbstractStructuredGrid, Integer, Integer}","page":"Grids","text":"cell_width(grid, d, i) -> T\n\nThe coordinate width of cell i along direction d: Discretization.cell_width on that direction's axis, with the grid's own wrap period. Non-negative whichever way the axis is stored.\n\nA coordinate width, distinct from the cell measure: on a sphere measure is R²cosφ·Δλ·Δφ and this is the Δλ or Δφ in it. measure_factors folds the metric into each factor, so it does not expose these separately.\n\n\n\n\n\n","title":"FlowGeometries.Grids.cell_width"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.cell_widths-Tuple{FlowGeometries.Grids.AbstractStructuredGrid, Integer}","page":"Grids","text":"cell_widths(grid, d) -> AbstractVector\n\ncell_width along the whole of direction d, as Discretization.cell_widths on its axis with the grid's own wrap period. A uniform direction gets an Axes.ConstantVector, so nothing is materialized.\n\n\n\n\n\n","title":"FlowGeometries.Grids.cell_widths"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.cells-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"cells(grid)\n\nWhat a traversal of every cell iterates, and cell_at turns one of its elements into a cell. Together they are how a layout is walked without knowing how it names a cell — see cell_address.\n\n\n\n\n\n","title":"FlowGeometries.Grids.cells"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.center_grid-Tuple{FlowGeometries.Grids.StaggeredGrid}","page":"Grids","text":"center_grid(sg) -> StructuredGrid\n\nThe primal grid sg was built from: every direction at Center(), which is where a tracer sits.\n\n\n\n\n\n","title":"FlowGeometries.Grids.center_grid"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.center_location-Union{Tuple{FlowGeometries.Grids.StaggeredGrid{T, N, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:NTuple{N, AbstractVector{T}}}}, Tuple{N}, Tuple{T}} where {T, N}","page":"Grids","text":"center_location(sg) -> NTuple{N,Center}\n\nEvery direction at Center().\n\n\n\n\n\n","title":"FlowGeometries.Grids.center_location"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.coordinate_names-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"coordinate_names(grid) -> NTuple{N,Symbol}\n\nThe grid's coordinate names, from its geometry: (:x, :y[, :z]) or (:λ, :φ[, :r]).\n\n\n\n\n\n","title":"FlowGeometries.Grids.coordinate_names"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.coordinates-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"coordinates(grid) -> NTuple{N,AbstractArray}\ncoordinates(grid, d::Integer) -> AbstractArray\n\nThe grid's coordinate arrays, or just direction d's. Shape depends on the grid architecture: a StructuredGrid stores one 1-D axis vector per direction, a CurvilinearGrid one N-D array per direction, an UnstructuredGrid one value per node. Direction order matches Geometry.point_names — (x, y, z) for Cartesian, (λ, φ, r) for spherical.\n\nSee also axis (the rectilinear spelling), coords (a single point).\n\n\n\n\n\n","title":"FlowGeometries.Grids.coordinates"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.coords!-Tuple{AbstractVector, FlowGeometries.Grids.AbstractGrid, Vararg{Integer}}","page":"Grids","text":"coords!(out, grid, I...) -> out\n\nWrite positional coordinates into a preallocated AbstractVector (length(out) ≥ N).\n\n\n\n\n\n","title":"FlowGeometries.Grids.coords!"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.coords-Tuple{FlowGeometries.Grids.AbstractGrid, Vararg{Integer}}","page":"Grids","text":"coords(grid, I...) -> NamedTuple\n\nPoint at indices I, as a geometry-named NamedTuple:\n\nCartesian: (x=, y=) or (x=, y=, z=)\nSpherical: (λ=, φ=) or (λ=, φ=, r=)\n\nSee also coords!, coords(::Type, grid, I...).\n\n\n\n\n\n","title":"FlowGeometries.Grids.coords"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.coords-Union{Tuple{S}, Tuple{Type{S}, FlowGeometries.Grids.AbstractGrid, Vararg{Integer}}} where S","page":"Grids","text":"coords(::Type{S}, grid, I...) -> S\n\nConstruct the point as type S — Tuple, NTuple{N,T}, NamedTuple, Vector{T}, or SVector{N,T}/MVector{N,T} when StaticArrays is loaded. See Geometry.build_point for how S is assembled.\n\n\n\n\n\n","title":"FlowGeometries.Grids.coords"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.corner_coords-Union{Tuple{N}, Tuple{G}, Tuple{T}, Tuple{FlowGeometries.Grids.CurvilinearGrid{T, G, N, TP, C, KC, MA, B} where {TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractArray{T, N}}, KC<:Union{Nothing, NTuple{N, AbstractArray{T, N}}}, MA<:AbstractArray{T, N}, B<:AbstractArray{Bool, N}}, Vararg{Integer, N}}} where {T, G, N}","page":"Grids","text":"corner_coords(grid::CurvilinearGrid, I...) -> NamedTuple\ncorner_coords(S, grid::CurvilinearGrid, I...) -> S\n\nVertex I of the cell-vertex array, named by the geometry exactly as coords names cell centers.\n\n\n\n\n\n","title":"FlowGeometries.Grids.corner_coords"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.corners-Tuple{FlowGeometries.Grids.CurvilinearGrid}","page":"Grids","text":"corners(grid::CurvilinearGrid) -> NTuple{N,AbstractArray}\ncorners(grid::CurvilinearGrid, d::Integer) -> AbstractArray\n\nThe cell-vertex coordinate arrays — one larger than coordinates in every direction, and in the same direction order.\n\nAvailable on a grid that was given them or built with keep_corners = true; see has_corners.\n\n\n\n\n\n","title":"FlowGeometries.Grids.corners"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.displacement","page":"Grids","text":"displacement(grid, I, J) -> NTuple{N,T}\ndisplacement(grid::UnstructuredGrid, i, j) -> NTuple{N,T}\n\nThe signed per-direction coordinate offset from cell I to cell J, reduced to the nearest image in every periodic direction — the offset Geometry.distance is taken from.\n\nA coordinate quantity, so it lives here, while the distance extends Geometry.distance. Across a periodic seam the two cells' stored coordinates differ by nearly a full period, and this reports the short way round.\n\n\n\n\n\n","title":"FlowGeometries.Grids.displacement"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.embed_point","page":"Grids","text":"embed_point(grid, p) -> NTuple\n\nOne coordinate tuple through the same transform embedded_points applies to the cell centres, so a query seeded by a point searches the space the index was built in.\n\n\n\n\n\n","title":"FlowGeometries.Grids.embed_point"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.embedded_at-Tuple{FlowGeometries.Grids.AbstractGrid, Any}","page":"Grids","text":"embedded_at(grid, cell) -> NTuple{D,T}\n\nA cell's centre in the space an index searches, taken from the grid: O(1) arithmetic where the coordinates are a formula, a read where they are data.\n\n\n\n\n\n","title":"FlowGeometries.Grids.embedded_at"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.embedded_points","page":"Grids","text":"embedded_points(grid) -> (pts, nghost, embedding)\n\nThe cell centres in the space an index searches, the number of periodic replications they carry, and the AbstractEmbedding saying what a radius means there.\n\nOne definition, so every index searches the same space as every other and as the k-d-tree construction path — the guarantee that an indexed query and a scan return the same cells rests on it.\n\n\n\n\n\n","title":"FlowGeometries.Grids.embedded_points"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.embedded_radius-Union{Tuple{T}, Tuple{FlowGeometries.Grids.CartesianEmbedding, T}} where T","page":"Grids","text":"embedded_radius(embedding, r) -> T\n\nA physical radius as a radius in the embedding.\n\n\n\n\n\n","title":"FlowGeometries.Grids.embedded_radius"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.embedding_of","page":"Grids","text":"embedding_of(grid) -> AbstractEmbedding\n\nThe Euclidean space this grid's cell centres sit in, and therefore what a physical radius means there.\n\nNamed separately from embedded_points because an index that streams the centres still has to know the space before it reads the first one.\n\n\n\n\n\n","title":"FlowGeometries.Grids.embedding_of"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.extent-Tuple{FlowGeometries.Grids.AbstractGrid, Integer}","page":"Grids","text":"extent(grid, d) -> T\n\nhi - lo from bounds: the span covered by direction d's samples. Zero for a singleton direction.\n\n\n\n\n\n","title":"FlowGeometries.Grids.extent"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.fold_candidates","page":"Grids","text":"fold_candidates(f, acc, index, grid, I, r) -> acc\n\nThread acc = f(acc, k) over every cell k the index reports near cell I, without building a list. A superset of the ball, each cell exactly once; the caller's exact distance gate decides membership.\n\nA fold allocates nothing and needs no per-query buffer, so it runs inside a kernel. A tree walk needs one, having to deduplicate the periodic images it searches over.\n\n\n\n\n\n","title":"FlowGeometries.Grids.fold_candidates"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.fold_candidates_at-NTuple{6, Any}","page":"Grids","text":"fold_candidates_at(f, acc, index, q, r, scratch) -> acc\n\nfold_candidates around an arbitrary point q, already in the index's embedding. A cell query is this one at the cell's own centre, so there is a single traversal.\n\nscratch is a candidate buffer for an index that has to materialize one — a tree does, since it must deduplicate the periodic images it searches over. A cell list folds directly and ignores it.\n\n\n\n\n\n","title":"FlowGeometries.Grids.fold_candidates_at"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.formula_neighbors","page":"Grids","text":"formula_neighbors(grid, cell) -> (ids::NTuple{K,Int}, n)\n\nA cell's neighbours as linear indices, in the first n entries of a fixed-width tuple, where K is max_neighbors(grid).\n\nThe tuple is a stack value, so a traversal over every cell of a layout whose adjacency is arithmetic allocates nothing and threads no scratch through. n varies where a layout has singular cells: a HEALPix pixel at a face corner has seven neighbours where the rest have eight.\n\nThe one method a FormulaNeighbors layout supplies.\n\n\n\n\n\n","title":"FlowGeometries.Grids.formula_neighbors"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.frequency-Tuple{FlowGeometries.Grids.IcosahedralGrid}","page":"Grids","text":"frequency(grid::IcosahedralGrid) -> Int\n\nThe subdivision frequency ν: the grid has 10ν² + 2 vertices and 20ν² triangles.\n\n\n\n\n\n","title":"FlowGeometries.Grids.frequency"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.grid_at-Union{Tuple{N}, Tuple{T}, Tuple{FlowGeometries.Grids.StaggeredGrid{T, N, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:NTuple{N, AbstractVector{T}}}, NTuple{N, FlowGeometries.Discretization.AbstractLocation}}} where {T, N}","page":"Grids","text":"grid_at(sg, loc) -> StructuredGrid\n\nThe grid of sg at location loc, one Discretization.Center/Discretization.Face per direction.\n\nAn ordinary StructuredGrid, so every rectilinear operator applies to it unchanged. It is built on request — there are 2^N locations and a mesh uses few of them — so a caller differencing repeatedly holds the result, the way a stencil plan is held.\n\n\n\n\n\n","title":"FlowGeometries.Grids.grid_at"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.has_cell_mesh-Tuple{Any}","page":"Grids","text":"has_cell_mesh(grid) -> Bool\n\nWhether cell_mesh has cells to give — what the operators that need a cell dispatch on.\n\n\n\n\n\n","title":"FlowGeometries.Grids.has_cell_mesh"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.has_corners-Tuple{FlowGeometries.Grids.CurvilinearGrid}","page":"Grids","text":"has_corners(grid) -> Bool\n\nWhether grid retained its cell-vertex arrays, and so can answer corners and corner_coords.\n\n\n\n\n\n","title":"FlowGeometries.Grids.has_corners"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.has_spatial_index-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"has_spatial_index(grid) -> Bool\n\nWhether the k-d tree behind spatial_index can be built for this grid — false until the NearestNeighbors extension is loaded. Answers \"is the tree available?\" without calling spatial_index speculatively and catching its error.\n\nThis is not a test for whether a grid can be indexed at all: cell_list needs no extension and is what the sweeps build.\n\n\n\n\n\n","title":"FlowGeometries.Grids.has_spatial_index"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.has_symmetric_adjacency","page":"Grids","text":"has_symmetric_adjacency(grid) -> Bool\n\nWhether the graph grid builds satisfies j ∈ N(i) ⟺ i ∈ N(j), answered from the layout.\n\nA symmetric graph with sorted rows has the same arrays read as CSR and as CSC, so Connectivity.sparse_adjacency_matrix hands its buffers to the matrix without transposing. Connectivity.is_symmetric_adjacency answers the same question about a built graph, and pays a transpose to do it; this answers before one exists.\n\nfalse by default, so a layout claims the shortcut only where its own formula gives it. Masking preserves it either way: an edge and its reverse are the same pair of cells, and both are dropped together. A layout whose neighbours are an index stencil answers through the stencil, which carries its own Stencils.is_symmetric.\n\n\n\n\n\n","title":"FlowGeometries.Grids.has_symmetric_adjacency"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.incident_nodes-Tuple{FlowGeometries.Grids.AbstractUnstructuredGrid, Integer}","page":"Grids","text":"incident_nodes(grid, idx::Integer) -> AbstractVector{<:Integer}\n\nThe nodes stored as incident to node idx, as a zero-copy view into the CSR adjacency.\n\nThis is storage: it reports what the mesh holds, with no regard for the mask. neighbors is the query, and it honours active_only on every layout.\n\n\n\n\n\n","title":"FlowGeometries.Grids.incident_nodes"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.index_within!-Tuple{AbstractVector{<:Integer}, Vararg{Any, 4}}","page":"Grids","text":"index_within!(buffer, index, grid, I, r) -> candidate cell indices\nindex_within(index, grid, I, r) -> candidate cell indices\n\nExtension hook: the cells an index reports near I, as linear indices. It must return a superset of the cells within r; the caller applies the exact distance gate, so over-returning is safe and under-returning is not.\n\nindex_within! overwrites and returns buffer, so a sweep over many cells allocates nothing per query. index_within is the same query into a fresh vector.\n\n\n\n\n\n","title":"FlowGeometries.Grids.index_within!"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.isactive-Tuple{FlowGeometries.Grids.AbstractGrid, Vararg{Integer}}","page":"Grids","text":"isactive(grid, I...) -> Bool\n\nWhether cell/node I participates (false = masked out).\n\n\n\n\n\n","title":"FlowGeometries.Grids.isactive"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.isperiodic-Tuple{FlowGeometries.Grids.AbstractGrid, Integer}","page":"Grids","text":"isperiodic(grid, d) -> Bool\n\nWhether coordinate direction d wraps. See topology for the type-level form and period for the wrap length.\n\n\n\n\n\n","title":"FlowGeometries.Grids.isperiodic"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.isuniform-Tuple{FlowGeometries.Grids.AbstractGrid, Integer}","page":"Grids","text":"isuniform(grid, d) -> Bool\nisuniform(grid) -> Bool\n\nWhether coordinate direction d has constant spacing known from its type (all directions, for the no-d form). The answer comes from the type alone; no code path inspects coordinate values to decide it. See spacing_trait for the form a method can dispatch on.\n\nA curvilinear or unstructured grid is never uniform: its coordinates are per-cell fields with no axes.\n\n\n\n\n\n","title":"FlowGeometries.Grids.isuniform"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.local_spacing-Tuple{FlowGeometries.Grids.AbstractStructuredGrid, Integer, Integer}","page":"Grids","text":"local_spacing(grid, d, i) -> (h_m, h_p)\n\nThe one-sided coordinate gaps around index i along direction d: Discretization.local_spacing on that direction's axis, with the wrap period taken from the grid, so a periodic seam is right without the caller supplying it.\n\nSigned, so a descending axis reports negative gaps — see the axis-level form for why, and cell_width for the non-negative width built from them. Allocation-free, so this is the per-point form to call inside a loop assembling a finite-difference operator.\n\n\n\n\n\n","title":"FlowGeometries.Grids.local_spacing"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.locate","page":"Grids","text":"locate(grid, p) -> cell index\nlocate(grid, p; active_only=false, topology, scratch) -> cell index\n\nThe cell of grid that p belongs to, as an NTuple of indices on a rectilinear or curvilinear grid and a node number on an UnstructuredGrid. p is a coordinate tuple in the grid's own coordinates.\n\nOn a StructuredGrid this is Discretization.locate per direction, so the answer is the cell whose faces bracket p — an O(1) lookup on a uniform axis and a bisection otherwise, with a periodic direction wrapped first. A direction p lies outside reports 0 for that direction.\n\nElsewhere there are no axes to bracket along and it is the nearest cell centre, which is exactly the containing cell for a node set, whose cells are the Voronoi regions of its nodes. On a curvilinear grid the two can differ where cells are strongly sheared, so the contract is nearest-centre. That form takes the keywords: topology carrying an index — cell_list — makes it a bin lookup, scratch is a Connectivity.ball_scratch buffer, and active_only restricts the answer to unmasked cells. It defaults to false here, where the ball queries default to true: which cell a point falls in is a question about the grid, answerable over the masked region.\n\n\n\n\n\n","title":"FlowGeometries.Grids.locate"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.location_at-Union{Tuple{N}, Tuple{T}, Tuple{FlowGeometries.Grids.StaggeredGrid{T, N, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:NTuple{N, AbstractVector{T}}}, Integer}} where {T, N}","page":"Grids","text":"location_at(sg, d) -> NTuple{N,AbstractLocation}\n\nWhere the velocity component along direction d sits: Face() in d, Center() in every other direction.\n\n\n\n\n\n","title":"FlowGeometries.Grids.location_at"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.locations-Union{Tuple{FlowGeometries.Grids.StaggeredGrid{T, N, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:NTuple{N, AbstractVector{T}}}}, Tuple{N}, Tuple{T}} where {T, N}","page":"Grids","text":"locations(sg) -> NTuple{N,NTuple{N,AbstractLocation}}\n\nAll N velocity locations, one per direction — location_at for each, which together are the Arakawa C staggering itself.\n\n\n\n\n\n","title":"FlowGeometries.Grids.locations"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.mask-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"mask(grid) -> AbstractArray{Bool}\n\nWhich cells/nodes participate, as an array over the grid's own shape. Ask isactive for one cell; this is the whole array, and it is what fixes the grid's size.\n\n\n\n\n\n","title":"FlowGeometries.Grids.mask"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.materialize-Union{Tuple{FlowGeometries.Grids.AbstractGrid{G, T}}, Tuple{T}, Tuple{G}} where {G, T}","page":"Grids","text":"materialize(grid) -> NTuple{D,Vector}\n\nEvery cell's coordinates, as one dense vector per direction.\n\nHow to ask a layout whose coordinates are a formula for them as data — for writing a file, or handing them to something that takes point clouds. It allocates D·n numbers, so it is an explicit call.\n\n\n\n\n\n","title":"FlowGeometries.Grids.materialize"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.max_neighbors","page":"Grids","text":"max_neighbors(grid) -> Int\n\nThe widest neighbour count any cell of grid can have, as a compile-time constant. It fixes the tuple width formula_neighbors returns.\n\n\n\n\n\n","title":"FlowGeometries.Grids.max_neighbors"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.maximum_spacing-Tuple{FlowGeometries.Grids.AbstractStructuredGrid, Integer}","page":"Grids","text":"maximum_spacing(grid, d) -> T\n\nLargest gap between consecutive samples along direction d, the counterpart of minimum_spacing. A direction of fewer than two samples reports 0, the identity for max.\n\n\n\n\n\n","title":"FlowGeometries.Grids.maximum_spacing"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.measure-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"measure(grid, I...) -> T\n\nCell measure at index I: length in 1-D, area in 2-D, volume in 3-D, or the node's control-volume size on an unstructured grid. area is the 2-D spelling of the same quantity.\n\n\n\n\n\n","title":"FlowGeometries.Grids.measure"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.measure_array-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"measure_array(grid) -> Array\n\nThe cell measure materialized densely. This is ∏ Nᵈ values — only ask for it when a dense array is genuinely required; measure already indexes and broadcasts.\n\n\n\n\n\n","title":"FlowGeometries.Grids.measure_array"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.measure_array-Union{Tuple{FlowGeometries.Grids.IcosahedralGrid{T, G} where G<:FlowGeometries.Geometry.AbstractSphericalGeometry{T}}, Tuple{T}} where T","page":"Grids","text":"measure_array(grid::IcosahedralGrid) -> Vector\n\nThe dual-cell areas of every vertex, the fan of spherical excesses evaluated once per symmetry class — see _ico_class. The two smallest barycentric parts key a (ν+1)² table.\n\n\n\n\n\n","title":"FlowGeometries.Grids.measure_array"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.measure_factors-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"measure_factors(grid) -> NTuple{N,AbstractVector} or nothing\n\nThe grid's per-axis measure factors when it has them, else nothing. Callers that can exploit separability (a zonal mean weights by one factor only, a global integral is a product of sums) can avoid touching ∏ Nᵈ values at all.\n\n\n\n\n\n","title":"FlowGeometries.Grids.measure_factors"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.minimum_spacing-Tuple{FlowGeometries.Grids.AbstractStructuredGrid, Integer}","page":"Grids","text":"minimum_spacing(grid, d) -> T\n\nSmallest gap between consecutive samples along direction d, as a non-negative magnitude. O(1) when the direction is isuniform and O(N_d) otherwise. With maximum_spacing it bounds how far an index window must reach to cover a given physical distance, as a neighbourhood-by-distance query on a stretched axis needs. The two are also the exact test of whether a stretched axis happens to be equally spaced: its gaps are identical when they are equal.\n\nA direction of fewer than two samples has no gap, and reports Inf — the identity for min.\n\n\n\n\n\n","title":"FlowGeometries.Grids.minimum_spacing"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.ncells-Tuple{FlowGeometries.Grids.CellMesh}","page":"Grids","text":"ncells(mesh) -> Int\n\nHow many cells the mesh has.\n\n\n\n\n\n","title":"FlowGeometries.Grids.ncells"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.ncoordinates-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"ncoordinates(grid) -> Int\n\nHow many coordinate directions a cell of grid has — 2 for (λ, φ), 3 for (x, y, z).\n\nSeparate from counting coordinates(grid) because a layout whose coordinates are a formula stores no arrays to count, and answers this from its parameters instead.\n\n\n\n\n\n","title":"FlowGeometries.Grids.ncoordinates"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.neighbor_nbrs-Tuple{FlowGeometries.Grids.UnstructuredGrid}","page":"Grids","text":"neighbor_nbrs(grid::UnstructuredGrid) -> AbstractVector{<:Integer}\nneighbor_ptr(grid::UnstructuredGrid) -> AbstractVector{<:Integer}\n\nThe CSR adjacency arrays: the flat neighbour indices, and the per-node offsets into them.\n\n\n\n\n\n","title":"FlowGeometries.Grids.neighbor_nbrs"},{"category":"function","location":"api/grids/#FlowGeometries.Grids.neighbors","page":"Grids","text":"neighbors(grid, I...; stencil = Stencils.Axial(1), active_only = true)\n\nThe neighbours of cell I, as a lazy sequence of linear indices that allocates nothing.\n\nWhere they come from is the layout's adjacency_source: index-space offsets, which stencil selects among, or the mesh's own stored incidence, over which a stencil has no meaning.\n\nactive_only applies either way: a masked cell has no neighbours and is nobody's neighbour. See incident_nodes for the unfiltered storage behind a stored graph.\n\n\n\n\n\n","title":"FlowGeometries.Grids.neighbors"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.nlon_in_ring-Tuple{FlowGeometries.Grids.RingGrid, Integer}","page":"Grids","text":"nlon_in_ring(grid::RingGrid, r::Integer) -> Int\n\nPoints in ring r, counted from the north.\n\n\n\n\n\n","title":"FlowGeometries.Grids.nlon_in_ring"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.node_cells-Tuple{FlowGeometries.Grids.CellMesh, Integer}","page":"Grids","text":"node_cells(mesh, i) -> AbstractVector{<:Integer}\n\nThe cells incident on node i, as a view into the mesh's own storage.\n\n\n\n\n\n","title":"FlowGeometries.Grids.node_cells"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.npanels-Tuple{FlowGeometries.Grids.CubedSphereGrid}","page":"Grids","text":"npanels(grid) -> Int\n\nHow many panels the layout has — six for the cubed sphere, two for Yin–Yang.\n\n\n\n\n\n","title":"FlowGeometries.Grids.npanels"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.npixels-Tuple{FlowGeometries.Grids.HEALPixGrid}","page":"Grids","text":"npixels(grid::HEALPixGrid) -> Int\n\n12·nside², the pixelization's own count — which is length(grid) whether or not a mask leaves some of them inactive.\n\n\n\n\n\n","title":"FlowGeometries.Grids.npixels"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.nrings-Tuple{FlowGeometries.Grids.RingGrid}","page":"Grids","text":"nrings(grid::RingGrid) -> Int\n\nHow many iso-latitude rings the grid has.\n\n\n\n\n\n","title":"FlowGeometries.Grids.nrings"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.nside-Tuple{FlowGeometries.Grids.HEALPixGrid}","page":"Grids","text":"nside(grid::HEALPixGrid) -> Int\n\nThe resolution parameter: the grid has 12·nside² pixels on 4·nside − 1 rings.\n\n\n\n\n\n","title":"FlowGeometries.Grids.nside"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.origin-Tuple{FlowGeometries.Grids.AbstractGrid, Integer}","page":"Grids","text":"origin(grid, d) -> T\n\nThe first coordinate along direction d.\n\n\n\n\n\n","title":"FlowGeometries.Grids.origin"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.panel_cell-Tuple{FlowGeometries.Grids.CubedSphereGrid, Integer}","page":"Grids","text":"panel_cell(grid, k) -> (panel, i, j)\n\nWhich panel cell k is on and where on it, the inverse of cell_id.\n\n\n\n\n\n","title":"FlowGeometries.Grids.panel_cell"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.panel_shape-Tuple{FlowGeometries.Grids.YinYangGrid}","page":"Grids","text":"panel_shape(grid::YinYangGrid) -> (nlon, nlat)\n\nThe cell counts across one panel.\n\n\n\n\n\n","title":"FlowGeometries.Grids.panel_shape"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.panel_size-Tuple{FlowGeometries.Grids.CubedSphereGrid}","page":"Grids","text":"panel_size(grid) -> Int\n\nCells across one panel of a panel layout: n for the cubed sphere, whose panels are n × n.\n\n\n\n\n\n","title":"FlowGeometries.Grids.panel_size"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.period-Tuple{FlowGeometries.Grids.UnstructuredGrid, Integer}","page":"Grids","text":"period(grid, d) -> T\n\nWrap length of coordinate direction d, meaningful only where isperiodic holds.\n\n\n\n\n\n","title":"FlowGeometries.Grids.period"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.periodic_flags-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"periodic_flags(grid) -> NTuple{N,Bool}\n\ntopology as one Bool per direction. Const-folds, and unlike indexing the heterogeneous topology tuple it stays type-stable under a runtime direction.\n\n\n\n\n\n","title":"FlowGeometries.Grids.periodic_flags"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.rebuild-Union{Tuple{G}, Tuple{G, NamedTuple}} where G<:FlowGeometries.Grids.AbstractGrid","page":"Grids","text":"rebuild(grid, fields::NamedTuple) -> grid\n\ngrid with the named fields replaced, its type parameters re-derived from what the new fields are.\n\nThe hook a storage change goes through — moving a grid's arrays to a device, rewrapping them — so one generic method serves every layout and no reconstruction elsewhere spells out a parameter list of its own.\n\nfields need only name what changes; everything else is carried over.\n\n\n\n\n\n","title":"FlowGeometries.Grids.rebuild"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.reorder-Union{Tuple{N}, Tuple{G}, Tuple{T}, Tuple{FlowGeometries.Grids.UnstructuredGrid{T, G, N, C, VA, B, TP} where {C<:NTuple{N, AbstractVector{T}}, VA<:AbstractVector{T}, B<:AbstractVector{Bool}, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}}, AbstractVector{<:Integer}}} where {T, G, N}","page":"Grids","text":"reorder(grid, perm) -> UnstructuredGrid\n\ngrid with its nodes in the order perm gives — coordinates, measure and mask permuted, and the adjacency and any CellMesh renumbered to match, so every index the grid reports is an index into the new order.\n\nPair it with spatial_order, and permute your own fields by the same perm: f[perm] is that field on the reordered grid.\n\n\n\n\n\n","title":"FlowGeometries.Grids.reorder"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.ring_of-Tuple{FlowGeometries.Grids.RingGrid, Integer}","page":"Grids","text":"ring_of(grid::RingGrid, i::Integer) -> Int\n\nWhich ring cell i belongs to.\n\nO(log nrings) by bisection of the cumulative counts. A closed form exists for the octahedral rule alone; a tabulated reduced grid's counts are arbitrary, so the search is what serves both.\n\n\n\n\n\n","title":"FlowGeometries.Grids.ring_of"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.ring_range-Tuple{FlowGeometries.Grids.RingGrid, Integer}","page":"Grids","text":"ring_range(grid::RingGrid, r::Integer) -> UnitRange{Int}\n\nThe cells of ring r, as a contiguous slice of the flattened numbering. O(1).\n\n\n\n\n\n","title":"FlowGeometries.Grids.ring_range"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.rotate-Tuple{FlowGeometries.Grids.AbstractStructuredGrid, FlowGeometries.Geometry.PoleRotation}","page":"Grids","text":"rotate(grid::StructuredGrid, rot) -> RotatedGrid\nunrotate(grid::StructuredGrid, rot) -> RotatedGrid\n\nThe same mesh with its coordinates expressed in the other frame of Geometry.PoleRotation rot — unrotate being the usual direction, taking a rotated-pole grid's rectilinear (λ′, φ′) axes to the geographic coordinates of each cell.\n\nA rotated lat–lon mesh is logically rectangular and geometrically warped, and only its own frame's axes are separable. The warping is a formula, so the result stores the mesh and the rotation and evaluates a cell's position where it is asked for; see RotatedGrid. Rotating a grid therefore costs one PoleRotation, against two centre arrays, two corner arrays and a dense measure to materialize it.\n\nTwo things carry over. The cell measure is exact under a rotation, an isometry of the sphere, so it is shared; recomputing it from rotated corners adds round-off. The index topology carries over too: the same mesh has the same neighbours, so a direction that wrapped still wraps, and longitude remains an angle mod 2π in either frame.\n\nTo difference along the mesh's own axes, ask base_grid for them: the frame changes where a cell is reported, leaving the lattice as it is.\n\n\n\n\n\n","title":"FlowGeometries.Grids.rotate"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.rotation-Tuple{FlowGeometries.Grids.RotatedGrid}","page":"Grids","text":"rotation(grid::RotatedGrid) -> PoleRotation\n\nThe frame the mesh is read in.\n\n\n\n\n\n","title":"FlowGeometries.Grids.rotation"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.sampling-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"sampling(grid) -> AbstractSphericalSampling | Nothing\n\nThe node-set recipe grid's axes were built from, or nothing where they were given directly.\n\nCoordinates do not determine it: Gauss–Legendre and an arbitrary lat–lon grid are the same numbers to within round-off, and only the recipe says which quadrature is exact on them. Keeping it means a grid can be asked for its matching weights, or rebuilt at another resolution, after construction.\n\n\n\n\n\n","title":"FlowGeometries.Grids.sampling"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.scheme-Tuple{FlowGeometries.Grids.HEALPixGrid}","page":"Grids","text":"scheme(grid::HEALPixGrid) -> RingScheme\n\nWhich pixel ordering the ids are in — Ring() or Nested().\n\n\n\n\n\n","title":"FlowGeometries.Grids.scheme"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.size_tuple-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"size_tuple(grid) -> NTuple{N,Int}\n\nsize(grid), kept as a named function for call sites that read better spelled out.\n\n\n\n\n\n","title":"FlowGeometries.Grids.size_tuple"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.spacing-Tuple{FlowGeometries.Grids.AbstractGrid, Integer}","page":"Grids","text":"spacing(grid, d) -> T\n\nThe constant spacing of coordinate direction d, available without reading any coordinate. Signed, so a descending axis reports a negative spacing. Raises for a direction that is not isuniform — for a nonuniform one use minimum_spacing / maximum_spacing for its range of gaps, local_spacing for the gaps at one index, or cell_width / cell_widths for the width of a cell.\n\n\n\n\n\n","title":"FlowGeometries.Grids.spacing"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.spacing_trait-Union{Tuple{d}, Tuple{FlowGeometries.Grids.AbstractGrid, Val{d}}} where d","page":"Grids","text":"spacing_trait(grid, ::Val{d}) -> UniformSpacing() | NonuniformSpacing()\n\nWhether direction d's spacing is known from its type, as a value a method can dispatch on.\n\nisuniform answers the same question as a Bool, which a method can only branch on. A kernel that selects the uniform form of an expression — a stencil row that is the same at every interior sample, an O(1) locate — selects it by dispatch, and that needs the direction in the type, hence the Val.\n\nisuniform(grid, d::Integer) serves a direction chosen at run time.\n\n\n\n\n\n","title":"FlowGeometries.Grids.spacing_trait"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.spatial_index-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"spatial_index(grid) -> opaque index\n\nExtension hook: a range-queryable spatial index over the grid's cell centres, overridden by the NearestNeighbors extension. Paired with index_within!.\n\n\n\n\n\n","title":"FlowGeometries.Grids.spatial_index"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.spatial_order-Union{Tuple{FlowGeometries.Grids.UnstructuredGrid{T, G, N, C, VA, B, TP} where {C<:NTuple{N, AbstractVector{T}}, VA<:AbstractVector{T}, B<:AbstractVector{Bool}, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}}}, Tuple{N}, Tuple{G}, Tuple{T}} where {T, G, N}","page":"Grids","text":"spatial_order(grid) -> Vector{Int}\n\nA permutation of the grid's nodes along a Morton (Z-order) curve: perm[k] is the node that belongs in position k.\n\nScattered nodes usually arrive in whatever order they were generated, and a neighbour traversal then jumps across the whole array per edge. Along a space-filling curve, neighbours in space are neighbours in memory, so neighbor_nbrs[ptr[i]:ptr[i+1]-1] reads mostly-adjacent addresses.\n\nThe order is a property of the points: the same set, differently shuffled, sorts to the same sequence. So this repairs an incoherent input order and leaves a coherent one as it is.\n\nThe permutation is returned, and construction does not apply it. The node index is the caller's handle on their own data, and a renumbering here leaves every field they hold pointing at the wrong node. Apply it with reorder and permute those fields the same way.\n\n\n\n\n\n","title":"FlowGeometries.Grids.spatial_order"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.topology-Tuple{FlowGeometries.Grids.AbstractGrid}","page":"Grids","text":"topology(grid) -> NTuple{N,AbstractTopology}\ntopology(grid, d) -> AbstractTopology\n\nThe grid's per-direction topology. Singletons, so this occupies no storage.\n\n\n\n\n\n","title":"FlowGeometries.Grids.topology"},{"category":"method","location":"api/grids/#FlowGeometries.Grids.unrotate-Tuple{FlowGeometries.Grids.AbstractStructuredGrid, FlowGeometries.Geometry.PoleRotation}","page":"Grids","text":"unrotate(grid::StructuredGrid, rot) -> RotatedGrid\n\nrotate the other way: the mesh's axes are the rotated frame's, and each cell is reported at its geographic position. The usual direction for a rotated-pole grid.\n\n\n\n\n\n","title":"FlowGeometries.Grids.unrotate"},{"category":"section","location":"api/#API-Reference","page":"Overview","text":"Nothing is exported and nothing is rebound at the package's top level: every name is reached through the submodule that defines it.\n\nusing FlowGeometries: FlowGeometries as FG\n\nOne page per module — the reference is large enough that a single page is slow to load, and the split keeps each module's surface readable on its own. The index below spans all of them.","title":"API Reference"},{"category":"section","location":"api/#Index","page":"Overview","text":"Pages = [\"api/axes.md\", \"api/geometry.md\", \"api/sampling.md\", \"api/stencils.md\",\n         \"api/discretization.md\", \"api/grids.md\", \"api/connectivity.md\", \"api/operators.md\",\n         \"api/execution.md\"]","title":"Index"},{"category":"module","location":"api/#FlowGeometries.FlowGeometries","page":"Overview","text":"FlowGeometries\n\nCoordinate metrics, spherical samplings, and grid types.\n\nAbstractGeometry\n├── AbstractCartesianGeometry  ← CartesianGeometry\n└── AbstractSphericalGeometry  ← SphericalGeometry\n\nAbstractSphericalSampling\n├── … spectral / lat-lon / HEALPix / cubed-sphere / icosahedral / Yin–Yang / scattered …\n\nAbstractGrid\n├── AbstractStructuredGrid     ← StructuredGrid\n├── AbstractCurvilinearGrid    ← CurvilinearGrid\n└── AbstractUnstructuredGrid   ← UnstructuredGrid\n\nNothing is exported and nothing is rebound at the top level: every name is reached through the submodule that defines it.\n\nusing FlowGeometries: FlowGeometries as FG\n\ngeo  = FG.Geometry.SphericalGeometry()\ngrid = FG.Connectivity.structured_grid(FG.SphericalSampling.GaussLegendreSampling(), 64)\nFG.Grids.coords(grid, 2, 3)                  # (λ=, φ=)\nFG.Geometry.distance(geo, p1, p2)\n\nSubmodules: Axes (coordinate axes and the spacing trait), Geometry (metrics), Stencils (neighbourhood shapes), Discretization (location, interpolation, staggering, derivative weights), SphericalSampling (where the points go), Grids (storage), Connectivity (topology), Operators (everything that reads or writes a field), Execution (how bulk loops run).\n\n\n\n\n\n","title":"FlowGeometries.FlowGeometries"},{"category":"section","location":"geometry/#geometry-page","page":"Geometry","text":"A geometry answers one question: what is the metric? It carries no points and no topology — only how to measure distance, area and volume, and what the coordinate directions are called.\n\nusing FlowGeometries: FlowGeometries as FG\n\ncart = FG.Geometry.CartesianGeometry()          # flat metric; carries no spacing\nsph  = FG.Geometry.SphericalGeometry()          # default Earth radius\nunit = FG.Geometry.SphericalGeometry(1.0)       # unit sphere\nwgs  = FG.Geometry.SpheroidGeometry()           # WGS 84 oblate spheroid","title":"Geometry"},{"category":"section","location":"geometry/#Element-type","page":"Geometry","text":"A geometry is parameterized by the float type it computes in, and that type fixes the width of everything expressed against it — coordinates, distances, metric factors. float_type reads it and similar_geometry carries a geometry to another width, keeping its shape:\n\nFG.Geometry.float_type(sph), FG.Geometry.similar_geometry(Float32, wgs)\n\nAnything that builds at a chosen width takes the type first, positionally, as zeros and rand do, so it takes part in dispatch and the result's element type is known from the signature. Where a constructor takes both a width and a geometry, the width wins and the geometry is carried to it; where only a geometry is given, its own width is the one meant:\n\ng32 = FG.Connectivity.structured_grid(Float32, FG.SphericalSampling.GaussLegendreSampling(), 16)\neltype(FG.Grids.axis(g32, 1)), FG.Geometry.float_type(FG.Grids.grid_geometry(g32))","title":"Element type"},{"category":"section","location":"geometry/#Coordinate-names","page":"Geometry","text":"The geometry decides what a point's components are called, and the grid types inherit that. Cartesian is (x, y[, z]); spherical is (λ, φ[, r]) — longitude, geographic latitude, radius.\n\nThe names are enforced: grid.x on a spherical grid raises a FieldError. Reading x and getting longitude is the kind of bug that survives code review and shows up as a wrong answer months later.\n\nFG.Grids.coords(grid, 2, 3)          # (λ = …, φ = …) on a spherical grid\nFG.Grids.coordinate_names(grid)      # (:λ, :φ)","title":"Coordinate names"},{"category":"section","location":"geometry/#Distance","page":"Geometry","text":"distance is great-circle on a sphere and Euclidean on a plane, and takes points in whatever representation you have — tuples, NamedTuples, or (with the StaticArrays extension) static vectors.\n\nFG.Geometry.distance(sph, (0.0, 0.0), (π/2, 0.0))     # quarter of the equator\n\nThe spherical version uses the haversine form, which stays accurate for nearby points where the law-of-cosines form loses precision to cancellation.","title":"Distance"},{"category":"section","location":"geometry/#Cell-measures","page":"Geometry","text":"FG.Geometry.area_element(sph, φ, Δλ, Δφ)              # R²·cosφ·Δλ·Δφ\nFG.Geometry.volume_element(sph, r, φ, Δλ, Δφ, Δr)     # r²·cosφ·Δλ·Δφ·Δr\nFG.Geometry.area_element(cart, 2.0, 3.0)              # dx·dy, from the cell's own extents\n\nThese are the pointwise elements. A grid's per-cell measure is built from them once at construction — see Grids.","title":"Cell measures"},{"category":"section","location":"geometry/#Cartesian-spherical","page":"Geometry","text":"p = FG.Geometry.spherical_to_cartesian(sph, (λ, φ))   # (x = …, y = …, z = …)\nFG.Geometry.cartesian_to_spherical(sph, (p.x, p.y, p.z))   # (λ = …, φ = …, r = …)\n\nVectors transform differently from points, because a vector at a point is expressed in that point's local frame. Those have their own pair:\n\n# components first, then the position they are expressed at\nv = FG.Geometry.vector_to_cartesian(sph, 1.0, -0.5, 0.0, λ, φ)   # (u_λ, u_φ, u_r) → (x, y, z)\nFG.Geometry.vector_from_cartesian(sph, v.x, v.y, v.z, λ, φ)      # and back","title":"Cartesian ↔ spherical"},{"category":"section","location":"geometry/#Tangent-plane-geometry","page":"Geometry","text":"ê = FG.Geometry.local_tangent_basis(sph, (λ, φ))     # (; λ = ê_λ, φ = ê_φ)\nFG.Geometry.project_to_tangent_plane(sph, (λ, φ), (λ + 1e-6, φ))\n\nproject_to_tangent_plane gives a neighbour's offset in the tangent plane at centre: the quantity a finite-difference or structure-function calculation on a sphere differences against.","title":"Tangent-plane geometry"},{"category":"section","location":"geometry/#Nonuniform-derivatives","page":"Geometry","text":"FG.Geometry.nonuniform_first_derivative(1.0, 0.0, 4.0, 1.0, 2.0)\n\nSecond-order accurate on an unequally spaced stencil — the usual three-point formula degrades to first order when h₋ ≠ h₊, which is the common case on a stretched grid.","title":"Nonuniform derivatives"},{"category":"section","location":"geometry/#A-spheroid-drives-the-whole-stack","page":"Geometry","text":"SpheroidGeometry overrides distance (Vincenty), area_element, volume_element and scale_factors; the grid, sampling and connectivity layers are inherited.\n\nFG.Geometry.distance(wgs, (0.0, 0.0), (0.0, π/2))    # quarter meridian, WGS 84\n\nFG.Geometry.prime_vertical_radius(wgs, 0.0), FG.Geometry.meridional_radius(wgs, π/2)\n\nGrid directions are (λ, φ, h), with h the height above the ellipsoid — not the absolute radius that a spherical grid's third direction carries.\n\nλ = range(0, 2π; length = 9)[1:8]\nφ = range(-π/2, π/2; length = 7)\ngs = FG.Grids.StructuredGrid(wgs, λ, φ)\nFG.Grids.coordinate_names(gs), FG.Grids.isperiodic(gs, 1), size(gs)\n\nThe surface element M(φ)·N(φ)cosφ·Δλ·Δφ factors, so the measure stays separable and an interior cell matches the geometry's own element exactly:\n\nΔλ, Δφ = FG.Grids.spacing(gs, 1), FG.Grids.spacing(gs, 2)\nFG.Grids.measure(gs, 3, 4) ≈ FG.Geometry.area_element(wgs, φ[4], Δλ, Δφ)\n\nAdding a height direction changes that. The geodetic volume element offsets both curvature radii by h, coupling φ and h, so no product of per-axis factors reproduces it and the measure is stored dense — which measure_factors reports by returning nothing:\n\ng3 = FG.Grids.StructuredGrid(wgs, λ, φ, range(0.0, 2000.0; length = 3))\nFG.Grids.measure_factors(gs) !== nothing, FG.Grids.measure_factors(g3) === nothing\n\nΔh = FG.Grids.spacing(g3, 3)\nFG.Grids.measure(g3, 3, 4, 2) ≈ FG.Geometry.volume_element(wgs, φ[4], 1000.0, Δλ, Δφ, Δh)","title":"A spheroid drives the whole stack"},{"category":"section","location":"geometry/#Adding-a-geometry","page":"Geometry","text":"Subtype AbstractCartesianGeometry, AbstractSphericalGeometry or AbstractEllipsoidalGeometry and you inherit the whole grid, sampling and connectivity stack. Each hierarchy asks for its shape parameters through accessors, so that is all a new geometry has to supply — a sphere defines radius, an ellipsoid semimajor_axis and flattening, and no method reads a field, so store them however you like:\n\nstruct UnitSphere{T} <: FG.Geometry.AbstractSphericalGeometry{T} end\nFG.Geometry.radius(::UnitSphere{T}) where {T} = one(T)\n\nu = UnitSphere{Float64}()\ngu = FG.Grids.StructuredGrid(u, λ, φ)\nsum(FG.Grids.measure(gu)), FG.Geometry.distance(u, (0.0, 0.0), (0.0, π/2))\n\nThat total is the unit sphere's area to the discretization's accuracy, and the distance is a quarter great circle — both from the one method above.","title":"Adding a geometry"},{"category":"section","location":"geometry/#Rotated-frames","page":"Geometry","text":"rot = FG.Geometry.PoleRotation(0.7, 0.3)     # the frame whose north pole is at (0.7, 0.3)\nFG.Geometry.rotate(rot, 0.7, 0.3)            # that pole maps to φ = π/2\n\nFG.Geometry.unrotate(rot, FG.Geometry.rotate(rot, 1.2, -0.4)...)   # round-trips\n\nA whole point set rotates in place — the shape a sampling's spherical_points output has — and the array forms allocate nothing:\n\nλs = [0.1, 1.2, 3.0, 5.5]\nφs = [0.0, -0.4, 0.9, 0.2]\nFG.Geometry.rotate!(λs, φs, rot)\nλs\n\nRotating a rectilinear spherical grid warps it, and only its own frame's axes stay separable. The warping is a formula, so the result keeps the mesh and the rotation and evaluates a cell's position where it is asked for. unrotate is the usual direction, taking a rotated-pole grid's (λ′, φ′) axes to the geographic coordinates of each cell.\n\nsph = FG.Geometry.SphericalGeometry()\nλr = range(0, 2π; length = 25)[1:24]\nφr = range(-1.2, 1.2; length = 13)\ngrot = FG.Grids.unrotate(FG.Grids.StructuredGrid(sph, λr, φr), rot)\ntypeof(grot).name.name, size(grot), FG.Grids.coordinate_names(grot)\n\nThe cell measure carries over exactly, a rotation being an isometry of the sphere, so the array is shared; recomputing it from the rotated corners adds roundoff. The index topology carries over too: same mesh, same neighbours, so a direction that wrapped still wraps.\n\ngplain = FG.Grids.StructuredGrid(sph, λr, φr)\nFG.Grids.measure(grot, 3, 4) == FG.Grids.measure(gplain, 3, 4),\nFG.Grids.isperiodic(grot, 1)\n\nSo rotating costs one PoleRotation, where materializing the warp cost two centre arrays, two corner arrays and a dense copy of that measure:\n\nBase.summarysize(grot) - Base.summarysize(gplain), sizeof(rot)","title":"Rotated frames"},{"category":"section","location":"api/operators/#Operators","page":"Operators","text":"","title":"Operators"},{"category":"type","location":"api/operators/#FlowGeometries.Operators.AbstractMaskPolicy","page":"Operators","text":"AbstractMaskPolicy\n\nWhat apply_stencil! does at the edge of the active region: BlankMasked, ShiftWithinRun or ReduceInRun.\n\nA type, like the image and reach conventions in Connectivity: which cells carry a number and which carry masked is visible in the call and fixed at compile time.\n\n\n\n\n\n","title":"FlowGeometries.Operators.AbstractMaskPolicy"},{"category":"type","location":"api/operators/#FlowGeometries.Operators.BlankMasked","page":"Operators","text":"BlankMasked()\n\nWrite masked at a cell that is inactive or whose stencil reads an inactive cell. The default, and the only policy that never invents a value: where the stencil cannot be formed from active data, there is no derivative.\n\nIts cost is a dead band. Every active cell within nodes - 1 of a masked cell is blanked, so a five-point derivative loses two cells either side of every coastline.\n\n\n\n\n\n","title":"FlowGeometries.Operators.BlankMasked"},{"category":"type","location":"api/operators/#FlowGeometries.Operators.GradientPlan","page":"Operators","text":"GradientPlan\n\nThe two index buffers are typed independently and hold any Integer, so a plan built off a CSR keeps that CSR's own width — see Connectivity._index_type.\n\nThe geometry of a least-squares gradient, separated from any field: for each cell, the coefficient on each neighbour's difference from it, once per coordinate direction. Built by Connectivity.gradient_plan, applied by gradient!.\n\nD is the number of directions the fit resolves, which is the grid's coordinate count: two on a surface — a (λ, φ) or (x, y) mesh — and three in a volume, where the neighbourhood spans a solid angle.\n\napply_stencil! covers the separable case. A CurvilinearGrid has no separable axis to build a scalar stencil along, and a node set's neighbours come from connectivity, so this is the gradient for both.\n\nThe construction, with tangent-plane displacements Δrₖ from Geometry.project_to_tangent_plane, differences Δfₖ = fₖ - f₀ and weights wₖ = 1/|Δrₖ|²: minimising Σₖ wₖ (∇f·Δrₖ - Δfₖ)² gives A ∇f = b with A = Σₖ wₖ Δrₖ⊗Δrₖ and b = Σₖ wₖ Δrₖ Δfₖ. A depends only on the geometry, so it is inverted once here and the per-neighbour coefficients A⁻¹ wₖ Δrₖ are what the plan stores; each apply is then one dot product per cell and allocates nothing.\n\nWhat this buys over inverting an index-space Jacobian:\n\nexact for a linear field on any stencil, however skewed — the least-squares combination cancels the leading truncation term, which a 2×2 Jacobian inverse does not;\nsecond order on locally symmetric stencils, degrading toward first on strongly skewed cells;\nit reduces to the centred difference where the stencil is separable and orthogonal (A diagonal), so it agrees with apply_stencil! where both apply.\n\nWhere A is rank deficient — a direction with no data, at a boundary or beside a mask — the pseudo-inverse zeroes that component, under the rule apply_stencil! states for a mask: a value the active data does not determine is not produced.\n\n\n\n\n\n","title":"FlowGeometries.Operators.GradientPlan"},{"category":"type","location":"api/operators/#FlowGeometries.Operators.ReduceInRun","page":"Operators","text":"ReduceInRun()\n\nShiftWithinRun, and where the run cannot hold nodes, use the largest window it can, down to order + 1 samples. masked below that, where no derivative of that order exists.\n\nThis trades accuracy order for coverage — a five-point scheme becomes three-point in a strait three cells wide — so it carries its own name. Ask for it when a value everywhere matters more than a uniform order.\n\nUnder this policy nodes is a ceiling, at the end of the axis as well as the end of a run: an axis with fewer than nodes samples uses as many as it has, and one with fewer than order + 1 is masked throughout. A single-latitude strip, a two-level column and a one-cell-wide channel are ordinary grids, and this asks for \"second order where the axis allows it\" with no clamping of nodes at the call site. The other two policies raise there, neither of them degrading.\n\n\n\n\n\n","title":"FlowGeometries.Operators.ReduceInRun"},{"category":"type","location":"api/operators/#FlowGeometries.Operators.ShiftWithinRun","page":"Operators","text":"ShiftWithinRun()\n\nShift the stencil to fit inside the run of active samples containing the cell, keeping the full node count — the same thing the stencil already does at the end of a bounded axis, with the end of the active run as the boundary. masked only where the run is shorter than nodes.\n\nThe accuracy order is therefore the same everywhere a value is written, as it is under Discretization.fd_weights's inward shift at a bounded end. On a run of at least nodes active samples the weights are identical to the unmasked ones, so the interior of an active region is bit-for-bit unchanged.\n\n\n\n\n\n","title":"FlowGeometries.Operators.ShiftWithinRun"},{"category":"method","location":"api/operators/#FlowGeometries.Discretization.axis_stencils-Union{Tuple{N}, Tuple{T}, Tuple{G}, Tuple{FlowGeometries.Grids.StructuredGrid{T, G, N, S, TP, C, AT, BT} where {S, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractVector{T}}, AT<:AbstractArray{T, N}, BT<:AbstractArray{Bool, N}}, Integer}} where {G, T, N}","page":"Operators","text":"Discretization.axis_stencils(grid, dim; order=1, nodes=order+1) -> (indices, weights)\n\nDiscretization.axis_stencils for direction dim of grid, taking that direction's axis and wrap period from the grid.\n\nThe table depends on the grid alone, so a caller differencing many fields along the same direction builds it once and hands it to the (out, field, grid, indices, weights, dim) form. The (out, field, grid, dim) form above rebuilds it on every call.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.axis_stencils"},{"category":"method","location":"api/operators/#FlowGeometries.Discretization.stencil_plan-Union{Tuple{N}, Tuple{T}, Tuple{G}, Tuple{FlowGeometries.Grids.StructuredGrid{T, G, N, S, TP, C, AT, BT} where {S, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractVector{T}}, AT<:AbstractArray{T, N}, BT<:AbstractArray{Bool, N}}, Integer}} where {G, T, N}","page":"Operators","text":"Discretization.stencil_plan(grid, dim; order=1, nodes=order+1) -> AbstractStencilPlan\n\nDiscretization.stencil_plan for direction dim of grid, taking that direction's axis and wrap period from the grid.\n\nThe form to hold in a loop over fields. Which plan it is follows the axis's own spacing, so a uniform direction gets the register-resident weights and a stretched one the table.\n\n\n\n\n\n","title":"FlowGeometries.Discretization.stencil_plan"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._bary_weights-Union{Tuple{T}, Tuple{Any, Any, Tuple{T, T}, Int64, Bool}} where T","page":"Operators","text":"_bary_weights(grid, geo, p0, nearest, active_only) -> (a, b, c, (w₁, w₂, w₃))\n\nThe cell of the grid's Grids.CellMesh containing p0, and the barycentric weights of p0 in it. a == 0 where there is none — outside the mesh, or against an inactive node.\n\nTrying the cells incident on nearest is exhaustive: p0 lies in the Voronoi cell of its nearest node, and in a Delaunay triangulation that region is covered by the triangles incident on that node.\n\nThe weights are computed in the tangent plane at p0, where the three vertices sit at their Geometry.local_displacements and p0 itself is the origin.\n\n\n\n\n\n","title":"FlowGeometries.Operators._bary_weights"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._curl_location-Union{Tuple{N}, Tuple{T}, Tuple{FlowGeometries.Grids.StaggeredGrid{T, N, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:NTuple{N, AbstractVector{T}}}, Int64}} where {T, N}","page":"Operators","text":"_curl_location(sg, i) -> NTuple{N,AbstractLocation}\n\nWhere (∇×u)_i lives: Discretization.Center in direction i and Discretization.Face in every other — the vorticity point of an Arakawa C cell for that component. In two directions the scalar curl is the i = 3 case, giving (Face, Face), the corner.\n\n\n\n\n\n","title":"FlowGeometries.Operators._curl_location"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._face_span-Tuple{Int64, Bool}","page":"Operators","text":"_face_span(nc, periodic) -> Int\n\nHow many samples a direction of nc cells carries at Discretization.Face: nc where it wraps, its last face being its first, and nc + 1 where it does not.\n\n\n\n\n\n","title":"FlowGeometries.Operators._face_span"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._fusable-NTuple{4, Any}","page":"Operators","text":"_fusable(out, field, geo, mask) -> Bool\n\nWhether the metric factor can be applied inside the sweep, which needs it to be constant across each contiguous direction-1 run the sweep writes.\n\nFor a geometry that declares direction 1 metric-invariant the factor depends on directions 2:N, so it is constant on every such run, whichever direction is differenced — the sweep scales run by run. A Cartesian metric is the identity and has nothing to apply, and the run addressing needs the linear layout.\n\n\n\n\n\n","title":"FlowGeometries.Operators._fusable"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._fuse_scale!-Tuple{AbstractArray, AbstractVector, Int64, Int64, Int64, Int64, Int64, Any}","page":"Operators","text":"_fuse_scale!(out, invh, R, start, nruns, runlen, rbase, masked) -> nothing\n\nScale a span the sweep has just written, run by run, while it is still in cache.\n\nThe span holds nruns contiguous runs of runlen cells along direction 1, and the metric factor is constant on each of them, direction 1 being metric-invariant. invh[1:R] holds one factor per spatial run in column-major order over directions 2:N, and a batch element repeats that cycle, so the run number is taken modulo R. rbase is the span's first run, counted the same way.\n\ninvh comes from _metric_scratch and is held across calls, so it may be longer than R: the count is the argument, never length(invh).\n\n\n\n\n\n","title":"FlowGeometries.Operators._fuse_scale!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._mask_may_hide-Tuple{Any, FlowGeometries.Operators.AbstractMaskPolicy, Bool}","page":"Operators","text":"_mask_may_hide(grid, policy, active_only) -> Bool\n\nWhether the neighbourhood test has anything to find: under BlankMasked an active-only k_nearest may have skipped an inactive cell nearer than the farthest it kept, and the test is a second range query over the same index. On an Grids.AllActive grid there is no inactive cell to skip, so the answer is known without the query.\n\n\n\n\n\n","title":"FlowGeometries.Operators._mask_may_hide"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._metric_row_factors!-Union{Tuple{dim}, Tuple{N}, Tuple{T}, Tuple{G}, Tuple{AbstractVector{T}, FlowGeometries.Grids.StructuredGrid{T, G, N, S, TP, C, AT, BT} where {S, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractVector{T}}, AT<:AbstractArray{T, N}, BT<:AbstractArray{Bool, N}}, Val{dim}, Val{N}}} where {G, T, N, dim}","page":"Operators","text":"_metric_row_factors!(buf, grid, Val(dim), Val(N)) -> Int\n\nWrite the inverse scale factor of every direction-1 run into buf, in column-major order over directions 2:N — the order the sweep writes them in, for any differenced direction. Returns how many.\n\nDirection 1 is metric-invariant here (see _fusable), so one factor covers a whole run and any axis-1 coordinate serves as the point's first component.\n\nA degenerate factor — one at or below the geometry's Discretization.metric_floor — is stored as zero, and _scale_span! blanks a run whose factor is zero.\n\n\n\n\n\n","title":"FlowGeometries.Operators._metric_row_factors!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._metric_scratch-Union{Tuple{T}, Tuple{Type{T}, Int64}} where T","page":"Operators","text":"_metric_scratch(T, n) -> Vector{T}\n\nA task-held buffer of at least n factors, as Connectivity.ball_scratch is for a ball query.\n\nThe fused sweeps are serial and call no user code, so one buffer per task is enough; it grows to the largest grid the task has swept and is refilled per call.\n\n\n\n\n\n","title":"FlowGeometries.Operators._metric_scratch"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._nodecount-Union{Tuple{Val{k}}, Tuple{k}} where k","page":"Operators","text":"_nodecount(nodes) -> Int\n\nThe node count, from either a Val or a plain Int.\n\nOne body then serves both. With a Val the trip count is a literal, so the loop unrolls and the weights reach registers; with an Int, the form a node count above the specialized set takes, it is an ordinary loop.\n\n\n\n\n\n","title":"FlowGeometries.Operators._nodecount"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._plan_sweep_host!-Union{Tuple{M}, Tuple{dim}, Tuple{K}, Tuple{T}, Tuple{N}, Tuple{S}, Tuple{AbstractArray{S, N}, Any, FlowGeometries.Discretization.UniformStencilPlan{T, K, MI, MW} where {MI<:(AbstractMatrix{<:Integer}), MW<:AbstractMatrix{T}}, Any, Any, Val{dim}, Val{N}, Val{M}}, Tuple{AbstractArray{S, N}, Any, FlowGeometries.Discretization.UniformStencilPlan{T, K, MI, MW} where {MI<:(AbstractMatrix{<:Integer}), MW<:AbstractMatrix{T}}, Any, Any, Val{dim}, Val{N}, Val{M}, Any}, Tuple{AbstractArray{S, N}, Any, FlowGeometries.Discretization.UniformStencilPlan{T, K, MI, MW} where {MI<:(AbstractMatrix{<:Integer}), MW<:AbstractMatrix{T}}, Any, Any, Val{dim}, Val{N}, Val{M}, Any, Int64}} where {S, N, T, K, dim, M}","page":"Operators","text":"_plan_sweep_host!(out, field, plan, mask, masked, Val(dim), Val(N), Val(M), invh, R) -> out\n\nThe host sweep for a plan. Same nest as _stencil_sweep_host! — Cartesian range walked directly, nest split at dim, node count in the type — with a uniform plan's interior row a tuple in registers and the shifted end rows read from its own O(K²) table.\n\ninvh fuses the metric: each span is scaled as it is written, while it is in cache, saving the second full pass over out a separate _scale_by_metric! costs. Pass nothing for the plain sweep.\n\n\n\n\n\n","title":"FlowGeometries.Operators._plan_sweep_host!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._scale_span!-Union{Tuple{S}, Tuple{AbstractArray{S}, Int64, Int64, Any, Any}} where S","page":"Operators","text":"_scale_span!(out, start, len, inv_h, masked) -> nothing\n\nMultiply the contiguous span out[start:start+len-1] by inv_h, or write masked across it when inv_h is zero — which is how a degenerate scale factor reaches here, derivative! having compared it against the geometry's own Discretization.metric_floor.\n\n\n\n\n\n","title":"FlowGeometries.Operators._scale_span!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._stencil_sweep_host!-Union{Tuple{M}, Tuple{dim}, Tuple{N}, Tuple{S}, Tuple{AbstractArray{S, N}, Any, Any, Any, Any, Any, Val{dim}, Any, Val{N}, Val{M}}, Tuple{AbstractArray{S, N}, Any, Any, Any, Any, Any, Val{dim}, Any, Val{N}, Val{M}, Any}, Tuple{AbstractArray{S, N}, Any, Any, Any, Any, Any, Val{dim}, Any, Val{N}, Val{M}, Any, Int64}} where {S, N, dim, M}","page":"Operators","text":"_stencil_sweep_host!(out, field, indices, weights, mask, masked, Val(dim), nodes, Val(N),\n                     Val(M), invh, R) -> out\n\nThe host sweep. The index-parallel form serves a device launch; this one adds three things a per-work-item body cannot express:\n\niterate the Cartesian range directly, with no index recovered per cell from a linear one;\nsplit the nest at dim, hoisting the stencil row — which depends only on the index along dim — out of the contiguous inner loop wherever the differenced direction is a slower-varying one;\ncarry the node count in the type, so the innermost loop has a known trip count, unrolls, and holds the weights in registers.\n\nThe arithmetic and its order are identical to _stencil_cell!, so the two paths agree bit for bit.\n\ninvh fuses the metric factor into the sweep — see _fuse_scale!. Pass nothing for the plain sweep.\n\n\n\n\n\n","title":"FlowGeometries.Operators._stencil_sweep_host!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._sym_eigvals3-Union{Tuple{T}, Tuple{Tuple{Tuple{T, T, T}, Tuple{T, T, T}, Tuple{T, T, T}}, T}} where T","page":"Operators","text":"_sym_eigvals3(A, tol) -> (λ₁, λ₂, λ₃)\n\nEigenvalues of a symmetric 3×3, largest first, by the closed form for the characteristic cubic: shift by the mean eigenvalue, scale, and read the three roots off cos at thirds of a turn.\n\nEigenvalues only. A closed-form eigenvector of a 3×3 loses accuracy when two eigenvalues are close, and _sympinv3 needs none: a pseudo-inverse of a symmetric matrix is a polynomial in that matrix, and its coefficients are functions of the eigenvalues alone.\n\n\n\n\n\n","title":"FlowGeometries.Operators._sym_eigvals3"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._sympinv-Union{Tuple{T}, Tuple{Tuple{Tuple{T, T}, Tuple{T, T}}, T}} where T","page":"Operators","text":"_sympinv(A::NTuple{D,NTuple{D,T}}, tol) -> NTuple{D,NTuple{D,T}}\n\nPseudo-inverse of a symmetric positive-semidefinite A given as its rows, dropping any eigendirection whose eigenvalue is at or below tol. D = 2 and D = 3, which are the tangent plane and the volume.\n\nA direction is dropped, never regularised — see _sympinv2, which this calls at D = 2.\n\n\n\n\n\n","title":"FlowGeometries.Operators._sympinv"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._sympinv2-Union{Tuple{T}, NTuple{4, T}} where T","page":"Operators","text":"_sympinv2(a, b, c, tol) -> (p11, p12, p22)\n\nPseudo-inverse of the symmetric 2×2 [a b; b c], dropping any eigendirection whose eigenvalue is below tol. Closed form: a 2×2 symmetric eigenproblem has one.\n\nA direction is dropped, never regularised. A stencil that carries no information along some tangent direction — every neighbour on one line, which happens at a boundary and beside a mask — leaves A singular there, and the data does not determine that component of the gradient. Inverting a nudged matrix answers with a number governed by the nudge.\n\n\n\n\n\n","title":"FlowGeometries.Operators._sympinv2"},{"category":"method","location":"api/operators/#FlowGeometries.Operators._sympinv3-Union{Tuple{T}, Tuple{Tuple{Tuple{T, T, T}, Tuple{T, T, T}, Tuple{T, T, T}}, T}} where T","page":"Operators","text":"_sympinv3(A, tol) -> NTuple{3,NTuple{3,T}}\n\nPseudo-inverse of a symmetric positive-semidefinite 3×3.\n\nAt every rank A⁺ is a polynomial in A with no constant term, so this is closed form and needs no eigenvector. Such a polynomial annihilates the null space and acts as 1/λ on the range, which is the Moore–Penrose conditions:\n\nrank 3: adj(A)/det(A), the ordinary inverse;\nrank 2: αA² + βA with α = -(λ₁+λ₂)/(λ₁λ₂)² and β = (λ₁² + λ₁λ₂ + λ₂²)/(λ₁λ₂)²;\nrank 1: A/λ₁², since A = λ₁vvᵀ there;\nrank 0: zero.\n\n\n\n\n\n","title":"FlowGeometries.Operators._sympinv3"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.apply_stencil!-Union{Tuple{NA}, Tuple{N}, Tuple{T}, Tuple{G}, Tuple{S}, Tuple{AbstractArray{S, NA}, AbstractArray{<:Any, NA}, FlowGeometries.Grids.StructuredGrid{T, G, N, S, TP, C, AT, BT} where {S, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractVector{T}}, AT<:AbstractArray{T, N}, BT<:AbstractArray{Bool, N}}, AbstractMatrix{<:Integer}, AbstractMatrix, Integer}} where {S, G, T, N, NA}","page":"Operators","text":"apply_stencil!(out, field, grid, indices, weights, dim; order=1, active_only=true,\n               masked=zero, policy=BlankMasked(), backend=nothing) -> out\n\nApply a stencil table built by Discretization.axis_stencils. The mask, the wrap period and the axis all come from grid.\n\nThe axis coming too, any mask policy works here. The bare (indices, weights) form has no axis to rebuild a window from at a mask edge, so it accepts only BlankMasked.\n\nThis is the form to use in a loop over fields: the table is the same for all of them, and it is the one part of the work that depends on the grid alone.\n\n\n\n\n\n","title":"FlowGeometries.Operators.apply_stencil!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.apply_stencil!-Union{Tuple{NA}, Tuple{N}, Tuple{T}, Tuple{G}, Tuple{S}, Tuple{AbstractArray{S, NA}, AbstractArray{<:Any, NA}, FlowGeometries.Grids.StructuredGrid{T, G, N, S, TP, C, AT, BT} where {S, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractVector{T}}, AT<:AbstractArray{T, N}, BT<:AbstractArray{Bool, N}}, Integer}} where {S, G, T, N, NA}","page":"Operators","text":"apply_stencil!(out, field, grid, dim; order=1, nodes=order+1, active_only=true, masked=zero) -> out\n\napply_stencil! with the axis, wrap period and mask taken from grid, so a periodic direction wraps and an inactive cell is honoured without restating any of it.\n\nOnly a rectilinear direction has a 1-D axis to difference along, so this is a StructuredGrid method.\n\n\n\n\n\n","title":"FlowGeometries.Operators.apply_stencil!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.apply_stencil!-Union{Tuple{N}, Tuple{S}, Tuple{AbstractArray{S, N}, AbstractArray{<:Any, N}, AbstractVector{<:AbstractFloat}, AbstractMatrix{<:Integer}, AbstractMatrix, Integer}} where {S, N}","page":"Operators","text":"apply_stencil!(out, field, x, indices, weights, dim; order=1, period=nothing, mask=nothing,\n               masked=zero, policy=BlankMasked(), backend=nothing) -> out\n\nApply a table built by Discretization.axis_stencils and keep the axis, so any mask policy works.\n\nThe table depends on the axis alone, so a caller differencing many fields along one direction builds it once. Degrading at a mask edge needs the axis to rebuild a window from, so the bare (indices, weights) form accepts only BlankMasked; this form takes both and serves every policy.\n\nThe split is the one the degrade path makes internally: the precomputed row is used wherever the window is intact, which is every cell away from a mask, and the axis is touched only where a window is rebuilt.\n\nBuilding the table is O(n) against an O(n²) apply, so holding it across fields matters most on small grids. It also removes an O(n · nodes) allocation from every call, at any size.\n\n\n\n\n\n","title":"FlowGeometries.Operators.apply_stencil!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.apply_stencil!-Union{Tuple{N}, Tuple{S}, Tuple{AbstractArray{S, N}, AbstractArray{<:Any, N}, AbstractVector{<:AbstractFloat}, Integer}} where {S, N}","page":"Operators","text":"apply_stencil!(out, field, x, dim; order=1, nodes=order+1, period=nothing,\n               mask=nothing, masked=zero) -> out\napply_stencil!(out, field, indices, weights, dim; mask=nothing, masked=zero) -> out\n\nApply a weight set along direction dim of field, writing out[I] = Σ_q weights[I[dim], q] · field[…, indices[I[dim], q], …].\n\nEvery convention this needs is fixed by the package: the result sits at the same location as the input, so there is no staggering decision, and the stencil shifts inward at a bounded end and wraps on a periodic one, which is Discretization.fd_weights's stated boundary behaviour and removes the need for a halo. Operations that do take those choices live elsewhere — a staggered difference, or a multi-direction operator like a divergence or a curl, each of which needs a result location and a boundary-condition policy.\n\nPass the axis and an order to have the weights built for you, or precomputed indices/weights from Discretization.axis_stencils to reuse them across many fields.\n\nWith a mask, a cell is written as masked when it is inactive or when its stencil reads an inactive cell — the derivative there is not determined by the active data, so it is not invented. out and field may not alias.\n\n\n\n\n\n","title":"FlowGeometries.Operators.apply_stencil!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.apply_stencil!-Union{Tuple{N}, Tuple{S}, Tuple{AbstractArray{S, N}, AbstractArray{<:Any, N}, FlowGeometries.Discretization.AbstractStencilPlan, Integer}} where {S, N}","page":"Operators","text":"apply_stencil!(out, field, plan, dim; mask=nothing, masked=zero, backend=nothing) -> out\n\nApply a held Discretization.stencil_plan along direction dim of field.\n\nThe primary form: the weights are built once, so a caller differencing many fields along one direction pays for them once, and nothing is allocated per call. The (out, field, x, dim; order, nodes) forms build a plan and call this, which is where their allocation comes from.\n\nmask and masked behave as they do for the table forms: a cell whose stencil reads an inactive cell is written masked.\n\n\n\n\n\n","title":"FlowGeometries.Operators.apply_stencil!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.curl!-Union{Tuple{T}, Tuple{S}, Tuple{AbstractMatrix{S}, AbstractArray, AbstractArray, FlowGeometries.Grids.StaggeredGrid{T, 2, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:Tuple{AbstractVector{T}, AbstractVector{T}}}}} where {S, T}","page":"Operators","text":"curl!(out, u1, u2, sg; masked = NaN) -> out\ncurl!(out, us, sg; masked = NaN) -> out\ncurl!(outs, us, sg; masked = NaN) -> outs\n\nThe curl of a C-staggered vector field, us[d] at the d-face location.\n\nIn two directions it is one scalar, written to out at the corner where both directions are at Discretization.Face:\n\n(1/J)·[ ∂(h_2 u_2)/∂x_1 − ∂(h_1 u_1)/∂x_2 ]\n\nIn three it is a vector, component i written to outs[i] at that component's own vorticity point — see _curl_location — with (i, j, k) running cyclically:\n\n(∇×u)_i = (1/(h_j·h_k))·[ ∂(h_k u_k)/∂x_j − ∂(h_j u_j)/∂x_k ]\n\nEach term is one difference across one cell. Like the divergence this is the circulation around the cell over its area, so the circulation telescopes: the curl summed against the corner measures over a closed or wrapping domain vanishes to round-off. Being the same differences the gradient makes, curl(gradient(f)) is zero to round-off as well.\n\nA point missing either difference — the outer faces of a bounded direction — is masked, as is one whose four bounding centres are not all active.\n\n\n\n\n\n","title":"FlowGeometries.Operators.curl!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.curl-Union{Tuple{T}, Tuple{AbstractArray, AbstractArray, FlowGeometries.Grids.StaggeredGrid{T, 2, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:Tuple{AbstractVector{T}, AbstractVector{T}}}}} where T","page":"Operators","text":"curl(u1, u2, sg; kwargs...) -> Array\ncurl(us, sg; kwargs...) -> Array | NTuple{3,Array}\n\ncurl! into fresh arrays: one at the corner in two directions, and one per component at its own vorticity point in three.\n\n\n\n\n\n","title":"FlowGeometries.Operators.curl"},{"category":"function","location":"api/operators/#FlowGeometries.Operators.derivative!","page":"Operators","text":"derivative!(out, field, grid, dim; order=1, nodes=order+1, policy=BlankMasked(),\n            masked=zero, active_only=true, backend=nothing) -> out\n\nThe derivative with respect to distance along direction dim: apply_stencil! divided by the metric factor,\n\n∂f/∂sᵈ = (1/hᵈ) · ∂f/∂ξᵈ,    hᵈ = Geometry.scale_factors(geo, p)[d]\n\nOn a Cartesian metric every hᵈ is 1 and this is apply_stencil! exactly, at no cost. Anywhere else it is the derivative a physical law is written in, singular points included.\n\nWhere the metric degenerates the derivative does not exist, and masked is written. Longitude at a pole is the case: h_λ = R cos φ → 0, so 1/h_λ diverges. The test scales with the geometry's own size and with the precision, |h| ≤ L·√eps(T). A fixed threshold serves neither: 1e-12 is below eps(Float32), and at Float32 on Earth's radius cos(Float32(π/2)) ≈ -4.4e-8 puts h_λ at the pole around 0.28 m.\n\nNo scale factor in this package depends on longitude, so hᵈ is constant along the first axis whichever direction is differenced. The scaling is applied once per remaining index and swept along that contiguous axis. (It is not generally constant along the differenced direction — on a spheroid h_φ = M(φ) varies with φ — so it is not hoisted that way.)\n\nA divergence or a curl is still the caller's to assemble, needing a result location and a boundary policy this does not choose. Note the flux form when doing so: on a sphere\n\n∇·u = (1/(R cos φ)) [ ∂u_λ/∂λ + ∂(u_φ cos φ)/∂φ ]\n\nso the second term differentiates u_φ cos φ, not u_φ; taking two physical derivatives and adding them is a different, wrong expression.\n\n\n\n\n\n","title":"FlowGeometries.Operators.derivative!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.derivative!-Union{Tuple{NA}, Tuple{N}, Tuple{T}, Tuple{G}, Tuple{S}, Tuple{AbstractArray{S, NA}, AbstractArray{<:Any, NA}, FlowGeometries.Grids.StructuredGrid{T, G, N, S, TP, C, AT, BT} where {S, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractVector{T}}, AT<:AbstractArray{T, N}, BT<:AbstractArray{Bool, N}}, AbstractMatrix{<:Integer}, AbstractMatrix, Integer}} where {S, G, T, N, NA}","page":"Operators","text":"derivative!(out, field, grid, indices, weights, dim; order=1, active_only=true, masked=zero,\n            policy=BlankMasked(), backend=nothing) -> out\n\nderivative! from a table the caller holds — the same reuse as the apply_stencil! form above, for the entry point a geometry-aware caller actually uses.\n\nThe metric fuses into the sweep here too, on the terms _fusable states.\n\n\n\n\n\n","title":"FlowGeometries.Operators.derivative!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.derivative!-Union{Tuple{NA}, Tuple{N}, Tuple{T}, Tuple{G}, Tuple{S}, Tuple{AbstractArray{S, NA}, AbstractArray{<:Any, NA}, FlowGeometries.Grids.StructuredGrid{T, G, N, S, TP, C, AT, BT} where {S, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractVector{T}}, AT<:AbstractArray{T, N}, BT<:AbstractArray{Bool, N}}, FlowGeometries.Discretization.AbstractStencilPlan, Integer}} where {S, G, T, N, NA}","page":"Operators","text":"derivative!(out, field, grid, plan, dim; active_only=true, masked=zero) -> out\n\nderivative! from a held Discretization.stencil_plan.\n\nThe form to use in a loop: the weights and each row's metric factor depend on the grid alone, so both are built once here. Where the factor is constant across the span the sweep writes — see _fusable — it is applied to each row as that row is written, while it is still in cache, saving a second pass over out.\n\n\n\n\n\n","title":"FlowGeometries.Operators.derivative!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.divergence!-Union{Tuple{T}, Tuple{S}, Tuple{N}, Tuple{AbstractArray{S, N}, NTuple{N, AbstractArray}, FlowGeometries.Grids.StaggeredGrid{T, N, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:NTuple{N, AbstractVector{T}}}}} where {N, S, T}","page":"Operators","text":"divergence!(out, us, sg; masked = NaN) -> out\n\nThe divergence of a C-staggered vector field — us[d] at the d-face location — written to out at the cell centres.\n\n(1/J)·Σ_d [ (J/h_d)·u_d ] differenced across the cell: the finite-volume divergence, the net flux through the cell's own faces over its own volume. It is discretely conservative — summing it against the cell measures telescopes, two neighbours' shared face cancelling to the bit, so a closed or wrapping domain integrates to zero to round-off.\n\nA centre whose bounding faces are not all active reads masked.\n\n\n\n\n\n","title":"FlowGeometries.Operators.divergence!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.divergence-Union{Tuple{N}, Tuple{T}, Tuple{NTuple{N, AbstractArray}, FlowGeometries.Grids.StaggeredGrid{T, N, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:NTuple{N, AbstractVector{T}}}}} where {T, N}","page":"Operators","text":"divergence(us, sg; kwargs...) -> Array\n\ndivergence! into a fresh array at the cell centres.\n\n\n\n\n\n","title":"FlowGeometries.Operators.divergence"},{"category":"function","location":"api/operators/#FlowGeometries.Operators.gradient!","page":"Operators","text":"gradient!(outs::Tuple, field, plan) -> outs\ngradient!(g1, g2, field, plan) -> (g1, g2)\ngradient!(g1, g2, g3, field, plan) -> (g1, g2, g3)\n\nApply a GradientPlan: the components of ∇field at every cell, written one per output array. One dot product per cell over its neighbours, allocating nothing.\n\nThere must be exactly ncomponents of them. field and the outputs are indexed linearly, so an N-D array of the grid's shape works as is. The components are named by plan.names — (:λ, :φ) on a sphere, (:x, :y) on a plane, (:x, :y, :z) in a volume — and are per unit distance, the displacements being metric already.\n\n\n\n\n\n","title":"FlowGeometries.Operators.gradient!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.gradient!-Union{Tuple{T}, Tuple{S}, Tuple{N}, Tuple{Tuple{AbstractArray{S, N}, Vararg{AbstractArray{S, N}}}, AbstractArray, FlowGeometries.Grids.StaggeredGrid{T, N, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:NTuple{N, AbstractVector{T}}}}} where {N, S, T}","page":"Operators","text":"gradient!(outs, f, sg; masked = NaN) -> outs\n\nThe gradient of a centre field f on a Grids.StaggeredGrid, component d written to outs[d] at the d-face location — the Arakawa C gradient.\n\nEach component is one difference across one cell, evaluated where it lives: (f[i] − f[i−1]) / (h_d · (x[i] − x[i−1])), with h_d the scale factor at the face. No averaging enters, so the C arrangement's gradient is second order on a uniform mesh and free of the two-grid null space a collocated difference has.\n\nThe outer two faces of a bounded direction have a cell on one side only, so no difference exists there and they are written masked. That is where a boundary condition goes: pass masked = 0 for a zero-gradient edge, or write the edge yourself. A wrapping direction has no such face, its faces all lying between two cells.\n\nA point whose cells are not all active is masked too, on the same rule.\n\n\n\n\n\n","title":"FlowGeometries.Operators.gradient!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.gradient-Union{Tuple{N}, Tuple{T}, Tuple{AbstractArray, FlowGeometries.Grids.StaggeredGrid{T, N, GC, AF} where {GC<:FlowGeometries.Grids.AbstractStructuredGrid, AF<:NTuple{N, AbstractVector{T}}}}} where {T, N}","page":"Operators","text":"gradient(f, sg; kwargs...) -> NTuple{N,Array}\n\ngradient! into fresh arrays, one per direction at that direction's face location.\n\n\n\n\n\n","title":"FlowGeometries.Operators.gradient"},{"category":"function","location":"api/operators/#FlowGeometries.Operators.gradient_plan","page":"Operators","text":"gradient_plan(grid; stencil=Stencils.Axial(1), active_only=true, conn=nothing) -> GradientPlan\n\nBuild the least-squares gradient of grid — the geometry of it, with no field involved. See GradientPlan for what it is and why it is that; apply it with gradient!.\n\nThis is the counterpart of apply_stencil! for the two architectures that have no separable axis to difference along: a CurvilinearGrid, whose neighbours come from its index topology, and an UnstructuredGrid, whose come from its stored adjacency. conn overrides the neighbour set; otherwise one is built, from stencil where the architecture takes one.\n\nOne direction per coordinate: a (λ, φ) or (x, y) surface resolves two, in its tangent plane, and a three-coordinate volume resolves three, on the local frame — see Geometry.local_displacement. The fixed-size symmetric solve behind it covers those two cases.\n\nA masked cell gets no coefficients at all and reads zero gradient, and an inactive neighbour is not offered to the fit, on the same rule as everywhere else: not determined by the active data, so not invented.\n\n\n\n\n\n","title":"FlowGeometries.Operators.gradient_plan"},{"category":"function","location":"api/operators/#FlowGeometries.Operators.interpolate","page":"Operators","text":"interpolate(field, grid, p; policy=BlankMasked(), masked=NaN, …) -> value\n\nThe value of field at the coordinate p, which is how observational data arrives: a station, a float or a ship track carries a coordinate.\n\nDiscretization.interpolation_weights gives the weights along one axis; this composes them, on every layout.\n\nStructuredGrid — multilinear, the tensor product of the per-axis weights. A periodic direction interpolates across its seam, so a coordinate past the last sample wraps to the first.\nCurvilinearGrid, UnstructuredGrid — a weighted least-squares plane fitted to the k nearest cells in the tangent plane at p, which is exact for a linear field and reproduces a cell's own value at its centre. Falls back to the weighted mean where the fit is rank deficient, that being the part of it the data still determines.\n\np may be written any way a point is accepted elsewhere.\n\nThe mask policies say what an inactive contributor means, as they do for a stencil: BlankMasked — the default — returns masked if any contributor is inactive, and ReduceInRun renormalizes over the active ones. ShiftWithinRun has no meaning here, there being no window to shift, and says so.\n\nA field carrying trailing batch axes beyond the grid's own — many tracers, or an ensemble, sharing one geometry — is evaluated for every element in one call: see interpolate! for the form that writes into a caller's buffer, which this one wraps.\n\n\n\n\n\n","title":"FlowGeometries.Operators.interpolate"},{"category":"function","location":"api/operators/#FlowGeometries.Operators.interpolate!","page":"Operators","text":"interpolate!(out, field, grid, p; …) -> out\n\ninterpolate for a batched field, writing one value per batch element into out.\n\nThe bracketing cell — or, off a rectilinear grid, the neighbour set and the least-squares fit — depends on the point and the geometry alone, so it is solved once and applied to every element. One call is therefore less work than interpolate per slice.\n\n\n\n\n\n","title":"FlowGeometries.Operators.interpolate!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.interpolate!-Union{Tuple{D}, Tuple{T}, Tuple{AbstractVector, AbstractArray, Union{FlowGeometries.Grids.CurvilinearGrid{T, G, N, TP, C, KC, MA} where {G<:FlowGeometries.Geometry.AbstractGeometry{T}, N, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractArray{T, N}}, KC<:Union{Nothing, NTuple{N, AbstractArray{T, N}}}, MA<:AbstractArray{T, N}}, FlowGeometries.Grids.UnstructuredGrid{T, G, N, C, VA} where {G<:FlowGeometries.Geometry.AbstractGeometry{T}, N, C<:NTuple{N, AbstractVector{T}}, VA<:AbstractVector{T}}}, NTuple{D, Real}}} where {T, D}","page":"Operators","text":"interpolate!(out, field, grid, p; k=8, …) -> out\n\ninterpolate off a rectilinear grid for a field carrying trailing batch axes, writing one value per batch element.\n\nThe k nearest cells and the mask verdict are a property of the point and the geometry — and the k-d tree query is the expensive part — so they are solved once here and the tangent-plane fit is then applied to every element.\n\n\n\n\n\n","title":"FlowGeometries.Operators.interpolate!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.interpolate!-Union{Tuple{NA}, Tuple{N}, Tuple{T}, Tuple{G}, Tuple{S}, Tuple{AbstractVector{S}, AbstractArray{<:Any, NA}, FlowGeometries.Grids.StructuredGrid{T, G, N, S, TP, C, AT, BT} where {S, TP<:NTuple{N, FlowGeometries.Grids.AbstractTopology}, C<:NTuple{N, AbstractVector{T}}, AT<:AbstractArray{T, N}, BT<:AbstractArray{Bool, N}}, NTuple{N, Real}}} where {S, G, T, N, NA}","page":"Operators","text":"interpolate!(out, field, grid, p; active_only=true, masked=NaN, policy=BlankMasked()) -> out\n\ninterpolate at one coordinate for a field carrying trailing batch axes: out receives one value per batch element, in the order those axes are laid out.\n\nThe bracketing cell and its 2^N corner weights depend on the point and the grid alone, so they are solved once here and applied to every element.\n\n\n\n\n\n","title":"FlowGeometries.Operators.interpolate!"},{"category":"method","location":"api/operators/#FlowGeometries.Operators.ncomponents-Union{Tuple{FlowGeometries.Operators.GradientPlan{D, T} where T}, Tuple{D}} where D","page":"Operators","text":"ncomponents(plan::GradientPlan{D}) -> D\n\nHow many directions the plan resolves, and so how many outputs gradient! writes.\n\n\n\n\n\n","title":"FlowGeometries.Operators.ncomponents"},{"category":"section","location":"api/execution/#Execution","page":"Execution","text":"","title":"Execution"},{"category":"method","location":"api/execution/#FlowGeometries.Execution._reduce_chunks-Union{Tuple{O}, Tuple{F}, Tuple{F, O, Integer, Nothing}} where {F, O}","page":"Execution","text":"_reduce_chunks(f, op, n, backend)\n\nf(range) over a partition of 1:n, combined left to right with op.\n\nDistinct from map_chunks because the serial path has nothing to collect: it hands f the whole range and returns that value directly. map_chunks builds a one-element Vector there, which the compiler usually erases; this path keeps a serial reduction allocation-free unconditionally.\n\n\n\n\n\n","title":"FlowGeometries.Execution._reduce_chunks"},{"category":"method","location":"api/execution/#FlowGeometries.Execution.allocate-Union{Tuple{T}, Tuple{Nothing, Type{T}, Integer}} where T","page":"Execution","text":"allocate(backend, T, n) -> AbstractVector{T}\n\nAn uninitialised length-n vector the loops running under backend can write.\n\nA CSR build allocates its degree, offset and neighbour arrays through this, so the buffers land where the passes that fill them run: an ordinary Vector under nothing and the host backends, device memory under a backend that launches kernels.\n\n\n\n\n\n","title":"FlowGeometries.Execution.allocate"},{"category":"method","location":"api/execution/#FlowGeometries.Execution.chunk_ranges-Tuple{Integer, Integer}","page":"Execution","text":"chunk_ranges(n, k) -> Vector{UnitRange{Int}}\n\nPartition 1:n into at most k contiguous ranges of near-equal length. Contiguity matters: each chunk then touches one span of every array the loop indexes.\n\n\n\n\n\n","title":"FlowGeometries.Execution.chunk_ranges"},{"category":"function","location":"api/execution/#FlowGeometries.Execution.exclusive_scan!","page":"Execution","text":"exclusive_scan!(out, counts, backend = nothing; init = 1) -> out\n\nWrite the exclusive prefix sums of counts into out, which is one element longer: out[1] = init and out[i+1] = out[i] + counts[i].\n\nThis is a CSR offset array — out[k] is where row k starts and out[end] - init is the total — and every connectivity builder here calls it between its counting pass and its filling pass. Under a threaded backend it is two passes over counts plus a serial scan of one sum per chunk.\n\n\n\n\n\n","title":"FlowGeometries.Execution.exclusive_scan!"},{"category":"method","location":"api/execution/#FlowGeometries.Execution.map_chunks-Union{Tuple{F}, Tuple{F, Integer, Nothing}} where F","page":"Execution","text":"map_chunks(f, n, backend) -> Vector\n\nApply f(range) over a partition of 1:n and collect one result per chunk, for a bulk operation that reduces. The caller combines them, so f needs no lock and the combining order is the caller's to fix, which keeps a threaded reduction independent of scheduling for an operation that is associative but not commutative.\n\nf is called at least once, on an empty range when n == 0, since a reduction has to produce a value; run_chunks has nothing to write there and does not call f. Being callable on an empty range is part of the contract, and it is how the result's element type is known before the chunks run.\n\n\n\n\n\n","title":"FlowGeometries.Execution.map_chunks"},{"category":"method","location":"api/execution/#FlowGeometries.Execution.reduce_indices-Union{Tuple{O}, Tuple{F}, Tuple{F, O, Any, Integer, Nothing}} where {F, O}","page":"Execution","text":"reduce_indices(f, op, init, n, backend)\n\nReduce f(i) over i in 1:n with op, under the execution policy backend names.\n\nThe per-index counterpart of _reduce_chunks, and the reduction a device can run: f reads index i and nothing else, so the work splits without a chunk body. Every integral, norm and count over a grid goes through this.\n\nop must be associative and init its identity. init seeds each partial as well as the whole, so the partials combine in any grouping.\n\nThe grouping follows the partition, never the scheduling, so a threaded result is deterministic. It matches the serial left fold wherever op is exactly associative; floating-point + is not, so a threaded sum can differ from the serial one in its last bits.\n\n\n\n\n\n","title":"FlowGeometries.Execution.reduce_indices"},{"category":"method","location":"api/execution/#FlowGeometries.Execution.run_chunks-Union{Tuple{F}, Tuple{F, Integer, Nothing}} where F","page":"Execution","text":"run_chunks(f, n, backend)\n\nApply f(range) over a partition of 1:n, under the execution policy backend names.\n\nf must be safe to run on disjoint index ranges concurrently — every write it makes has to be determined by the index, never accumulated across chunks. nothing means serial and hands f the whole range in one call, so the serial path adds no partitioning at all.\n\n\n\n\n\n","title":"FlowGeometries.Execution.run_chunks"},{"category":"method","location":"api/execution/#FlowGeometries.Execution.run_indices-Union{Tuple{F}, Tuple{F, Integer, Nothing}} where F","page":"Execution","text":"run_indices(f, n, backend)\n\nApply f(i) for each i in 1:n, under the execution policy backend names. f must write only what i determines, with no accumulation across indices — the same contract as run_chunks, stated per index, the form a device launch expresses.\n\nThis is the form a kernel maps onto: with KernelAbstractions loaded and a device backend, f becomes the body of a launch over 1:n.\n\n\n\n\n\n","title":"FlowGeometries.Execution.run_indices"},{"category":"method","location":"api/execution/#FlowGeometries.Execution.scalar-Tuple{Array, Integer}","page":"Execution","text":"scalar(v, i) -> v[i]\n\nOne element of a buffer that may not be host memory.\n\n\n\n\n\n","title":"FlowGeometries.Execution.scalar"},{"category":"section","location":"api/geometry/#Geometry","page":"Geometry","text":"","title":"Geometry"},{"category":"type","location":"api/geometry/#FlowGeometries.Geometry.AbstractCartesianGeometry","page":"Geometry","text":"AbstractCartesianGeometry{T} <: AbstractGeometry{T}\n\nCartesian metrics.  Default: CartesianGeometry.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.AbstractCartesianGeometry"},{"category":"type","location":"api/geometry/#FlowGeometries.Geometry.AbstractEllipsoidalGeometry","page":"Geometry","text":"AbstractEllipsoidalGeometry{T} <: AbstractGeometry{T}\n\nOblate-spheroid metrics. Default: SpheroidGeometry. Coordinate names match the spherical convention, (λ, φ[, h]), with φ the geodetic latitude.\n\nA subtype implements semimajor_axis and flattening; the rest — semiminor_axis, eccentricity², the curvature radii, distance, area_element, scale_factors — follow from those two.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.AbstractEllipsoidalGeometry"},{"category":"type","location":"api/geometry/#FlowGeometries.Geometry.AbstractGeometry","page":"Geometry","text":"AbstractGeometry{T<:AbstractFloat}\n\nSupertype for coordinate metrics (T is the float type).\n\n\n\n\n\n","title":"FlowGeometries.Geometry.AbstractGeometry"},{"category":"type","location":"api/geometry/#FlowGeometries.Geometry.AbstractSphericalGeometry","page":"Geometry","text":"AbstractSphericalGeometry{T} <: AbstractGeometry{T}\n\nSpherical metrics.  Default: SphericalGeometry.\n\nA subtype implements radius; every other method here is written in terms of it.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.AbstractSphericalGeometry"},{"category":"type","location":"api/geometry/#FlowGeometries.Geometry.CartesianGeometry","page":"Geometry","text":"CartesianGeometry()\nCartesianGeometry(T)\nCartesianGeometry{T}()\n\nFlat metric in T, of any dimension.\n\nIt carries no grid spacing. A cell's extent belongs to the grid's axes, which state it per cell and per direction, and the dimension likewise lives on the grid.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.CartesianGeometry"},{"category":"type","location":"api/geometry/#FlowGeometries.Geometry.PoleRotation","page":"Geometry","text":"PoleRotation(λp, φp)\n\nThe frame whose north pole sits at (λp, φp) of the original one — a rotated-pole grid's coordinate change. Apply it with rotate and undo it with unrotate.\n\nThe tilt's sine and cosine are part of the rotation: θ = φp − π/2 is a property of the frame, so it is resolved once here and read by every point carried through it.\n\nrotate and unrotate work at the width of the points given them, carrying the rotation there; T is the width the frame itself is stored and carried from. similar_rotation moves a rotation between widths explicitly.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.PoleRotation"},{"category":"type","location":"api/geometry/#FlowGeometries.Geometry.SphericalGeometry","page":"Geometry","text":"SphericalGeometry{T} <: AbstractSphericalGeometry{T}\n\nDefault spherical geometry with sphere radius R (meters; default Earth 6.371e6).\n\n\n\n\n\n","title":"FlowGeometries.Geometry.SphericalGeometry"},{"category":"type","location":"api/geometry/#FlowGeometries.Geometry.SpheroidGeometry","page":"Geometry","text":"SpheroidGeometry(a, f)\nSpheroidGeometry()\n\nOblate spheroid of equatorial radius a and flattening f = (a-b)/a. The no-argument form is WGS 84, a = 6378137.0, f = 1/298.257223563.\n\ndistance, area_element, volume_element and scale_factors differ from a sphere. Grid directions are (λ, φ, h), with h the height above the ellipsoid, where a sphere's third direction is an absolute radius.\n\nRectilinear grids and the index-space connectivity built on them work as they do for a sphere; the samplings are purely angular and so carry over unchanged. So does everything written against the two primitives embed and local_tangent_basis — the local frame, the vector and tensor rotations, the tangent-plane projection, and therefore the least-squares gradient and the scattered interpolation. See AbstractLonLatGeometry for why the frame is shared.\n\nThe spherical area routines do not: unstructured_grid's Voronoi areas and the cell areas behind spherical_grid are built on spherical excess and 4πR²/n, which are sphere identities. They stay restricted to AbstractSphericalGeometry, and raise on an ellipsoid.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.SpheroidGeometry"},{"category":"type","location":"api/geometry/#FlowGeometries.Geometry.AbstractLonLatGeometry","page":"Geometry","text":"AbstractLonLatGeometry{T}\n\nThe geometries whose coordinates are (λ, φ, …) about a polar axis: AbstractSphericalGeometry and AbstractEllipsoidalGeometry.\n\nThey share their local frame. On an oblate spheroid the geodetic latitude is defined by the surface normal, n̂ = (cosφ·cosλ, cosφ·sinλ, sinφ), and the parallel through a point is a circle in a plane of constant z, so ê_λ = (-sinλ, cosλ, 0) and ê_φ = n̂ × ê_λ — the sphere's three vectors at the same (λ, φ). Every frame rotation here is therefore one implementation for both hierarchies. What differs is the position a (λ, φ) sits at, which is embed's business, and the physical length of a unit coordinate step, which is scale_factors'.\n\nThe two hierarchies are siblings, so that an ellipsoid does not inherit the area identities — 4πR²/n, spherical excess — that hold on a sphere alone. This Union names the part they share.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.AbstractLonLatGeometry"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry._ambient_names-Tuple{Val{1}}","page":"Geometry","text":"Component names of an ambient-frame vector (as returned by local_tangent_basis).\n\n\n\n\n\n","title":"FlowGeometries.Geometry._ambient_names"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry._at-Union{Tuple{N}, Tuple{T}, Tuple{Type{T}, NTuple{N, Real}}} where {T<:AbstractFloat, N}","page":"Geometry","text":"Convert positional point values to the geometry's own float type T.\n\n\n\n\n\n","title":"FlowGeometries.Geometry._at"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry._enu_frame-Union{Tuple{T}, Tuple{Union{FlowGeometries.Geometry.AbstractEllipsoidalGeometry{T}, FlowGeometries.Geometry.AbstractSphericalGeometry{T}}, Real, Real}} where T","page":"Geometry","text":"_enu_frame(geo, λ, φ) -> (ê_λ, ê_φ, ê_r)\n\nThe local east/north/up triad at (λ, φ) in ambient Cartesian components.\n\nThe one definition of the frame, so a vector rotation, a tensor rotation and a tangent-plane projection cannot drift apart. local_tangent_basis is its first two vectors under their coordinate names.\n\n\n\n\n\n","title":"FlowGeometries.Geometry._enu_frame"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry._lonlat-Tuple{Any}","page":"Geometry","text":"_lonlat(p) -> (λ, φ)\n_xyz(v) -> (x, y, z)\n\nA point's components for a consumer that needs a fixed number of them, with the arity checked.\n\nλ, φ = as_ntuple(p) does not check it: on a one-component point the destructure reads p[2] and reports an index out of bounds, when what is wrong is the point. These say so instead, and return a pair or a triple whose type does not depend on how many components the point had.\n\nThe arity is the dispatch, so the accepted width is a method signature and the tuple is indexed only where its type is known wide enough.\n\n\n\n\n\n","title":"FlowGeometries.Geometry._lonlat"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.area_element-Union{Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractCartesianGeometry{T}, Real, Real}} where T","page":"Geometry","text":"area_element(geo::AbstractCartesianGeometry, dx, dy)\narea_element(geo::AbstractSphericalGeometry, φ, dλ, dφ)\n\nLocal cell area from the cell's own extents: dx·dy on the plane, R²·cosφ·dλ·dφ on the sphere.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.area_element"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.area_element-Union{Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry{T}, Real, Real, Real}} where T","page":"Geometry","text":"area_element(geo::AbstractEllipsoidalGeometry, φ, dλ, dφ)\n\nM(φ)·N(φ)cosφ·dλ·dφ, the exact ellipsoidal surface element — equal to a²(1-e²)cosφ / (1-e²sin²φ)²·dλ·dφ.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.area_element"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.as_ntuple-Tuple{NamedTuple}","page":"Geometry","text":"as_ntuple(p) -> Tuple\nas_ntuple(p, Val(N)) -> NTuple{N}\n\nNormalize a point (Tuple, NamedTuple, or AbstractVector of length 1–3) to a plain Tuple.\n\nA vector's length is a runtime value, so for one the first form returns a union of the three tuple widths. Every entry point here reduces a point to a value whose type does not depend on that width — distance to a scalar, spherical_to_cartesian to three components — so the union splits across the branch and each arm infers concretely and allocates nothing.\n\nscale_factors is the exception, its result being one factor per direction. Name the width with Val(N) there, or hand it a tuple.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.as_ntuple"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.as_tensor6-Tuple{NTuple{6, Any}}","page":"Geometry","text":"as_tensor6(τ) -> NTuple{6}\n\nNormalize a symmetric rank-2 tensor to a plain 6-tuple of its independent components, from a Tuple, NamedTuple or AbstractVector — the rank-2 counterpart of as_ntuple.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.as_tensor6"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.build_point-Union{Tuple{N}, Tuple{Type{NamedTuple}, NTuple{N, Symbol}, NTuple{N, Any}}} where N","page":"Geometry","text":"build_point(S, names::NTuple{N,Symbol}, vals::NTuple{N}) -> S\n\nAssemble a point/vector in representation S from positional vals and their names. Bare NamedTuple takes names; a parameterized NamedTuple type takes its own. Everything else is built from vals alone, covering Tuple, NTuple{N,T}, Vector{T}, SVector{N,T}, MVector{N,T}, and any user type with a positional constructor.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.build_point"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.cartesian_to_geodetic-Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry, Any}","page":"Geometry","text":"cartesian_to_geodetic(geo, xyz) -> NamedTuple{(:λ,:φ,:h)}\ncartesian_to_geodetic(S, geo, xyz) -> S\n\nInverse of geodetic_to_cartesian: Earth-centred Cartesian to geodetic (λ, φ, h) with λ ∈ (-π, π], φ the geodetic latitude in [-π/2, π/2], and h the height above the ellipsoid.\n\nUnlike the spherical inverse this is not closed-form — geodetic latitude satisfies a quartic — so it is solved by Newton's method on\n\ntan φ = z(N+h) / (p(N(1-e²)+h)),   p = √(x²+y²)\n\nwith h = p·cosφ + z·sinφ − a√(1-e²sin²φ) substituted each step, which is exact for any φ and stays finite at the poles where p/cosφ − N does not. Convergence is quadratic and reaches eps(T) in three or four steps at terrestrial flattening. On the polar axis λ = 0 and the latitude is ±π/2 exactly.\n\nThe foot of the normal is unique for any point outside the ellipsoid's evolute — every position on or above the surface, and far below it. Deep inside, near the centre, a point has several feet and no preferred one; the centre itself is reported as (0, 0, -a), the same convention cartesian_to_spherical uses at the origin.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.cartesian_to_geodetic"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.cartesian_to_spherical-Tuple{FlowGeometries.Geometry.AbstractSphericalGeometry, Any}","page":"Geometry","text":"cartesian_to_spherical(geo, xyz) -> NamedTuple{(:λ,:φ,:r)}\ncartesian_to_spherical(S, geo, xyz) -> S\n\nInverse of spherical_to_cartesian: Cartesian (x, y, z) to (λ, φ, r) with λ ∈ (-π, π], φ ∈ [-π/2, π/2], and r the absolute radius from the origin. At the origin λ = φ = 0.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.cartesian_to_spherical"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.distance-Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry, Any, Any}","page":"Geometry","text":"distance(geo::AbstractEllipsoidalGeometry, p1, p2)\n\n2-D (λ, φ): geodesic distance by Vincenty's inverse method, iterated to 1e-12 in the auxiliary longitude, which holds the result to well under a millimetre at Earth scale.\n\nVincenty's iteration converges slowly for very nearly antipodal point pairs. It is capped, and on reaching the cap the result is the great-circle distance on a sphere of the mean radius.\n\n3-D (λ, φ, h): the chord through Cartesian (ECEF), mirroring the spherical 3-D convention. A lone (λ,) is the shorter arc of the equator.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.distance"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.distance-Tuple{FlowGeometries.Geometry.AbstractGeometry, Any, Any}","page":"Geometry","text":"distance(geo::AbstractGeometry, pt1, pt2)\n\nDistance between two points. Spherical 2D uses great-circle (Haversine); spherical 3D uses the chord through Cartesian, with r the absolute radius from the origin; a lone (λ,) is the shorter arc of its circle.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.distance"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.eccentricity²-Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry}","page":"Geometry","text":"First eccentricity squared, e² = f(2-f).\n\n\n\n\n\n","title":"FlowGeometries.Geometry.eccentricity²"},{"category":"function","location":"api/geometry/#FlowGeometries.Geometry.embed","page":"Geometry","text":"embed(geo, coords) -> NTuple{D,T}\nembed(S, geo, coords) -> S\n\nA point's position in the ambient Cartesian space this metric is realized in: the coordinates themselves on the plane, the point on the sphere of radius R, the Earth-centred position of a geodetic (λ, φ[, h]) on a spheroid.\n\nOne of the two primitives the metric-independent constructions here are written against; the other is local_tangent_basis. A straight-line displacement, a chord distance, a tangent-plane projection and a least-squares gradient need a position and a frame and nothing else, so each is written once and serves every geometry supplying those two.\n\nThis is the ambient position, always. What a spatial index searches is a separate question — an index may scale a sphere's surface coordinates to make an arc comparable to a chord, which is FlowGeometries.Grids.embed_point and its FlowGeometries.Grids.AbstractEmbedding.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.embed"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.flattening-Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry}","page":"Geometry","text":"flattening(geo) -> T\n\nFlattening f = (a-b)/a. Defaults to the f field.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.flattening"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.float_type-Union{Tuple{FlowGeometries.Geometry.AbstractGeometry{T}}, Tuple{T}} where T<:AbstractFloat","page":"Geometry","text":"float_type(geo) -> Type{<:AbstractFloat}\n\nThe element type geo computes in. Anything that pairs a geometry with an optional element type defaults to this, so a geometry given without one keeps its own width.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.float_type"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.geodetic_to_cartesian-Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry, Any}","page":"Geometry","text":"geodetic_to_cartesian(geo, coords) -> (; x, y, z)\n\n(λ, φ, h) — geodetic latitude and height above the ellipsoid — to Earth-centred Cartesian: x = (N(φ)+h)cosφ·cosλ, y = (N(φ)+h)cosφ·sinλ, z = (N(φ)(1-e²)+h)sinφ. The 2-D form takes h = 0, the surface.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.geodetic_to_cartesian"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.jacobian-Tuple{FlowGeometries.Geometry.AbstractGeometry, Any}","page":"Geometry","text":"jacobian(geo, point) -> T\n\n∏ of scale_factors: the volume element per unit coordinate volume at point.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.jacobian"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.local_displacement-Tuple{FlowGeometries.Geometry.AbstractGeometry, Any, Any}","page":"Geometry","text":"local_displacement(geo, center, neighbor) -> NamedTuple\n\nneighbor's displacement from center, resolved on the local coordinate frame at center, one component per coordinate — the quantity a least-squares fit differences against.\n\nTwo coordinates is project_to_tangent_plane, which this calls: a surface has a tangent plane and the displacement lies in it. Three is the same ambient chord on the full local frame — (x, y, z) on the plane, and eastward/northward/outward on a sphere or spheroid, the third direction being the normal unit_vector gives.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.local_displacement"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.local_tangent_basis-Tuple{FlowGeometries.Geometry.AbstractGeometry, Any}","page":"Geometry","text":"local_tangent_basis(geo, coords) -> NamedTuple\nlocal_tangent_basis(S, geo, coords) -> NamedTuple of `S`\n\nCoordinate-aligned unit vectors at coords, in the ambient Cartesian frame:\n\nCartesian: (; x = ê_x, y = ê_y), 2-component\nSpherical and spheroidal: (; λ = ê_λ, φ = ê_φ), 3-component — the eastward and northward tangents, which are the same triad on either (see AbstractLonLatGeometry)\n\nThe basis vectors themselves are Tuples by default; pass a leading S to get them as SVectors (or any other representation) so they can be used with vector arithmetic directly.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.local_tangent_basis"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.meridional_radius-Union{Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry{T}, Real}} where T","page":"Geometry","text":"meridional_radius(geo, φ) -> T\n\nM(φ) = a(1-e²) / (1 - e²sin²φ)^{3/2}: the radius of curvature along the meridian. A unit step in latitude covers M(φ).\n\n\n\n\n\n","title":"FlowGeometries.Geometry.meridional_radius"},{"category":"function","location":"api/geometry/#FlowGeometries.Geometry.metric_invariant_directions","page":"Geometry","text":"metric_invariant_directions(geo) -> NTuple{K,Int}\n\nThe coordinate directions along which scale_factors does not vary.\n\nA bulk operation that divides by a scale factor then solves it once per line: on a sphere no factor depends on longitude, so a whole row shares one value.\n\nThe default is (), no direction invariant, and each concrete geometry declares its own. A subtype of AbstractSphericalGeometry may write its own scale_factors, and an inherited claim about them hoists a value that varies, giving a wrong derivative. Declare the directions for your own geometry to opt in.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.metric_invariant_directions"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.named_point-Union{Tuple{N}, Tuple{FlowGeometries.Geometry.AbstractGeometry, NTuple{N, Any}}} where N","page":"Geometry","text":"named_point(geo, vals::NTuple{N}) -> NamedTuple\n\nBuild the geometry-appropriate named point from positional values (axis order).\n\n\n\n\n\n","title":"FlowGeometries.Geometry.named_point"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.nonuniform_first_derivative-Union{Tuple{T}, NTuple{5, T}} where T<:AbstractFloat","page":"Geometry","text":"nonuniform_first_derivative(f_m, f_0, f_p, h_m, h_p)\n\nStandard 3-point, 2nd-order-accurate centered finite-difference approximation of the first derivative at the middle node on a possibly nonuniform stencil. f_m, f_0, f_p are the function values at the minus/center/plus nodes, and h_m = x_0 - x_{-}, h_p = x_{+} - x_0 > 0 are the (physical) left/right spacings.\n\nReduces exactly to the uniform central difference (f_p - f_m)/(2h) when h_m == h_p == h, and is exact for linear and quadratic f for any h_m, h_p.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.nonuniform_first_derivative"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.point_names-Tuple{FlowGeometries.Geometry.AbstractCartesianGeometry, Val{1}}","page":"Geometry","text":"point_names(geo, Val(N)) -> NTuple{N,Symbol}\n\nField names for an N-coordinate point in this geometry. Cartesian: (:x,), (:x,:y), (:x,:y,:z), then (:x1, …, :xN). Spherical: (:λ,), (:λ,:φ), (:λ,:φ,:r), then (:λ,:φ,:r,:q4,…).\n\n\n\n\n\n","title":"FlowGeometries.Geometry.point_names"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.prime_vertical_radius-Union{Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry{T}, Real}} where T","page":"Geometry","text":"prime_vertical_radius(geo, φ) -> T\n\nN(φ) = a / √(1 - e²sin²φ): the radius of curvature perpendicular to the meridian. A unit step in longitude covers N(φ)cosφ.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.prime_vertical_radius"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.project_to_tangent_plane-Tuple{FlowGeometries.Geometry.AbstractGeometry, Any, Any}","page":"Geometry","text":"project_to_tangent_plane(geo, center, neighbor) -> NamedTuple\nproject_to_tangent_plane(S, geo, center, neighbor) -> S\n\nTangent-plane displacement of neighbor relative to center along the local coordinate basis from local_tangent_basis: the ambient chord between the two embedded positions, resolved on that basis.\n\nA least-squares gradient and a scattered interpolation are built on this, so both work on any geometry supplying those two primitives — a spheroid included, where the chord runs between geodetic positions and the basis is the same triad.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.project_to_tangent_plane"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.radius-Tuple{FlowGeometries.Geometry.AbstractSphericalGeometry}","page":"Geometry","text":"radius(geo) -> T\n\nThe sphere radius. The one method a spherical geometry supplies; defaults to the R field.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.radius"},{"category":"function","location":"api/geometry/#FlowGeometries.Geometry.rotate!","page":"Geometry","text":"rotate!(λ, φ, rot) -> (λ, φ)\nunrotate!(λ, φ, rot) -> (λ, φ)\n\nRotate a whole point set in place — the form a sampling's spherical_points output takes. λ and φ are any arrays of matching shape, so this covers a scattered node set and a grid's 2-D coordinate fields alike. Allocates nothing.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.rotate!"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.rotate-Tuple{FlowGeometries.Geometry.PoleRotation, Real, Real}","page":"Geometry","text":"rotate(rot, λ, φ) -> (λ′, φ′)\n\n(λ, φ) expressed in the rotated frame. The rotation's own pole maps to φ′ = π/2.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.rotate"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.scale_factors-Union{Tuple{N}, Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractCartesianGeometry{T}, NTuple{N, Real}}} where {T, N}","page":"Geometry","text":"scale_factors(geo, point) -> NTuple\nscale_factors(geo, point, Val(N)) -> NTuple{N}\n\nPhysical length of a unit coordinate step in each direction at point — see FlowGeometries.Discretization.scale_factors.\n\nCartesian: 1 in every direction, the metric being the identity. Spherical: (R·cosφ, R) on the surface of radius R, and (r·cosφ, r, 1) where a radius direction is present, r being that point's own radius.\n\nThe result is one factor per direction, so from a point whose length is a runtime value — an AbstractVector — its width is one too. Val(N) names that width, checks it, and gives a concrete NTuple{N}.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.scale_factors"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.semimajor_axis-Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry}","page":"Geometry","text":"semimajor_axis(geo) -> T\n\nEquatorial radius a. Defaults to the a field.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.semimajor_axis"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.semiminor_axis-Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry}","page":"Geometry","text":"Semi-minor axis b = a(1-f).\n\n\n\n\n\n","title":"FlowGeometries.Geometry.semiminor_axis"},{"category":"function","location":"api/geometry/#FlowGeometries.Geometry.similar_geometry","page":"Geometry","text":"similar_geometry(T, geo) -> AbstractGeometry{T}\n\ngeo with its element type changed to T, keeping its shape parameters.\n\nA geometry fixes the width of everything computed against it — coordinates, distances, metric factors — so building a grid at one element type around a geometry at another silently promotes the whole grid back. Anything that takes an element type and a geometry together should pass the geometry through here first.\n\nA geometry already at T is returned unchanged, whatever its type, so a geometry defined outside this package inherits the whole stack without supplying anything. Only an actual change of width needs a method, and only a geometry that wants to support one has to define it.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.similar_geometry"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.similar_rotation-Union{Tuple{T}, Tuple{Type{T}, FlowGeometries.Geometry.PoleRotation{T}}} where T<:AbstractFloat","page":"Geometry","text":"similar_rotation(T, rot) -> PoleRotation{T}\n\nrot at element width T, the counterpart of similar_geometry for a frame. The tilt is re-resolved at the new width from (λp, φp).\n\n\n\n\n\n","title":"FlowGeometries.Geometry.similar_rotation"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.spherical_excess-Union{Tuple{T}, Tuple{Tuple{T, T, T}, Tuple{T, T, T}, Tuple{T, T, T}}} where T","page":"Geometry","text":"spherical_excess(a, b, c) -> T\n\nSpherical excess of the triangle spanned by three unit vectors, via Van Oosterom & Strackee (1983):\n\ntan(E/2) = |a · (b × c)| / (1 + a·b + b·c + c·a)\n\nOne atan and no other transcendental, against L'Huilier's three great-circle distances (each its own trig) plus four tangents. Taking directions lets a mesh convert each vertex once — see unit_vector — and share it across every triangle. Multiply by R² for an area, as triangle_area does.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.spherical_excess"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.spherical_to_cartesian-Tuple{FlowGeometries.Geometry.AbstractSphericalGeometry, Any}","page":"Geometry","text":"spherical_to_cartesian(geo, coords) -> NamedTuple{(:x,:y,:z)}\nspherical_to_cartesian(S, geo, coords) -> S\n\n(λ, φ) on the reference sphere of radius R, or (λ, φ, r) with r the absolute radius from the origin, mapped to Cartesian coordinates of the same space.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.spherical_to_cartesian"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.tensor_from_local-Union{Tuple{T}, Tuple{Union{FlowGeometries.Geometry.AbstractEllipsoidalGeometry{T}, FlowGeometries.Geometry.AbstractSphericalGeometry{T}}, Vararg{Real, 8}}} where T<:AbstractFloat","page":"Geometry","text":"tensor_from_local(geo, τλλ, τφφ, τrr, τλφ, τλr, τφr, λ, φ) -> NamedTuple\ntensor_from_local(geo, τ, λ, φ) -> NamedTuple\ntensor_from_local(S, geo, args...) -> S\n\nThe inverse of tensor_to_local: τ = Rᵀ τ' R, returning the ambient Cartesian components (; xx, yy, zz, xy, xz, yz). Composing the two in either order is the identity.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.tensor_from_local"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.tensor_to_local-Union{Tuple{T}, Tuple{Union{FlowGeometries.Geometry.AbstractEllipsoidalGeometry{T}, FlowGeometries.Geometry.AbstractSphericalGeometry{T}}, Vararg{Real, 8}}} where T<:AbstractFloat","page":"Geometry","text":"tensor_to_local(geo, τxx, τyy, τzz, τxy, τxz, τyz, λ, φ) -> NamedTuple\ntensor_to_local(geo, τ, λ, φ) -> NamedTuple\ntensor_to_local(S, geo, args...) -> S\n\nRotate a symmetric rank-2 tensor from the ambient Cartesian frame into the local (ê_λ, ê_φ, ê_r) frame at (λ, φ): τ' = R τ Rᵀ, returning (; λλ, φφ, rr, λφ, λr, φr).\n\nThe rank-1 counterpart is vector_from_cartesian; between them they cover essentially every field anyone rotates — a stress, a strain rate, a covariance, a subfilter flux. τ may be given as the six independent components or as any 6-component representation, in the order above.\n\nR is the same basis local_tangent_basis defines, so the two cannot drift apart — and being a rotation of the frame alone, it holds on a spheroid as it does on a sphere.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.tensor_to_local"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.triangle_area-Union{Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractSphericalGeometry{T}, Any, Any, Any}} where T<:AbstractFloat","page":"Geometry","text":"triangle_area(geo, p1, p2, p3) -> T\n\nExact area of the spherical triangle through three (λ, φ) points, each in any accepted representation. R² times spherical_excess.\n\nThis converts all three points on every call. A mesh that already holds vertex directions should call spherical_excess on those and scale once.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.triangle_area"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.triangle_area_from_unit_vectors-Union{Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractSphericalGeometry{T}, Tuple{T, T, T}, Tuple{T, T, T}, Tuple{T, T, T}}} where T<:AbstractFloat","page":"Geometry","text":"triangle_area_from_unit_vectors(geo, u1, u2, u3) -> T\ntriangle_area_from_unit_vectors(R², u1, u2, u3) -> T\n\ntriangle_area for vertices already held as unit vectors — R² times spherical_excess, with no coordinate conversion.\n\nIt carries its own name because dispatch cannot tell the two apart: a unit vector and a 3-D spherical point (λ, φ, r) are both 3-tuples.\n\nPass R² itself to walk many triangles: a mesh shares vertices between cells, so the squaring belongs outside the loop alongside the one-per-vertex unit_vector call. That is the form the cell-area and Voronoi paths here use.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.triangle_area_from_unit_vectors"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.unit_vector-Union{Tuple{T}, Tuple{Type{T}, Any}} where T<:AbstractFloat","page":"Geometry","text":"unit_vector(T, p) -> NTuple{3,T}\n\nThe direction of the (λ, φ) point p on the unit sphere, in any accepted point representation. This is spherical_to_cartesian with the radius divided out, as a bare tuple: the form the spherical-triangle kernels want, computed once per vertex and reused across every triangle sharing it.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.unit_vector"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.unrotate-Tuple{FlowGeometries.Geometry.PoleRotation, Real, Real}","page":"Geometry","text":"unrotate(rot, λ′, φ′) -> (λ, φ)\n\nInverse of rotate.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.unrotate"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.vector_from_cartesian-Union{Tuple{T}, Tuple{Union{FlowGeometries.Geometry.AbstractEllipsoidalGeometry{T}, FlowGeometries.Geometry.AbstractSphericalGeometry{T}}, Vararg{Real, 5}}} where T<:AbstractFloat","page":"Geometry","text":"vector_from_cartesian(geo, ux, uy, uz, λ, φ) -> NamedTuple{(:λ,:φ,:r)}\nvector_from_cartesian(geo, v, λ, φ) -> NamedTuple{(:λ,:φ,:r)}\nvector_from_cartesian(S, geo, args...) -> S\n\nMap an ambient Cartesian vector (ux, uy, uz) at (λ, φ) into local components (λ = u_λ, φ = u_φ, r = u_r) — eastward, northward, and along the outward normal. v may be any 3-component representation. The inverse of vector_to_cartesian, and like it the same on a sphere and on a spheroid.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.vector_from_cartesian"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.vector_to_cartesian-Union{Tuple{T}, Tuple{Union{FlowGeometries.Geometry.AbstractEllipsoidalGeometry{T}, FlowGeometries.Geometry.AbstractSphericalGeometry{T}}, Vararg{Real, 5}}} where T<:AbstractFloat","page":"Geometry","text":"vector_to_cartesian(geo, u_λ, u_φ, u_r, λ, φ) -> NamedTuple{(:x,:y,:z)}\nvector_to_cartesian(geo, u_λ, u_φ, λ, φ) -> NamedTuple{(:x,:y,:z)}\nvector_to_cartesian(S, geo, args...) -> S\n\nMap the components of a vector given in the local (ê_λ, ê_φ, ê_r) basis at (λ, φ) into the ambient Cartesian basis. This transforms vector components at a point; see embed for positions. The 2-component form assumes u_r = 0.\n\nA rotation of the frame and nothing else, so it is one and the same on a sphere and on a spheroid — see AbstractLonLatGeometry.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.vector_to_cartesian"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.volume_element-Union{Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractCartesianGeometry{T}, Real, Real, Real}} where T","page":"Geometry","text":"volume_element(geo::AbstractCartesianGeometry, dx, dy, dz)\nvolume_element(geo::AbstractSphericalGeometry, r, φ, dλ, dφ, dr)\n\nLocal cell volume from the cell's own extents. The spherical form takes the local radius r at this level, giving the shell element r²·cosφ·dλ·dφ·dr; the reference radius does not enter.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.volume_element"},{"category":"method","location":"api/geometry/#FlowGeometries.Geometry.volume_element-Union{Tuple{T}, Tuple{FlowGeometries.Geometry.AbstractEllipsoidalGeometry{T}, Vararg{Real, 5}}} where T","page":"Geometry","text":"volume_element(geo::AbstractEllipsoidalGeometry, φ, h, dλ, dφ, dh)\n\n(N(φ)+h)cosφ · (M(φ)+h) · dλ·dφ·dh, the geodetic volume element at ellipsoidal height h.\n\nUnlike the spherical form this does not factor into a function of φ times a function of h: both curvature radii are offset by h, so the two directions are coupled.\n\n\n\n\n\n","title":"FlowGeometries.Geometry.volume_element"},{"category":"section","location":"discretization/#discretization-page","page":"Discretization","text":"The geometric inputs a numerical method needs from a grid: where a point falls, the weights that interpolate to it, where a cell's faces are, the metric factors of the coordinate system, and the finite-difference weights of any stencil.\n\nAll of it is a function of coordinates alone. Applying weights to a field is not here: that needs a result location, a boundary-condition policy and a halo convention, all of them the caller's to choose.","title":"Discretization"},{"category":"section","location":"discretization/#Centres-and-faces","page":"Discretization","text":"using FlowGeometries: FlowGeometries as FG\nD = FG.Discretization\nO = FG.Operators\nA = FG.Axes\n\nx = [0.0, 1.0, 3.0, 6.0]\nD.faces(x)\n\nN centres give N+1 faces, at the midpoints, with the outermost two extrapolated a half-cell beyond the end centres. Note that centres do not determine faces — the system is underdetermined by one — so that midpoint rule is a stated convention, the same one the curvilinear corner reconstruction uses.\n\nA uniform axis keeps its uniformity, so nothing is lost by asking for its faces:\n\nu = A.UniformAxis(0.0, 0.5, 6)\nA.isuniform(D.faces(u)), A.spacing(D.faces(u)), length(D.faces(u))\n\nD.nodes(u, D.Center()) === u, length(D.nodes(u, D.Face()))","title":"Centres and faces"},{"category":"section","location":"discretization/#Gaps-and-cell-widths","page":"Discretization","text":"local_spacing is the gap either side of one sample, and cell_width the width of one cell. Both are a scalar subtraction of two stored numbers — no array is built — so they are the forms to call per grid point. faces materializes the whole axis, which suits a sweep over all of it.\n\nxs = cumsum([0.0, 1.0, 0.3, 2.5, 0.7, 4.0])\nD.local_spacing(xs, 3), D.cell_width(xs, 3)\n\ncell_width is the distance between the cell's two faces:\n\nf = D.faces(xs)\n[D.cell_width(xs, i) for i in eachindex(xs)] ≈ abs.(diff(f))\n\nThe gaps are signed and the width is not. A derivative needs the sign, since it distinguishes an axis stored increasing from one stored decreasing; a width is a length and cannot be negative. Reverse the axis and cell i moves to n+1-i, where the gaps come back negated and swapped — the same two neighbours, now on the other side — while the width is unchanged:\n\nxr = reverse(xs)                              # cell 3 of xs is cell 4 of xr\nD.local_spacing(xr, 4), D.cell_width(xr, 4)\n\nAt a bounded end the outward gap is 0 and the caller falls back to a one-sided stencil. Given a period it wraps, so a seam behaves like the interior:\n\nλ = collect(range(0, 2π * (1 - 1/8); length = 8))\nD.local_spacing(λ, 8), D.local_spacing(λ, 8, 2π)\n\ncell_widths is the whole axis at once. A uniform axis returns an Axes.ConstantVector — one number and a length — so nothing is materialized for it either:\n\nD.cell_widths(u), D.cell_widths(xs)\n\nOn a grid, Grids.local_spacing, Grids.cell_width and Grids.cell_widths take the direction's axis and its wrap period from the grid, so a periodic seam is right without being asked for — see the Grids page.","title":"Gaps and cell widths"},{"category":"section","location":"discretization/#Point-location","page":"Discretization","text":"D.locate(u, 0.2), D.locate(u, 0.4), D.locate(u, -5.0)\n\nlocate returns the cell containing a coordinate, or 0 outside it. Cells are the intervals between faces, so cell 1 of u spans [-0.25, 0.25). It is O(1) on a uniform axis — a direct payoff of the spacing living in the type — and O(log n) by bisection on a stretched one. Both storage orders work.\n\nD.locate(xs, 2.0), D.nearest_index(xs, 2.0)\n\nnearest_index always returns a valid index, clamping a coordinate outside the axis to the nearer end; exact ties go to the lower index. It brackets and compares, so it is O(log n) on a stretched axis and O(1) on a uniform one, and both paths compare the same two samples: a uniform axis and its collect hold the same numbers and agree about which is nearest.","title":"Point location"},{"category":"section","location":"discretization/#Interpolation-weights","page":"Discretization","text":"i, w = D.interpolation_weights(xs, 2.0)\ni, w, sum(w)\n\nWeights only — applying them is w[1]*f[i] + w[2]*f[i+1] at the call site. For higher order on an arbitrarily spaced axis, lagrange_weights is exact for polynomials up to nodes-1:\n\nidx, wl = D.lagrange_weights(xs, 2.0, 4)\nidx, sum(wl)","title":"Interpolation weights"},{"category":"section","location":"discretization/#Finite-difference-weights","page":"Discretization","text":"fd_weights is the recursion of Fornberg (1988), Math. Comp. 51, 699–706. One recursion covers every case: any derivative order, any node count (hence any order of accuracy), any evaluation point, and arbitrarily spaced nodes.\n\nD.fd_weights([-1.0, 0.0, 1.0], 0.0, 1)      # centred first difference\n\nD.fd_weights([-1.0, 0.0, 1.0], 0.0, 2)      # centred second difference\n\nD.fd_weights([-2.0, -1.0, 0.0, 1.0, 2.0], 0.0, 1)   # fourth-order first derivative\n\nWith m nodes the weights are exact for polynomials of degree m-1, so the accuracy order is m - order. Nothing about that requires equal spacing:\n\nD.fd_weights([-1.7, -0.4, 0.9], 0.0, 1)     # same order, unequal nodes\n\nThe axis form centres the stencil on a sample and shifts it inward at a boundary, so the node count — and therefore the accuracy order — holds at the two ends as well as the interior:\n\nxg = collect(range(0.0, 1.0; length = 11))\nidx1, w1 = D.fd_weights(xg, 1, 1, 5)        # at the first sample\nidxm, wm = D.fd_weights(xg, 6, 1, 5)        # in the interior\nidx1, idxm\n\nFG.Geometry.nonuniform_first_derivative is the three-node, first-derivative case of the same thing. Fed the gaps above it is a complete derivative at a point, which is the shape an operator assembled at the call site takes — a divergence, a curl, a staggered difference are each this plus the metric factors and whatever boundary policy the caller chose:\n\ng(x) = 3x^2 - 2x + 5                        # exact for a quadratic, on any spacing\nmaximum(abs(FG.Geometry.nonuniform_first_derivative(\n                g(xs[i-1]), g(xs[i]), g(xs[i+1]), D.local_spacing(xs, i)...) - (6xs[i] - 2))\n        for i in 2:length(xs)-1)\n\nSigned gaps are what carry that through: the same expression on a descending axis differentiates with respect to the coordinate, with no correction at the call site:\n\nmaximum(abs(FG.Geometry.nonuniform_first_derivative(\n                g(xr[i-1]), g(xr[i]), g(xr[i+1]), D.local_spacing(xr, i)...) - (6xr[i] - 2))\n        for i in 2:length(xr)-1)","title":"Finite-difference weights"},{"category":"section","location":"discretization/#Applying-a-weight-set","page":"Discretization","text":"apply_stencil! is the one function here that touches a field, and only along a single direction with the result left where the input was. That case needs no convention the package has not already fixed: nothing to stagger, fd_weights' inward shift at a bounded end, wrapping on a periodic one — so no halo either.\n\nx = collect(range(0.0, 2.0; length = 11))\nf = @. 3x^2 - 2x + 5\nout = similar(f)\nO.apply_stencil!(out, f, x, 1; order = 1, nodes = 3)\nmaximum(abs, out .- (6x .- 2))          # exact for a quadratic, ends included\n\nA stretched axis is equally exact, the weights being built per sample:\n\nxs = [0.0, 0.11, 0.37, 0.9, 1.05, 1.6, 1.62, 2.0]\nouts = similar(xs)\nO.apply_stencil!(outs, (@. 3xs^2 - 2xs + 5), xs, 1; order = 1, nodes = 3)\nmaximum(abs, outs .- (6xs .- 2))\n\nGiven a period the stencil stays centred and wraps, carrying the wrapped samples' coordinates across the seam so the spacing there is the true one — the seam is then no worse than the interior:\n\nλ = collect(range(0, 2π; length = 65)[1:64])\no = similar(λ)\nO.apply_stencil!(o, sin.(λ), λ, 1; order = 1, nodes = 5, period = 2π)\nmaximum(abs, o .- cos.(λ)), abs(o[1] - cos(λ[1]))\n\nThe grid form takes the axis, the wrap period and the mask from the grid, so a periodic direction wraps without being told. Where a mask bites, a cell whose stencil reads an inactive sample is written as masked:\n\ngeo = FG.Geometry.CartesianGeometry()\nX = collect(range(0.0, 1.0; length = 9)); Y = collect(range(0.0, 2.0; length = 7))\nF = [xi^2 + 3yi for xi in X, yi in Y]\nmk = trues(9, 7); mk[5, 3] = false\ngm = FG.Grids.StructuredGrid(geo, X, Y, mk)\nOm = similar(F)\nO.apply_stencil!(Om, F, gm, 1; order = 1, nodes = 3, masked = NaN)\nOm[3:7, 3]                               # the masked cell and the two that read it","title":"Applying a weight set"},{"category":"section","location":"discretization/#What-a-mask-edge-does-to-the-stencil","page":"Discretization","text":"Blanking is the default, and it is not free: a cell is blanked when any sample its window reads is inactive, so a single masked cell takes out up to nodes - 1 cells either side of it. A five-point derivative on a short axis can be annihilated outright by one hole.\n\nThe window already shifts inward at the end of an axis, precisely so the node count — and the accuracy order — survives a boundary. The end of an active run is the same situation, and ShiftWithinRun treats it that way:\n\nxr = collect(0.0:1.0:6.0)\nmr = trues(7, 1); mr[4, 1] = false\ngr = FG.Grids.StructuredGrid(geo, xr, [0.0], mr)\nfr = reshape(collect(0.0:6.0), 7, 1)         # f = x, so df/dx is exactly 1\n\nblanked  = zeros(7, 1); shifted = zeros(7, 1)\nO.apply_stencil!(blanked, fr, gr, 1; order = 1, nodes = 5, masked = NaN)\nO.apply_stencil!(shifted, fr, gr, 1; order = 1, nodes = 5, masked = NaN,\n                 policy = O.ShiftWithinRun())\nvec(blanked), vec(shifted)\n\nEvery cell is blanked in the first, and every active cell is exact in the second. The policies are:\n\npolicy at a run edge\nBlankMasked the default — write masked wherever the window reads an inactive sample\nShiftWithinRun shift the window to fit inside the run, keeping nodes and the accuracy order; masked only where the run is shorter than nodes\nReduceInRun as above, and where the run cannot hold nodes, use the largest window it can, down to order + 1\n\nReduceInRun is the one policy that lowers the accuracy order without saying so, hence its own name: a narrow strait wants an answer at reduced order, and elsewhere ShiftWithinRun reports a run too short by writing masked.\n\nA cell whose window already lies inside its run reuses the precomputed row, so a run's interior is bit-for-bit what BlankMasked gives, and only the cells within nodes - 1 of an edge cost anything extra. A run that wraps a periodic seam is a single run.\n\nBuild the weights once with axis_stencils to reuse them across many fields; applying a precomputed set allocates nothing.\n\nidx, w = D.axis_stencils(X, 1, 3)\nsize(idx), size(w)","title":"What a mask edge does to the stencil"},{"category":"section","location":"discretization/#The-physical-derivative","page":"Discretization","text":"apply_stencil! differentiates with respect to a coordinate. On any curved geometry that is not the derivative a physical law is written in — that one is per unit distance, ∂f/∂sᵈ = (1/hᵈ)·∂f/∂ξᵈ. derivative! is the two together:\n\nR   = 6.371e6\nsph = FG.Geometry.SphericalGeometry(R)\nlon = collect(range(0, 2π * (1 - 1/48); length = 48))\nlat = collect(range(-π/2, π/2; length = 25))          # both poles are rows of this grid\ngs  = FG.Grids.StructuredGrid(sph, lon, lat)\nfs  = [sin(φ) for _ in lon, φ in lat]                 # ∂/∂north should be cos(φ)/R\ndn  = zeros(48, 25)\nO.derivative!(dn, fs, gs, 2; order = 1, nodes = 5, masked = NaN)\ndn[1, 12], cos(lat[12]) / R\n\nWhere the metric degenerates the derivative does not exist, and masked is written. Longitude at a pole is that case — h_λ = R cos φ → 0:\n\nde = zeros(48, 25)\nO.derivative!(de, [sin(λ) * cos(φ) for λ in lon, φ in lat], gs, 1;\n              order = 1, nodes = 5, masked = NaN)\nall(isnan, de[:, 1]), all(isnan, de[:, 25]), any(isnan, de[:, 2:24])\n\nThe threshold is metric_floor, L·√eps(T) — relative to the geometry's size and to the element type. An absolute constant cannot serve both: 1e-12 is below eps(Float32), and in Float32 cos(Float32(π/2)) ≈ -4.4e-8, so h_λ at the pole is around 0.28 metres and a fixed small threshold never fires.\n\nA divergence or a curl remains the caller's to assemble, needing a result location and a boundary-condition policy this does not choose. When you do, note the flux form — on a sphere\n\nnablacdotmathbfu = frac1Rcosvarphileftfracpartial u_lambdapartiallambda\n                        + fracpartial (u_varphicosvarphi)partialvarphiright\n\nso the second term differentiates u_φ·cos φ, not u_φ. Adding two physical derivatives is a different expression, and a wrong one.","title":"The physical derivative"},{"category":"section","location":"discretization/#Off-a-rectilinear-grid:-the-least-squares-gradient","page":"Discretization","text":"apply_stencil! needs a separable axis to difference along. A CurvilinearGrid has none, and a node set's neighbours come from connectivity, so neither has a stencil. Operators.gradient_plan builds a least-squares gradient for them, and gradient! applies it:\n\ncart = FG.Geometry.CartesianGeometry{Float64}()\nnn   = 14\nxg   = [t + 0.35u for t in range(0, 10; length = nn), u in range(0, 6; length = nn)]   # sheared\nyg   = [u - 0.2t  for t in range(0, 10; length = nn), u in range(0, 6; length = nn)]\ncgrid = FG.Grids.CurvilinearGrid(cart, xg, yg, trues(nn, nn); measure = fill(1.0, nn, nn))\nplan  = FG.Operators.gradient_plan(cgrid)\n\nfld = 2.0 .* xg .- 3.0 .* yg .+ 7        # ∇ = (2, -3) everywhere\ng1, g2 = zeros(nn, nn), zeros(nn, nn)\nO.gradient!(g1, g2, fld, plan)\nmaximum(abs, g1 .- 2), maximum(abs, g2 .+ 3)\n\nExact for a linear field on any stencil, however skewed — the least-squares combination cancels the leading truncation term, which inverting a 2×2 index-space Jacobian does not. It is second order on locally symmetric stencils and degrades toward first on strongly skewed cells, and where the stencil is separable and orthogonal it is the centred difference, so it agrees with apply_stencil! where both apply.\n\nA depends only on the geometry, so the plan holds the per-neighbour coefficients and each apply is one dot product per cell, allocating nothing. Where A is rank deficient — every neighbour on one line, at a boundary or beside a mask — that component is zeroed, under the rule apply_stencil! states for a mask.","title":"Off a rectilinear grid: the least-squares gradient"},{"category":"section","location":"discretization/#Evaluating-a-field-at-a-coordinate","page":"Discretization","text":"Observational data arrives with a coordinate. interpolate evaluates a field there:\n\nxa = collect(range(0, 2; length = 11)); ya = collect(range(-1, 3; length = 9))\nga = FG.Grids.StructuredGrid(cart, xa, ya)\nfa = [2xi - 3yj + 0.5xi * yj + 7 for xi in xa, yj in ya]\nO.interpolate(fa, ga, (0.7, 1.1)), 2*0.7 - 3*1.1 + 0.5*0.7*1.1 + 7\n\nMultilinear on a rectilinear grid; a weighted least-squares plane on a curvilinear grid or a node set, which is exact for a linear field and reproduces a cell's own value at its centre. A periodic direction interpolates across its seam, so a coordinate past the last sample wraps to the first.\n\nThe mask policies mean here what they mean for a stencil: BlankMasked returns masked when a contributor is inactive, ReduceInRun renormalizes over the active ones, and ShiftWithinRun is refused, there being no window to shift.\n\nAnything that does need a convention the package has not chosen — a staggered difference, or a multi-direction operator like a divergence or a curl, which also need a result location and a boundary-condition policy — is assembled at the call site from these weights and the metric factors below.","title":"Evaluating a field at a coordinate"},{"category":"section","location":"discretization/#A-batch-of-fields-on-one-grid","page":"Discretization","text":"A field may carry trailing axes beyond the grid's own — many tracers, an ensemble, a time window — sharing one geometry. Those axes are batch: every entry point takes them, differencing only along dim, which indexes the grid's directions.\n\ngb = FG.Grids.StructuredGrid(cart, xa, ya)\nfb = cat((fa .+ 100k for k in 1:4)...; dims = 3)      # (11, 9, 4) against a 2-D grid\nob = similar(fb)\nO.derivative!(ob, fb, gb, 1; order = 1, nodes = 3)\nsize(ob), ob[5, 4, 1] ≈ ob[5, 4, 4]                   # same derivative, offset field\n\nNothing about the grid depends on the batch, so the grid-only work — the stencil table, the metric factors, the interpolation weights, a gradient plan's coefficients — is computed once and reused across it. One pass over the batch is therefore less work than a pass per slice.\n\nIt also matters for a device backend. apply_stencil! launches over the whole output, batch included, so a batched call is one launch over prod(spatial) · prod(batch) work items where a slice loop is one launch each over prod(spatial). A 64×64 slice is 4096 work items, well short of saturating a GPU; the batch is the axis with the parallelism to fill it.\n\nEvaluating a batch at a coordinate gives one value per element, through the ! form or its allocating wrapper as elsewhere in the package:\n\nout = Vector{Float64}(undef, 4)\nO.interpolate!(out, fb, gb, (0.7, 1.1))\nout ≈ O.interpolate(fb, gb, (0.7, 1.1))               # the allocating form agrees\n\nAn unbatched field still returns a scalar — the rank decides, so nothing is inferred from a length at run time. A mask stays the grid's own shape and applies to every element; a field whose spatial extent disagrees with the grid is still an error, and so is asking to difference along a batch axis.","title":"A batch of fields on one grid"},{"category":"section","location":"discretization/#Metric-factors","page":"Discretization","text":"G = FG.Geometry\nsph = G.SphericalGeometry(2.0)\nG.scale_factors(sph, (0.0, π/3)), G.scale_factors(sph, (0.0, π/3, 5.0))\n\nscale_factors gives the physical length of a unit coordinate step in each direction — (R cosφ, R) on a sphere's surface, (r cosφ, r, 1) with a radius direction, and 1 throughout for a Cartesian metric. They turn a coordinate derivative into a physical one, ∂/∂sᵈ = (1/hᵈ)·∂/∂ξᵈ, so a divergence or a curl is assembled from these plus fd_weights with its conventions stated at the call site.\n\nG.jacobian(sph, (0.0, 0.5)), 4cos(0.5)\n\njacobian is their product: the volume element per unit coordinate volume, and the quantity the grid's own cell measure is built from.\n\nAn ellipsoidal metric supplies its own curvature radii, and nothing else in the stack changes:\n\nwgs = G.SpheroidGeometry()\nG.scale_factors(wgs, (0.0, 0.0)), G.meridional_radius(wgs, π/2) > wgs.a","title":"Metric factors"},{"category":"section","location":"sampling/#sampling-page","page":"Spherical Sampling","text":"A sampling answers: where are the points? It is independent of the metric and of how the data is stored.","title":"Spherical Sampling"},{"category":"section","location":"sampling/#The-families","page":"Spherical Sampling","text":"sampling layout points notes\nGaussLegendreSampling tensor product (2N−1) × N exact quadrature to lmax = N−1\nDriscollHealySampling tensor product 2N × N equiangular, includes the north pole\nDriscollHealyEqualSampling tensor product N × N the square DH1 layout\nClenshawCurtisSampling tensor product (2N−1) × N open — no polar points\nMcEwenWiauxSampling tensor product (2L−1) × L ~half the samples of classical DH\nLatLonSampling tensor product arbitrary regional or global; no spectral claim\nHEALPixSampling iso-latitude rings 12·nside² equal area by construction\nCubedSphereSampling 6 panels 6n² quasi-uniform\nIcosahedralSampling geodesic 10ν²+2 quasi-uniform; hexagonal dual\nYinYangSampling 2 overlapping panels 2·nlon·nlat no polar singularity\nScatteredSphericalSampling arbitrary any for NUFFT-style paths\n\n(Image: Six spherical samplings)\n\nGauss–Legendre and Clenshaw–Curtis put their points on iso-latitude rings that crowd toward the poles; HEALPix, the cubed sphere and the icosahedral geodesic are quasi-uniform. Yin–Yang is two overlapping panels, neither of which reaches a pole.","title":"The families"},{"category":"section","location":"sampling/#Traits","page":"Spherical Sampling","text":"Ask a sampling what it is, and do not test its type:\n\nFG.SphericalSampling.is_tensor_product(s)                       # fits an (nlon × nlat) structured grid?\nFG.SphericalSampling.is_iso_latitude(s)                          # points lie on rings of constant φ?\nFG.SphericalSampling.is_equal_area(s)                            # every cell the same area?\nFG.SphericalSampling.admits_exact_bandlimited_quadrature(s)      # weights integrate products exactly at `bandlimit`?\n\nThat last one is strict. Spectral analysis forms products of two degree-lmax fields, so it needs exactness to degree 2·lmax:\n\nsampling exact for a single Pₗ bandlimit needs exact?\nGauss–Legendre 2N−1 N−1 2N−2 yes\nDriscoll–Healy N−1 N/2−1 N−2 yes\nClenshaw–Curtis N−1 N−1 2N−2 no\n\nClenshaw–Curtis's band limit describes what its grid can represent; its quadrature only supports analysis to about (N−1)/2. Use Gauss–Legendre when the analysis has to be exact.","title":"Traits"},{"category":"section","location":"sampling/#Sizing","page":"Spherical Sampling","text":"FG.SphericalSampling.bandlimit(s, nlat)              # lmax this grid resolves\nFG.SphericalSampling.nlat_for_bandlimit(s, 15)       # the inverse\nFG.SphericalSampling.nlon_for_nlat(s, nlat)          # the matching longitude count\nFG.SphericalSampling.axes_lengths(s, nlat)           # (; nlon, nlat)\nFG.SphericalSampling.npoints(s, nlat)                # total points","title":"Sizing"},{"category":"section","location":"sampling/#Axes,-points-and-weights","page":"Spherical Sampling","text":"Tensor-product samplings give separable axes; the rest give point lists.\n\nax = FG.SphericalSampling.spherical_axes(FG.SphericalSampling.GaussLegendreSampling(), 64)      # (; λ, φ) — 127 and 64 long\nw  = FG.SphericalSampling.latitude_weights(FG.SphericalSampling.GaussLegendreSampling(), 64)    # 64 weights, Σw = 2\n\nIf you need both, ask for both. Gauss–Legendre nodes and weights come out of one root solve; requesting them separately solves twice:\n\nq = FG.SphericalSampling.spherical_quadrature(FG.SphericalSampling.GaussLegendreSampling(), 64) # (; λ, φ, w) — one solve\n\nEvery one of these has an in-place form that writes into your buffers and allocates nothing beyond its return value: spherical_axes!, latitude_weights!, spherical_quadrature!, spherical_points!.\n\nλ = Vector{Float64}(undef, 127); φ = Vector{Float64}(undef, 64); w = similar(φ)\nFG.SphericalSampling.spherical_quadrature!(λ, φ, w, FG.SphericalSampling.GaussLegendreSampling(), 64)\nsum(w)\n\nWeights are normalized so Σw = ∫₀^π sinθ dθ = 2 for every sampling that has them. McEwen–Wiaux has none: its quadrature is built on an extension of the sphere to a torus, and the sine-series rule at its nodes is not exact even at l = 0, so asking for them raises.","title":"Axes, points and weights"},{"category":"section","location":"sampling/#Sampling-specific-constructors","page":"Spherical Sampling","text":"FG.SphericalSampling.cubed_sphere_points(n).panel |> unique     # 6n² cell centres + panel ids\nlength(FG.SphericalSampling.icosahedral_mesh(ν).triangles)      # 20ν² triangles\nlength(FG.SphericalSampling.icosahedral_vertices(ν).λ)          # 10ν²+2 points, no topology built\nsize(FG.SphericalSampling.yin_yang_panels(nlon, nlat).yang.λ)\nFG.SphericalSampling.healpix_npix(nside), FG.SphericalSampling.healpix_nring(nside)\n\nyin_yang_panels returns two different shapes. The yin panel is a separable lat–lon patch in its own frame, so it is a pair of axes; yang is that panel rotated onto the sphere, separable in neither global longitude nor latitude, so it is a pair of nlon × nlat fields.\n\nicosahedral_vertices! writes the points straight into your buffers with no mesh built at all, so its allocation count does not grow with ν.","title":"Sampling-specific constructors"},{"category":"section","location":"sampling/#Ring-grids-and-quasi-uniform-lattices","page":"Spherical Sampling","text":"A reduced Gaussian grid keeps the Gaussian latitudes but gives each ring only as many longitudes as its circumference warrants, so it is not a tensor product:\n\noct = FG.SphericalSampling.OctahedralGaussianSampling(80)     # ECMWF octahedral, N = 80\nFG.SphericalSampling.nrings(oct), FG.SphericalSampling.npoints(oct), 4 * 80 * (80 + 9)\n\nfirst(FG.SphericalSampling.nlon_per_ring(oct), 4)             # 20 at the pole, +4 per ring\n\nIts latitude weights are Gauss–Legendre's, so the quadrature is exact; the longitude factor varies by ring because the ring populations do:\n\nw = FG.SphericalSampling.latitude_weights(oct)\ncounts = FG.SphericalSampling.nlon_per_ring(oct)\nsum(w), sum(w[j] * (2π / counts[j]) * counts[j] for j in eachindex(counts)) / 4π","title":"Ring grids and quasi-uniform lattices"},{"category":"section","location":"sampling/#Walking-a-map-ring-by-ring","page":"Spherical Sampling","text":"nlon_per_ring builds the whole table. To take rings one at a time, nlon_in_ring and ring_range answer for a single ring in O(1) and allocate nothing, so a per-ring transform or a zonal reduction carries no running offset and no table:\n\nSS = FG.SphericalSampling\npts = SS.spherical_points(oct)\nzonal = [sum(view(pts.φ, SS.ring_range(oct, r))) / SS.nlon_in_ring(oct, r)\n         for r in 1:SS.nrings(oct)]\nzonal[1], zonal[end]                  # each ring is at one latitude, so this is that latitude\n\nBoth are defined for every sampling laid out in rings — the octahedral and tabulated reduced Gaussians, HEALPix, and the tensor-product grids, which take their nlat first:\n\nSS.ring_range(SS.HEALPixSampling(4), 3), SS.ring_range(SS.GaussLegendreSampling(), 8, 3)\n\nThe Fibonacci lattice spreads points one per equal-area band with a golden-angle longitude step, which gives a quasi-uniform set with no polar clustering and no panel seams:\n\nfib = FG.SphericalSampling.spherical_points(FG.SphericalSampling.FibonacciSampling(1000))\nextrema(diff(sin.(fib.φ)))            # exactly equal steps in z","title":"Walking a map ring by ring"},{"category":"section","location":"sampling/#HEALPix-pixel-indexing","page":"Spherical Sampling","text":"SS = FG.SphericalSampling\nθ, ϕ = SS.pix2ang(8, 100)             # RING by default\nSS.ang2pix(8, θ, ϕ)\n\nSS.ring2nest(8, 100), SS.nest2ring(8, SS.ring2nest(8, 100))\n\nNested() selects the quadtree ordering, which needs nside to be a power of two; Ring() works for any nside. pix2vec/vec2pix are the same maps through a unit vector.\n\nring_info describes a whole iso-latitude ring at once, so a map can be walked ring by ring without decoding every pixel. Ring widths run 4, 8, … through the polar cap, hold at 4·nside across the equatorial belt, then shrink symmetrically:\n\n[SS.ring_info(4, r).ringpix for r in 1:(4 * 4 - 1)]\n\ninfo = SS.ring_info(4, 6)\ninfo.startpix, info.ringpix, info.latitude, info.shifted\n\nThe rings tile the map exactly — startpix is contiguous and the widths sum to 12·nside²:\n\nrings = [SS.ring_info(4, r) for r in 1:15]\nsum(i -> i.ringpix, rings) == 12 * 4^2, rings[1].startpix == 0","title":"HEALPix pixel indexing"},{"category":"section","location":"sampling/#Points-sit-at-cell-centres","page":"Spherical Sampling","text":"Every sampling here places points at cell centres. Sampling the panel edges makes adjacent panels emit coincident points while the connectivity folds those same edges onto a different panel; points and topology then disagree, and a grid built from them carries duplicate nodes and zero-area cells. Cell centres also give the degenerate sizes (n = 1, nlon = 1) from the same formula, with no special case.","title":"Points sit at cell centres"},{"category":"section","location":"api/sampling/#Spherical-sampling","page":"Spherical sampling","text":"","title":"Spherical sampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.AbstractEquiangularAlgorithm","page":"Spherical sampling","text":"AbstractEquiangularAlgorithm\n\nWhich construction computes an equiangular node family's sine sums: Recurrence, always available, or Transform, which needs an FFT planner.\n\nA type, as the mask policies are. The two agree to round-off, so naming one pins a result across machines that differ in whether an FFT backend is installed.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.AbstractEquiangularAlgorithm"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.AbstractLatLonSampling","page":"Spherical sampling","text":"AbstractLatLonSampling <: AbstractTensorProductSphericalSampling\n\nGeophysical / model latitude–longitude grids. No exact band-limited SHT claim.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.AbstractLatLonSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.AbstractReducedGaussianSampling","page":"Spherical sampling","text":"AbstractReducedGaussianSampling <: AbstractRingSampling\n\nRing samplings on Gaussian latitudes. Default: OctahedralGaussianSampling.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.AbstractReducedGaussianSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.AbstractRingSampling","page":"Spherical sampling","text":"AbstractRingSampling <: AbstractSphericalSampling\n\nIso-latitude rings whose longitude count varies by ring, so the layout is not a tensor product. A reduced Gaussian grid is the canonical case: latitudes are the Gaussian ones, but each ring carries only as many longitudes as its circumference warrants.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.AbstractRingSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.AbstractSpectralQuadratureSampling","page":"Spherical sampling","text":"AbstractSpectralQuadratureSampling <: AbstractTensorProductSphericalSampling\n\nSamplings with a known exact quadrature for band-limited spherical harmonics (up to a documented lmax ↔ grid-size relation).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.AbstractSpectralQuadratureSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.AbstractSphericalSampling","page":"Spherical sampling","text":"AbstractSphericalSampling\n\nHow points are placed on the sphere. Orthogonal to FlowGeometries.Geometry.AbstractSphericalGeometry (the metric / radius) and to grid architecture (StructuredGrid vs unstructured).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.AbstractSphericalSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.AbstractTensorProductSphericalSampling","page":"Spherical sampling","text":"AbstractTensorProductSphericalSampling <: AbstractSphericalSampling\n\nIso-latitude × equispaced-longitude (or arbitrary lon/lat) layouts that fit a 2D StructuredGrid with axes (λ, φ).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.AbstractTensorProductSphericalSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.ClenshawCurtisSampling","page":"Spherical sampling","text":"ClenshawCurtisSampling <: AbstractClenshawCurtisSampling\n\nOpen equiangular Clenshaw–Curtis grid: θ_i = π(i12)N_θ (no poles), N_λ = 2N_θ  1, l_max = N_θ  1.\n\nThis is not the same as Driscoll–Healy: different N_θ(l_max), open vs polar-cap nodes, and a different quadrature.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.ClenshawCurtisSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.CubedSphereSampling","page":"Spherical sampling","text":"CubedSphereSampling <: AbstractCubedSphereSampling\n\nInscribed-cube projection: six logically rectangular panels (gnomonic by default). Quasi-uniform alternative to global lat–lon (e.g. FV3).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.CubedSphereSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.DriscollHealyEqualSampling","page":"Spherical sampling","text":"DriscollHealyEqualSampling <: AbstractDriscollHealySampling\n\nDriscoll–Healy DH1 layout: N_θ  N_θ with N_θ = 2(l_max+1), Δθ = πN_θ, Δλ = 2πN_θ.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.DriscollHealyEqualSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.DriscollHealySampling","page":"Spherical sampling","text":"DriscollHealySampling <: AbstractDriscollHealySampling\n\nDriscoll–Healy equiangular grid, rectangular (DH2) layout: N_θ  2N_θ with N_θ = 2(l_max+1), Δθ = πN_θ, Δλ = πN_θ. Includes the north pole band, excludes the south (weight at the north pole is zero).\n\nSee also DriscollHealyEqualSampling for the square DH1 N_θ  N_θ layout.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.DriscollHealySampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.FibonacciSampling","page":"Spherical sampling","text":"FibonacciSampling(n)\n\nn points on the spherical Fibonacci (golden-spiral) lattice: z_k = (2k+1)/n - 1 with λ_k = 2πk/φ mod 2π, φ the golden ratio.\n\nz advances in equal steps, so the points are spread one per equal-area band, and the golden angle in longitude — the most irrational rotation there is — keeps them from lining up into visible spokes at any n. Quasi-uniform with no polar clustering and no panel seams.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.FibonacciSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.GaussLegendreSampling","page":"Spherical sampling","text":"GaussLegendreSampling <: AbstractGaussLegendreSampling\n\nGauss–Legendre (Gauss–Neumann) latitudes: μ = cosθ at the N_θ roots of P_N_θ, with N_λ = 2N_θ  1 equispaced longitudes. Exact for band-limit l_max = N_θ  1.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.GaussLegendreSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.HEALPixSampling","page":"Spherical sampling","text":"HEALPixSampling <: AbstractHEALPixSampling\n\nHierarchical Equal Area isoLatitude Pixelisation (Górski et al.). Equal-area pixels on 4 N_mathrmside  1 iso-latitude rings; not a tensor-product N_λ  N_φ array (n_λ varies by ring).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.HEALPixSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.IcosahedralSampling","page":"Spherical sampling","text":"IcosahedralSampling <: AbstractIcosahedralSampling\n\nIcosahedral geodesic discretization (triangular faces; hexagonal dual is the Voronoi mesh). Quasi-uniform; unstructured connectivity.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.IcosahedralSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.LatLonSampling","page":"Spherical sampling","text":"LatLonSampling <: AbstractLatLonSampling\n\nArbitrary model lat–lon (regional or global, poles optional). No spectral exactness claim.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.LatLonSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.McEwenWiauxSampling","page":"Spherical sampling","text":"McEwenWiauxSampling <: AbstractMcEwenWiauxSampling\n\nMcEwen–Wiaux (2011) equiangular sampling: θ_t = π(2t+1)(2L1) for t = 0L1, λ_p = 2π p(2L1) for p = 02L2, with L = l_max+1. Requires asymptotically  2 L^2 samples (about half of classical DH).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.McEwenWiauxSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.Nested","page":"Spherical sampling","text":"Nested()\n\nPixels numbered so that each is subdivided into four contiguous children — a quadtree per base face. The ordering that makes a neighbourhood contiguous. Requires nside to be a power of two.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.Nested"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.OctahedralGaussianSampling","page":"Spherical sampling","text":"OctahedralGaussianSampling(N)\n\nOctahedral reduced Gaussian grid with N latitude rings between pole and equator (2N rings in all).\n\nRing i counted from either pole carries 4i + 16 longitudes — 20 on the ring nearest the pole and four more on each successive ring — which totals 4N(N+9) points. Latitudes are the 2N Gaussian latitudes, so the latitude quadrature is the Gauss–Legendre one.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.OctahedralGaussianSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.OpenNodes","page":"Spherical sampling","text":"OpenNodes(), ClosedNodes()\n\nThe two equiangular colatitude families: open θᵢ = π(i−½)/N (Clenshaw–Curtis) and closed θᵢ = π(i−1)/N (Driscoll–Healy). They are types, so the sum below dispatches on which one it has.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.OpenNodes"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.Recurrence","page":"Spherical sampling","text":"Recurrence()\n\nThe angle-addition recurrence: O(nlat·nterm), no dependency, defined for every element type.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.Recurrence"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.ReducedGaussianSampling","page":"Spherical sampling","text":"ReducedGaussianSampling(nlon_per_ring)\n\nReduced Gaussian grid with an explicit longitude count per ring, north to south.\n\nThe classical reduced grids are published as tables — the count holds constant across blocks of latitudes and jumps between them — so the table is the input. Use OctahedralGaussianSampling for the octahedral rule, which is a formula.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.ReducedGaussianSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.Ring","page":"Spherical sampling","text":"Ring()\n\nPixels numbered along iso-latitude rings, north to south and east within a ring. The ordering that makes a ring contiguous, so a longitude transform per ring is possible.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.Ring"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.RingScheme","page":"Spherical sampling","text":"RingScheme\n\nWhich HEALPix pixel ordering is meant: Ring or Nested.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.RingScheme"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.ScatteredSphericalSampling","page":"Spherical sampling","text":"ScatteredSphericalSampling <: AbstractScatteredSphericalSampling\n\nArbitrary (λ φ) point set (NUFFT / NUFSHT paths).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.ScatteredSphericalSampling"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.Transform","page":"Spherical sampling","text":"Transform()\n\nOne length-nlat backward transform: O(nlat·log nlat). Implemented by the AbstractFFTs extension, and raises without one — a planner has to exist for this to mean anything.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.Transform"},{"category":"type","location":"api/sampling/#FlowGeometries.SphericalSampling.YinYangSampling","page":"Spherical sampling","text":"YinYangSampling <: AbstractYinYangSampling\n\nOverset of two low-latitude lat–lon patches (Kageyama–Sato), avoiding polar singularities while keeping structured coordinates on each panel.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.YinYangSampling"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._cubed_cell_angles-Union{Tuple{T}, Tuple{Type{T}, Int64, Int64, Int64}} where T<:AbstractFloat","page":"Spherical sampling","text":"_cubed_cell_angles(::Type{T}, n, i, j) -> (ξ, η)\n\nPanel-local gnomonic angles of cell (i, j)'s centre: ξ = -π/4 + (i - ½)·(π/2)/n.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._cubed_cell_angles"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._cubed_cell_edges-Union{Tuple{T}, Tuple{Type{T}, Int64, Int64, Int64}} where T<:AbstractFloat","page":"Spherical sampling","text":"_cubed_cell_edges(::Type{T}, n, i, j) -> (X1, X2, Y1, Y2)\n\nTangents of cell (i, j)'s panel-local boundary angles, -π/4 + (i-1)·Δ and -π/4 + i·Δ. These are the gnomonic-plane coordinates its area is the solid angle of.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._cubed_cell_edges"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._cubed_cell_lonlat-Union{Tuple{T}, Tuple{Type{T}, Vararg{Int64, 4}}} where T<:AbstractFloat","page":"Spherical sampling","text":"_cubed_cell_lonlat(::Type{T}, n, f, i, j) -> (λ, φ)\n\nCell (f, i, j)'s centre in global (λ, φ): its panel-local angles through the gnomonic map and onto the sphere.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._cubed_cell_lonlat"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._cubed_lin-NTuple{4, Int64}","page":"Spherical sampling","text":"_cubed_lin(f, i, j, n) -> Int\n_cubed_unlin(lin, n) -> (f, i, j)\n\nThe cubed sphere's cell numbering and its inverse: panel by panel, i fastest within a panel. This is the order cubed_sphere_points! writes, so it is what every consumer of those points indexes by.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._cubed_lin"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._equiangular_algorithm-Tuple{Type}","page":"Spherical sampling","text":"_equiangular_algorithm(T) -> AbstractEquiangularAlgorithm\n\nThe default for element type T: Recurrence unless a loaded extension can plan a transform for it. The single place availability is consulted, so no method below branches on it.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._equiangular_algorithm"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._equiangular_sums!-Union{Tuple{T}, Tuple{AbstractVector{T}, Any, Int64, Int64, FlowGeometries.SphericalSampling.Recurrence}} where T<:AbstractFloat","page":"Spherical sampling","text":"The recurrence: sin((2k+1)θ) = 2cos(2θ)·sin((2k−1)θ) − sin((2k−3)θ), seeded with s₋₁ = −sin θ, s₀ = sin θ, so each term costs two multiplies and no transcendental.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._equiangular_sums!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._equiangular_sums!-Union{Tuple{T}, Tuple{AbstractVector{T}, Any, Int64, Int64}} where T<:AbstractFloat","page":"Spherical sampling","text":"_equiangular_sums!(s, family, nlat, nterm[, algorithm]) -> s\n\ns[i] = Σ_{k=0}^{nterm-1} sin((2k+1)·θᵢ)/(2k+1) for the given node family.\n\nWith no algorithm, _equiangular_algorithm chooses one for the element type.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._equiangular_sums!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._equiangular_weights!-Union{Tuple{T}, Tuple{AbstractVector{T}, Any, Int64}, Tuple{AbstractVector{T}, Any, Int64, FlowGeometries.SphericalSampling.AbstractEquiangularAlgorithm}} where T<:AbstractFloat","page":"Spherical sampling","text":"_equiangular_weights!(w, family, nlat[, algorithm]) -> w\n\nLatitude quadrature weights for an equiangular node set, from the sine-series expansion of the sinθ Jacobian:\n\nwᵢ = (4/N)·sinθᵢ·Σ_{k=0}^{⌊N/2⌋} sin((2k+1)θᵢ)/(2k+1)\n\nfamily is OpenNodes or ClosedNodes, so one construction serves both. Weights sum to ∫₀^π sinθ dθ = 2 — see latitude_weights for why every sampling here uses that normalization.\n\nalgorithm selects which construction computes the sums; omitted, the element type's default does.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._equiangular_weights!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._gauss_legendre!-Union{Tuple{T}, Tuple{Type{T}, Union{Nothing, AbstractVector}, Union{Nothing, AbstractVector}, Int64}} where T<:AbstractFloat","page":"Spherical sampling","text":"_gauss_legendre!(T, μ, w, n)\n\nCore solve. Either output may be nothing, so a caller that wants only the nodes or only the weights needs no scratch vector for the other half.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._gauss_legendre!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._gauss_legendre_newton!-Union{Tuple{TW}, Tuple{T}, Tuple{Type{T}, Type{TW}, Union{Nothing, AbstractVector}, Union{Nothing, AbstractVector}, Int64, Int64}} where {T<:AbstractFloat, TW<:AbstractFloat}","page":"Spherical sampling","text":"_gauss_legendre_newton!(T, TW, μ, w, n, m)\n\nNodes and weights by Newton on Pₙ, evaluated by the Bonnet recurrence from a Tricomi start.\n\nO(n) per root, so O(n²) overall — but it is exact arithmetic converging to eps(TW), which the asymptotic expansion's fixed Float64 coefficient set cannot do. That makes it the right method for wide element types at any n, and for small n, where the expansion is inaccurate and the quadratic cost is microseconds.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._gauss_legendre_newton!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._gauss_legendre_μ!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}}} where T<:AbstractFloat","page":"Spherical sampling","text":"_gauss_legendre_μ!(μ, w) -> NamedTuple{(:μ,:w)}\n\nWrite n = length(μ)-point Gauss–Legendre nodes/weights on μ  (-1 1) into the provided buffers, ascending in μ (length(μ) == length(w)).\n\nNewton's method on Pₙ, evaluated by the Bonnet recurrence, from a Tricomi starting estimate accurate to O(n³) — so 2–4 iterations reach machine precision. Roots come in  pairs, so only the upper half is solved.\n\nNeeds O(1) scratch. Time is O(n²): each of the n2 roots costs an O(n) recurrence. The Bogaert-style asymptotic expansions below are the O(n) path.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._gauss_legendre_μ!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._healpix_nested_cloud!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}, Integer}} where T<:AbstractFloat","page":"Spherical sampling","text":"_healpix_nested_cloud!(λ, φ, nside) -> (; λ, φ)\n\nThe whole HEALPix cloud in NESTED pixel order.\n\nThe ring quantities are tabulated once, 4·nside − 1 of them, so the acos and the latitude conversion run per ring here too. A nested pixel's ring and position along it come from its face coordinates (_hp_xyf2ringj), and the coordinates are then the ring walk's own expressions, so the two orderings hold the same numbers to the bit. Both writes are sequential.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._healpix_nested_cloud!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._hp_ang2xyf-Union{Tuple{T}, Tuple{Int64, T, T}} where T<:AbstractFloat","page":"Spherical sampling","text":"_hp_ang2xyf(nside, θ, ϕ) -> (ix, iy, face)\n\nFace-local coordinates of the pixel containing colatitude θ, longitude ϕ, by the HEALPix projection (Górski et al. 2005). Division and remainder, so it holds for any nside, including one that is not a power of two.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._hp_ang2xyf"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._ico_decode-Tuple{Int64, Int64}","page":"Spherical sampling","text":"_ico_decode(id, ν) -> (kind, a, b)\n\nWhich entity owns vertex id: kind = 1 a corner (a the corner, b unused), 2 a macro-edge interior (a the edge, b its position from the edge's low corner), 3 a face interior (a the face, b its position in the face's own lattice walk).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._ico_decode"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._ico_face_ij-Tuple{Int64, Int64}","page":"Spherical sampling","text":"_ico_face_ij(k, ν) -> (i, j)\n\nThe face-interior lattice node whose position in the walk for i in 1:(ν-1), j in 1:(ν-1-i) is k.\n\nRow i of the walk holds ν-1-i nodes, so the nodes before it number S(i-1) = (i-1)(ν-1) - (i-1)i/2. Then m = i-1 is the largest value with S(m) < k, and j = k - S(m). S(m) < k rearranges to m² - m(2ν-3) + 2k > 0, so the smaller root of that quadratic brackets m; its floor is taken and then stepped in either direction, so the result does not depend on the square root's last bit.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._ico_face_ij"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._ico_fold_incident_triangles-Union{Tuple{F}, Tuple{F, Any, Vararg{Int64, 4}}} where F","page":"Spherical sampling","text":"_ico_fold_incident_triangles(f, acc, i, j, face, ν) -> acc\n\nThread acc = f(acc, other1, other2) over the triangles of one face that contain lattice node (i, j), other1 and other2 being the other two vertices' ids.\n\nA triangle belongs to exactly one face, so folding over each of a vertex's occurrences visits each incident triangle once. The two lattice orientations give up to three triangles each.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._ico_fold_incident_triangles"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._ico_lattice_id-NTuple{4, Int64}","page":"Spherical sampling","text":"_ico_lattice_id(f, i, j, ν) -> Int\n\n_ico_node_id with the load-time constants supplied: the global id of face f's barycentric lattice node (i, j).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._ico_lattice_id"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._ico_node_id-Tuple{Int64, Tuple{Int64, Int64, Int64}, Int64, Int64, Int64, AbstractMatrix{Int64}, Int64, Int64}","page":"Spherical sampling","text":"_ico_node_id(f, face, i, j, ν, edge_index, nint, face_base) -> Int\n\nGlobal vertex id of barycentric lattice node (i, j) (with i + j ≤ ν, weights ν-i-j, i, j on the face's corners A, B, C) of face f. A node on a corner or a macro-edge resolves to that shared entity's id, so the two faces meeting at an edge agree with no lookup table.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._ico_node_id"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._ico_occurrences-Tuple{Int64, Int64}","page":"Spherical sampling","text":"_ico_occurrences(id, ν) -> (NTuple{5,NTuple{3,Int}}, n)\n\nEvery (face, i, j) lattice position vertex id occupies, in the first n entries. A face interior has one, a macro-edge interior two, a corner five; the twelve corners are the geodesic sphere's twelve pentagons.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._ico_occurrences"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._ico_vertex_dir-Union{Tuple{T}, Tuple{Type{T}, Int64, Int64}} where T<:AbstractFloat","page":"Spherical sampling","text":"_ico_vertex_dir(::Type{T}, id, ν) -> NTuple{3,T}\n\nVertex id's unit direction, from the entity that owns it: a base corner, a normalized point along a macro-edge, or a normalized barycentric combination on a face. The same expressions icosahedral_vertices! writes, one vertex at a time.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._ico_vertex_dir"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._icosahedron_base-Union{Tuple{Type{T}}, Tuple{T}} where T<:AbstractFloat","page":"Spherical sampling","text":"_icosahedron_base(T) -> NTuple{12,NTuple{3,T}}\n\nThe 12 unit vertices of the base icosahedron, in the order _ICOSAHEDRON_FACES indexes them. A tuple, so it costs no allocation. Every raw vertex is a permutation of (0, ±1, ±φ) and so shares the norm √(1+φ²), formed once.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._icosahedron_base"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._put_lonlat!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}, Int64, Tuple{T, T, T}, Type{T}}} where T<:AbstractFloat","page":"Spherical sampling","text":"_put_lonlat!(λ, φ, v, p, T)\n\nNormalize p and write vertex v's longitude and latitude. Top-level, so it captures nothing.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._put_lonlat!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._yin_yang_panel_coords-Union{Tuple{T}, Tuple{Type{T}, Vararg{Int64, 4}}} where T<:AbstractFloat","page":"Spherical sampling","text":"_yin_yang_panel_coords(::Type{T}, nlon, nlat, i, j) -> (λ, φ)\n\nCell (i, j)'s centre in the panel's own frame, where the panel is the separable lat–lon patch [-3π/4, 3π/4] × [-π/4, π/4]. For yin this frame is the global one.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._yin_yang_panel_coords"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling._yin_yang_rotate-Union{Tuple{T}, Tuple{T, T}} where T<:AbstractFloat","page":"Spherical sampling","text":"_yin_yang_rotate(λ, φ) -> (λ_global, φ_global)\n\nThe Kageyama–Sato rotation carrying a panel-frame (λ, φ) onto yang's position on the sphere. Yang is yin rigidly rotated, so this is the whole difference between the two panels.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling._yin_yang_rotate"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.admits_exact_bandlimited_quadrature-Tuple{FlowGeometries.SphericalSampling.AbstractSphericalSampling}","page":"Spherical sampling","text":"admits_exact_bandlimited_quadrature(sampling) -> Bool\n\nWhether this sampling's latitude_weights integrate the products that spectral analysis actually forms — two degree-lmax functions, hence degree 2·lmax — exactly at the sampling's own bandlimit.\n\nThat is a stronger statement than \"the quadrature integrates a single P_l up to lmax\", and the distinction decides the answer here. Measured exactness of the weights in this package:\n\nsampling exact for a single P_l up to bandlimit needs 2·lmax exact?\nGauss–Legendre 2N−1 N−1 2N−2 yes\nDriscoll–Healy N−1 N/2−1 N−2 yes\nClenshaw–Curtis N−1 N−1 2N−2 no\n\nClenshaw–Curtis's band limit describes what its grid can represent; its quadrature supports quadrature-based analysis only to lmax ≈ (N−1)/2. Use GaussLegendreSampling when analysis must be exact at the stated band limit.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.admits_exact_bandlimited_quadrature"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.ang2pix-Tuple{Integer, Real, Real}","page":"Spherical sampling","text":"ang2pix(nside, θ, ϕ; scheme = Ring()) -> Int\n\nThe 0-based index of the pixel containing colatitude θ ∈ [0, π] and longitude ϕ.\n\nθ is a colatitude, matching the HEALPix convention throughout this section; use colatitude to convert a geographic latitude.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.ang2pix"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.axes_lengths-Tuple{FlowGeometries.SphericalSampling.AbstractTensorProductSphericalSampling, Integer}","page":"Spherical sampling","text":"axes_lengths(sampling, nlat; nlon=nothing) -> NamedTuple{(:nlon,:nlat)}\n\nBuffer lengths required by spherical_axes!: (; nlon, nlat).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.axes_lengths"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.bandlimit-Tuple{FlowGeometries.SphericalSampling.AbstractGaussLegendreSampling, Integer}","page":"Spherical sampling","text":"bandlimit(sampling, nlat) -> lmax\n\nMaximum spherical-harmonic degree supported by nlat latitude samples under sampling.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.bandlimit"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.colatitude-Tuple{Real}","page":"Spherical sampling","text":"Colatitude θ  0 π from geographic latitude φ  -π2 π2.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.colatitude"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.cubed_sphere_points!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}, Union{Nothing, AbstractVector{<:Integer}}, Integer}} where T<:AbstractFloat","page":"Spherical sampling","text":"cubed_sphere_points!(λ, φ, panel, n; backend=nothing) -> NamedTuple{(:λ,:φ,:panel)}\n\nGnomonic cubed-sphere cell centres into caller-owned buffers of length 6n², plus each point's panel index. Pass panel = nothing when the panel id is not wanted, and it is not computed.\n\nSee cubed_sphere_points for the allocating form.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.cubed_sphere_points!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.cubed_sphere_points-Tuple{Integer}","page":"Spherical sampling","text":"cubed_sphere_points([T = Float64], n; backend=nothing) -> NamedTuple{(:λ,:φ,:panel)}\n\nGnomonic cubed-sphere cell centres: 6n² distinct points, plus each point's panel index.\n\nAllocating wrapper around cubed_sphere_points!. Use spherical_points(CubedSphereSampling(), n) when the panel id is not needed.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.cubed_sphere_points"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.geographic_latitude-Tuple{Real}","page":"Spherical sampling","text":"Geographic latitude φ from colatitude θ.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.geographic_latitude"},{"category":"function","location":"api/sampling/#FlowGeometries.SphericalSampling.icosahedral_mesh","page":"Spherical sampling","text":"icosahedral_mesh([T = Float64], frequency) -> (; λ, φ, edges, triangles, verts)\n\nGeodesic vertices at frequency ν as both lon/lat (λ, φ) and unit vectors (verts), plus the 10ν²+2 mesh's undirected edges (i,j) with i < j and its 20ν² triangles (i,j,k) (1-based). Vertex numbering is topological — corners, then macro-edge interiors, then face interiors — so it is deterministic and every vertex is generated exactly once.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.icosahedral_mesh"},{"category":"function","location":"api/sampling/#FlowGeometries.SphericalSampling.icosahedral_vertices","page":"Spherical sampling","text":"icosahedral_vertices([T = Float64], frequency=1) -> NamedTuple{(:λ,:φ)}\n\nThe 10ν²+2 geodesic vertices as longitude/latitude, without building the mesh topology — several times faster than icosahedral_mesh at large ν, which also returns edges and triangles.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.icosahedral_vertices"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.icosahedral_vertices!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}}, Tuple{AbstractVector{T}, AbstractVector{T}, Integer}} where T<:AbstractFloat","page":"Spherical sampling","text":"icosahedral_vertices!(λ, φ, frequency = 1) -> NamedTuple{(:λ,:φ)}\n\nWrite the 10ν²+2 geodesic vertices' longitude/latitude into the caller's buffers, allocating nothing. The numbering is icosahedral_mesh's topological one — corners, then macro-edge interiors, then face interiors — so each vertex is emitted at its own index with no intermediate array and no lookup.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.icosahedral_vertices!"},{"category":"function","location":"api/sampling/#FlowGeometries.SphericalSampling.latitude_weights!","page":"Spherical sampling","text":"latitude_weights!(w, sampling, nlat) -> w\n\nFill preallocated latitude quadrature weights (length(w) == nlat). The equiangular families also take algorithm — see latitude_weights.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.latitude_weights!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.latitude_weights!-Union{Tuple{T}, Tuple{AbstractVector{T}, FlowGeometries.SphericalSampling.AbstractClenshawCurtisSampling, Integer}} where T<:AbstractFloat","page":"Spherical sampling","text":"latitude_weights!(w, ::AbstractClenshawCurtisSampling, nlat)\n\nWeights for the open nodes θᵢ = π(i−½)/N, from the same sine-series rule as the closed equiangular families.\n\nThese integrate a single P_l exactly for l ≤ N−1, which is weaker than bandlimit(ClenshawCurtisSampling(), N) = N−1 suggests: spectral analysis integrates products of two degree-lmax functions, so a quadrature exact to l ≤ N−1 supports quadrature-based analysis only up to lmax ≈ (N−1)/2. The reported band limit describes what the grid represents; this quadrature integrates to half of it. Use GaussLegendreSampling (exact to 2N−1) where analysis must be exact at the stated band limit.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.latitude_weights!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.latitude_weights-Tuple{FlowGeometries.SphericalSampling.AbstractReducedGaussianSampling}","page":"Spherical sampling","text":"latitude_weights([T = Float64], sampling::AbstractReducedGaussianSampling) -> Vector{T}\n\nGauss–Legendre weights for the grid's rings, north to south, normalized as everywhere else in this module so that Σw = 2. A full-sphere integral is then Σⱼ wⱼ (2π/nlonⱼ) Σᵢ f, the longitude factor varying by ring because the ring populations do.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.latitude_weights"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.latitude_weights-Tuple{FlowGeometries.SphericalSampling.AbstractSphericalSampling, Integer}","page":"Spherical sampling","text":"latitude_weights([T = Float64], s, nlat) -> Vector{T}\nlatitude_weights!(w, s, nlat) -> w\n\nLatitude quadrature weights wⱼ for sampling s, normalized so that\n\nΣⱼ wⱼ = ∫₀^π sinθ dθ = 2\n\nfor every sampling that provides them. The weights therefore carry the sinθ Jacobian and nothing else; the longitude factor is the caller's, so a full-sphere integral is always\n\n∫ f dΩ ≈ (2π/nlon) · Σⱼ wⱼ Σᵢ f(λᵢ, φⱼ)\n\nregardless of which sampling produced the weights. Not every sampling has them — LatLonSampling has no spectral quadrature at all, and McEwenWiauxSampling's is a different construction.\n\nThe equiangular families — Driscoll–Healy and Clenshaw–Curtis — additionally take algorithm::AbstractEquiangularAlgorithm, which pins the construction of their sine sums. The two constructions agree only to round-off, so naming one fixes the result across machines that differ in whether an FFT backend is installed. Gauss–Legendre's weights come from a root solve, so it takes no algorithm.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.latitude_weights"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.nlat_for_bandlimit-Tuple{FlowGeometries.SphericalSampling.AbstractGaussLegendreSampling, Integer}","page":"Spherical sampling","text":"nlat_for_bandlimit(sampling, lmax) -> nlat\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.nlat_for_bandlimit"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.nlon_for_nlat-Tuple{FlowGeometries.SphericalSampling.AbstractGaussLegendreSampling, Integer}","page":"Spherical sampling","text":"nlon_for_nlat(sampling, nlat) -> nlon\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.nlon_for_nlat"},{"category":"function","location":"api/sampling/#FlowGeometries.SphericalSampling.nlon_in_ring","page":"Spherical sampling","text":"nlon_in_ring(sampling, ring) -> Int\nnlon_in_ring(sampling, nlat, ring) -> Int\n\nLongitudes on a single iso-latitude ring, counted from the north pole, in O(1) and allocating nothing.\n\nThe per-ring form of nlon_per_ring, and the one a ring-by-ring loop wants: the table costs an allocation and an O(nrings) build per call, which a loop over rings pays again on every iteration if it asks for it there.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.nlon_in_ring"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.nlon_per_ring-Tuple{FlowGeometries.SphericalSampling.OctahedralGaussianSampling}","page":"Spherical sampling","text":"nlon_per_ring(sampling) -> Vector{Int}\nnlon_per_ring(sampling, nlat; nlon=nothing) -> Vector{Int}\n\nLongitudes on each iso-latitude ring, north to south.\n\nDefined for every sampling laid out in rings, so a caller walking a map ring by ring — a per-ring longitude transform, a zonal reduction, a row-wise sweep — writes one loop for all of them. A sampling that carries its own size (HEALPixSampling, the reduced Gaussians) answers from itself; a tensor-product one takes the nlat that fixes its shape, as npoints does.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.nlon_per_ring"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.npoints-Tuple{FlowGeometries.SphericalSampling.AbstractScatteredSphericalSampling, AbstractVector, AbstractVector}","page":"Spherical sampling","text":"npoints(::AbstractScatteredSphericalSampling, λ, φ) -> Int\n\nThe number of points in a scattered set, i.e. length(λ).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.npoints"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.npoints-Tuple{FlowGeometries.SphericalSampling.AbstractTensorProductSphericalSampling, Integer}","page":"Spherical sampling","text":"npoints(sampling, args...) -> Int\n\nNumber of geographic samples produced by spherical_points! / spherical_points.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.npoints"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.nrings-Tuple{FlowGeometries.SphericalSampling.OctahedralGaussianSampling}","page":"Spherical sampling","text":"nrings(sampling) -> Int\nnrings(sampling, nlat) -> Int\n\nNumber of iso-latitude rings, for any sampling laid out in them — the loop bound that goes with nlon_per_ring.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.nrings"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.pix2ang-Tuple{Integer, Integer}","page":"Spherical sampling","text":"pix2ang([T = Float64], nside, pix; scheme = Ring()) -> (θ, ϕ)\n\nColatitude and longitude of pixel pix's centre (0-based index).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.pix2ang"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.pix2vec-Tuple{Integer, Integer}","page":"Spherical sampling","text":"pix2vec([T = Float64], nside, pix; scheme = Ring()) -> NTuple{3}\n\nUnit vector to pixel pix's centre.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.pix2vec"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.ring2nest-Tuple{Integer, Integer}","page":"Spherical sampling","text":"ring2nest(nside, pix) -> Int\nnest2ring(nside, pix) -> Int\n\nConvert a 0-based pixel index between the two orderings. Both need nside to be a power of two, which is the condition for the nested quadtree to exist.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.ring2nest"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.ring_info-Tuple{Integer, Integer}","page":"Spherical sampling","text":"ring_info([T = Float64], nside, ring) -> NamedTuple\n\nWhat HEALPix ring ring ∈ 1:(4·nside-1) contains, counted from the north pole: startpix (the 0-based RING index of its first pixel, matching ang2pix), ringpix (how many pixels it holds), colatitude, latitude, and shifted — whether its pixel centres are offset half a pixel in ϕ.\n\nRing width grows 4, 8, … through the polar cap, is 4·nside across the equatorial belt, and shrinks again symmetrically, so this is how to walk a HEALPix map ring by ring without decoding every pixel.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.ring_info"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.ring_latitudes-Tuple{FlowGeometries.SphericalSampling.AbstractReducedGaussianSampling}","page":"Spherical sampling","text":"ring_latitudes([T = Float64], sampling) -> Vector{T}\n\nThe Gaussian latitudes of a reduced Gaussian grid's rings, north to south.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.ring_latitudes"},{"category":"function","location":"api/sampling/#FlowGeometries.SphericalSampling.ring_range","page":"Spherical sampling","text":"ring_range(sampling, ring) -> UnitRange{Int}\nring_range(sampling, nlat, ring) -> UnitRange{Int}\n\nThe 1-based slice of the flattened point vector holding one iso-latitude ring, north to south — the indices spherical_points writes that ring into.\n\nO(1) for every sampling, so a ring-by-ring pass is a loop over slices carrying no running offset. The octahedral rule's offset is a closed form in the ring index; the tabulated reduced grid carries cumulative counts, built once with the sampling.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.ring_range"},{"category":"function","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_axes","page":"Spherical sampling","text":"spherical_axes([T = Float64], sampling, nlat; nlon=…) -> NamedTuple{(:λ,:φ)}\n\nAllocating wrapper around spherical_axes!.\n\nThe element type leads, as it does for zeros and rand, so it takes part in dispatch and the returned eltype is known from the signature.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_axes"},{"category":"function","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_axes!","page":"Spherical sampling","text":"spherical_axes!(λ, φ, sampling, nlat; nlon=…) -> NamedTuple{(:λ,:φ)}\n\nFill preallocated longitude / geographic-latitude axes. Requires length(λ) == sz.nlon and length(φ) == sz.nlat for sz = axes_lengths(...).\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_axes!"},{"category":"function","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_points!","page":"Spherical sampling","text":"spherical_points!(λ, φ, sampling, args...) -> NamedTuple{(:λ,:φ)}\n\nFill preallocated point-coordinate buffers. See npoints for required lengths.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_points!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_points!-Tuple{AbstractVector, AbstractVector, FlowGeometries.SphericalSampling.AbstractScatteredSphericalSampling, AbstractVector, AbstractVector}","page":"Spherical sampling","text":"spherical_points!(λ_out, φ_out, ::AbstractScatteredSphericalSampling, λ, φ) -> NamedTuple\n\nCopy a scattered point set into caller-owned buffers.\n\nThe source arrays are required, a scattered sampling carrying no rule to generate points from.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_points!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_points!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}, FlowGeometries.SphericalSampling.AbstractReducedGaussianSampling}} where T<:AbstractFloat","page":"Spherical sampling","text":"spherical_points!(λ, φ, sampling::AbstractReducedGaussianSampling; scratch = nothing) -> NamedTuple\n\nRing-by-ring points of a reduced Gaussian grid, north to south, longitudes equispaced within each ring starting at zero. Buffer length is npoints.\n\nThe Gaussian latitudes need one value per ring, which cannot overlap the output here (see the note in the body), so scratch — any vector of at least nrings(sampling) elements — makes the fill allocation-free for a caller filling many grids.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_points!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_points!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}, FlowGeometries.SphericalSampling.FibonacciSampling}} where T<:AbstractFloat","page":"Spherical sampling","text":"spherical_points!(λ, φ, sampling::FibonacciSampling) -> NamedTuple\n\nThe n golden-spiral points, allocating nothing.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_points!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_points-Tuple{FlowGeometries.SphericalSampling.AbstractScatteredSphericalSampling, AbstractVector, AbstractVector}","page":"Spherical sampling","text":"spherical_points(::AbstractScatteredSphericalSampling, λ, φ) -> NamedTuple{(:λ,:φ)}\n\nA scattered sampling's points are the caller's arrays, so this hands them back. It exists so a scattered set can be driven through the same entry point as a generated one.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_points"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_points-Tuple{FlowGeometries.SphericalSampling.AbstractTensorProductSphericalSampling, Integer}","page":"Spherical sampling","text":"spherical_points([T = Float64], sampling, args...) -> NamedTuple{(:λ,:φ)}\n\nEvery point of the sampling, flattened, as longitude/latitude vectors. Allocating wrapper around spherical_points!; use npoints to size buffers for the in-place form.\n\nFor a tensor-product sampling this is the outer product of its axes, so prefer spherical_axes when the separable form will do.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_points"},{"category":"function","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_quadrature!","page":"Spherical sampling","text":"spherical_quadrature!(λ, φ, w, sampling, nlat; nlon=…) -> NamedTuple{(:λ,:φ,:w)}\n\nFill preallocated axes and latitude weights together. Buffer lengths are as for spherical_axes!, with length(w) == sz.nlat.\n\nThis is the entry point to use whenever both are needed. Gauss–Legendre nodes and weights fall out of a single root solve, but spherical_axes! keeps only the nodes and latitude_weights! only the weights — so calling them in sequence pays the O(n²) solve twice. Every other sampling has independent closed forms for the two, and gets the generic method.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_quadrature!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.spherical_quadrature-Tuple{FlowGeometries.SphericalSampling.AbstractTensorProductSphericalSampling, Integer}","page":"Spherical sampling","text":"spherical_quadrature([T = Float64], sampling, nlat; nlon=…) -> NamedTuple{(:λ,:φ,:w)}\n\nAllocating wrapper around spherical_quadrature!.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.spherical_quadrature"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.vec2pix-Tuple{Integer, Any}","page":"Spherical sampling","text":"vec2pix(nside, v; scheme = Ring()) -> Int\n\nThe 0-based index of the pixel containing direction v, which need not be normalized.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.vec2pix"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.yin_yang_panels!-Union{Tuple{T}, Tuple{AbstractVector{T}, AbstractVector{T}, AbstractMatrix{T}, AbstractMatrix{T}, Integer, Integer}} where T<:AbstractFloat","page":"Spherical sampling","text":"yin_yang_panels!(λyin, φyin, λyang, φyang, nlon, nlat) -> (; yin, yang)\n\nThe two Kageyama–Sato panels. yin is a pair of axes (nlon and nlat long): in its own frame the panel is a separable lat–lon patch. yang is that panel rotated onto the sphere, which is separable in neither global longitude nor latitude, so it is a pair of nlon × nlat fields, one (λ, φ) per cell.\n\nThe argument types carry that difference.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.yin_yang_panels!"},{"category":"method","location":"api/sampling/#FlowGeometries.SphericalSampling.yin_yang_panels-Tuple{Integer, Integer}","page":"Spherical sampling","text":"yin_yang_panels([T = Float64], nlon, nlat) -> (; yin, yang)\n\nAllocating wrapper around yin_yang_panels!.\n\n\n\n\n\n","title":"FlowGeometries.SphericalSampling.yin_yang_panels"},{"category":"section","location":"#FlowGeometries.jl","page":"Home","text":"Coordinate metrics, spherical samplings, and grid types for flow-field analysis on the plane and the sphere.\n\nThe package has no dependencies. Everything optional — spatial search, tessellation, sparse output, static vectors, FFT-based quadrature, device transfer, threading — arrives through package extensions, so you pay for exactly what you load.","title":"FlowGeometries.jl"},{"category":"section","location":"#Three-orthogonal-choices","page":"Home","text":"(Image: Six spherical samplings)\n\nThe central idea is that three questions people usually conflate are kept separate:\n\nquestion answered by examples\nWhat is the metric? Geometry CartesianGeometry, SphericalGeometry\nWhere are the points? SphericalSampling Gauss–Legendre, HEALPix, cubed sphere, icosahedral, Yin–Yang\nHow is the data stored and connected? Grids, Connectivity structured, curvilinear, unstructured\n\nA Gauss–Legendre sampling can live in a structured grid under a spherical geometry; so can a lat–lon sampling. Changing one does not force a change in the others.","title":"Three orthogonal choices"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url = \"https://github.com/jbphyswx/FlowGeometries.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"The package exports nothing. Import it qualified:\n\nusing FlowGeometries: FlowGeometries as FG\n\nA spherical grid from a spectral sampling:\n\ngeo  = FG.Geometry.SphericalGeometry()                                   # unit-agnostic; default Earth radius\ngrid = FG.Connectivity.structured_grid(FG.SphericalSampling.GaussLegendreSampling(), 64)       # 127 × 64 lon/lat\n\nFG.Grids.coords(grid, 3, 5)          # (λ = …, φ = …) — named for the geometry, not (x, y)\nFG.Grids.measure(grid, 3, 5)         # that cell's area\nsum(FG.Grids.measure(grid)) / (4π * geo.R^2)   # ≈ 1: the cells tile the sphere\n\nNodes and quadrature weights together, from a single solve:\n\nq = FG.SphericalSampling.spherical_quadrature(FG.SphericalSampling.GaussLegendreSampling(), 1024)\nq.λ, q.φ, q.w                  # longitudes, latitudes, latitude weights\nsum(q.w)                       # 2, i.e. ∫₋₁¹ dμ\n\nAn unstructured grid on a quasi-uniform sampling, with exact dual-cell areas:\n\ng = FG.Connectivity.unstructured_grid(FG.SphericalSampling.IcosahedralSampling(16))   # 2562 nodes\nsum(FG.Grids.measure(g)) / (4π * FG.Geometry.SphericalGeometry().R^2) # 1.0 to machine precision\nFG.Grids.neighbors(g, 1)                                     # this node's neighbours\n\nNeighbours on a structured grid, without materializing anything:\n\nout = Vector{Int}(undef, 8)\nn = FG.Connectivity.neighbors!(out, grid, 3, 5; stencil = FG.Stencils.Moore(1))  # writes n indices","title":"Quick start"},{"category":"section","location":"#Conventions","page":"Home","text":"Nothing is exported, and nothing is rebound at the top level. Every name is reached through the submodule that defines it: FG.Axes, FG.Geometry, FG.Stencils, FG.Discretization, FG.SphericalSampling, FG.Grids, FG.Connectivity, FG.Execution.\nCoordinates are named by the geometry. A spherical grid has grid.λ, grid.φ; a Cartesian one has grid.x, grid.y. Asking for the wrong one is an error, never a silent alias for the other.\nSpherical coordinates are the polar frame (λ, φ, r) — longitude, geographic latitude, radius — not east/north.\nCells are cell-centred. A sampling's points are cell centres, and the connectivity treats each point as one cell. The two never disagree.\nf! means it writes into your buffers and allocates nothing beyond its return value — asserted in the test suite for every such form, at more than one problem size.\nNonstandard element types work. Float32 and BigFloat are supported throughout; algorithms that cannot reach a given precision defer to ones that can.","title":"Conventions"},{"category":"section","location":"#Contents","page":"Home","text":"Pages = [\"geometry.md\", \"sampling.md\", \"grids.md\", \"connectivity.md\", \"extensions.md\", \"performance.md\", \"api.md\"]\nDepth = 2","title":"Contents"},{"category":"section","location":"extensions/#Extensions","page":"Extensions","text":"The package itself has no dependencies. Every optional capability is a package extension that loads when — and only when — you load its trigger.\n\nload this and you get\nNearestNeighbors k-d-tree neighbour construction for UnstructuredGrid, and indexed ball queries\nQuickhull spherical Voronoi dual areas for arbitrary point sets\nDelaunayTriangulation planar Voronoi areas\nSparseArrays sparse_adjacency_matrix\nStaticArrays SVector/MVector points and returns, in vector form end to end\nAbstractFFTs O(n log n) equiangular quadrature weights\nAdapt move a grid to another storage backend (GPU arrays, wrappers)\nKernelAbstractions run the index-parallel loops as device kernels\nComputationalBackends opt-in threading through the execution tags","title":"Extensions"},{"category":"section","location":"extensions/#NearestNeighbors-—-spatial-search","page":"Extensions","text":"Required to build an UnstructuredGrid from a bare point set:\n\nusing NearestNeighbors\ng = FG.Grids.UnstructuredGrid(geo, λ, φ, mask; k = 6, areas = areas)          # k nearest\ng = FG.Grids.UnstructuredGrid(geo, x, y, mask; radius = 0.02, areas = areas,  # or by radius\n                        periodic = (true, true), period = (Lx, Ly))\n\nOn a sphere the tree is built on the unit-sphere embedding, where nearest-by-chord is exactly nearest-by-great-circle — which also makes longitude wrap for free, since λ and λ+2π embed to the same point. Cartesian domains wrap by replicating the point set at the periodic images.\n\nIt also supplies the reusable index behind Connectivity.indexed, which brings a ball query on a curvilinear or node grid down to a range search:\n\nusing NearestNeighbors\ntop = FG.Connectivity.indexed(grid)                    # holds a k-d tree over the cell centres\nbuf = FG.Connectivity.ball_scratch()                   # candidate buffer, one per task\nFG.Connectivity.neighbors_within(grid, i, j; ball = r, topology = top, scratch = buf)\n\nThe same embedding serves construction and queries, and the index only ever returns a superset of the ball — the exact distance gate still decides membership — so the result is the same set of cells the scan gives. On a spheroid that superset is genuine: the ECEF chord the tree searches is a lower bound on the Vincenty geodesic, so the query over-returns and the gate trims.","title":"NearestNeighbors — spatial search"},{"category":"section","location":"extensions/#Quickhull-/-DelaunayTriangulation-—-Voronoi-areas","page":"Extensions","text":"Only needed for genuinely arbitrary point sets. Every built-in sampling has a closed-form cell area and does not touch these.\n\nusing Quickhull\ng = FG.Connectivity.unstructured_grid(FG.SphericalSampling.ScatteredSphericalSampling(), λ, φ)\n\nThe spherical dual cell of a node is the polygon through the circumcentres of its incident triangles — the dual of the convex hull on the sphere.","title":"Quickhull / DelaunayTriangulation — Voronoi areas"},{"category":"section","location":"extensions/#KernelAbstractions-—-device-execution","page":"Extensions","text":"Bulk loops here come in two shapes, and only one maps to a kernel. run_indices applies a body to one index at a time with nothing carried across indices; run_chunks hands a contiguous range to a body that accumulates across it. Loading KernelAbstractions makes the first launchable on any backend it supports; the second raises on a device backend.\n\nusing KernelAbstractions\nbackend = KernelAbstractions.CPU()          # or a vendor backend\nFG.Operators.apply_stencil!(out, field, x, 1; order = 2, backend = backend)\nFG.Connectivity.build_connectivity(grid; stencil = FG.Stencils.Moore(1), backend = backend)\n\nBoth are bit-identical to the serial result, which the suite checks on KernelAbstractions.CPU() — no GPU needed to verify that the code is device-generic. There is no per-vendor code in the package: a backend arrives from the caller and the kernel is compiled for it.\n\nA kernel cannot allocate, and the suite gates every per-cell entry point at zero bytes.\n\nBall queries follow from the same rule. Without an index a query reads only coordinates and the mask and allocates nothing, so it runs inside a launch — including through foreach_within, which becomes one body per cell:\n\nFG.Connectivity.foreach_within(grid; ball = r,\n                               topology = FG.Connectivity.MetricTopology(grid),\n                               backend = backend) do I, J, d\n    ...\nend\n\nThe indexed form stays on the host: the index is a k-d tree, a host structure, so Adapt raises on a topology carrying one. Little is lost — the tree spares a single thread an O(n) scan, and a device has a thread per cell.","title":"KernelAbstractions — device execution"},{"category":"section","location":"extensions/#SparseArrays","page":"Extensions","text":"using SparseArrays\nA = FG.Connectivity.sparse_adjacency_matrix(grid)      # SparseMatrixCSC{Bool,Int}\nA = FG.Connectivity.sparse_adjacency_matrix(conn; Ti = Int32, Tv = Float64)","title":"SparseArrays"},{"category":"section","location":"extensions/#StaticArrays","page":"Extensions","text":"Adds SVector/MVector methods that stay in vector form end to end, with no tuple round-trip:\n\nusing StaticArrays\np = FG.Grids.coords(SVector, grid, 3, 5)\nFG.Geometry.distance(geo, p1, p2)\nFG.Geometry.project_to_tangent_plane(SVector{2,Float64}, geo, centre, neighbour)\n\nA caller already working in SVectors keeps that type through both calls, with no conversion at either end. See Performance for the measured effect.","title":"StaticArrays"},{"category":"section","location":"extensions/#AbstractFFTs-—-fast-equiangular-weights","page":"Extensions","text":"Driscoll–Healy and Clenshaw–Curtis weights come from a sine series that costs O(n²) directly. Loading any FFT implementation turns it into one length-nlat transform:\n\nusing FFTW      # or any AbstractFFTs backend\nFG.SphericalSampling.latitude_weights(FG.SphericalSampling.DriscollHealySampling(), 4096)   # one length-4096 transform\n\nThe trigger is AbstractFFTs, so any backend serves. With no backend loaded, an angle-addition recurrence gives the same weights to 1.4e-14 — correctness never depends on the extension.","title":"AbstractFFTs — fast equiangular weights"},{"category":"section","location":"extensions/#Adapt-—-device-transfer","page":"Extensions","text":"using Adapt, CUDA\ndev = adapt(CuArray, grid)\n\nHandles all three grid types plus CSRConnectivity and IndexTopology. A SeparableMeasure moves its factors, so the device receives O(∑ Nᵈ) numbers, and AllActive carries only its size.","title":"Adapt — device transfer"},{"category":"section","location":"extensions/#ComputationalBackends-—-threading","page":"Extensions","text":"using ComputationalBackends: ThreadedBackend\nFG.SphericalSampling.cubed_sphere_points(512; backend = ThreadedBackend())\nFG.Grids.CurvilinearGrid(geo, λ, φ, mask; backend = ThreadedBackend())\nFG.Connectivity.build_connectivity(grid; backend = ThreadedBackend())\n\nSerial by default. Results are bit-identical to serial. See Performance for the measured speedups and for which kernels are threaded.","title":"ComputationalBackends — threading"}]
}
